---
phase: 01-inequality-engine-ineq-01-ineq-02-ineq-03-out-01
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - inequality.py
  - reports/inequality_sad_code.csv
  - reports/inequality_sad_sam.csv
  - reports/inequality_samcode_skew.csv
  - reports/inequality_expansion.csv
  - reports/lorenz_sad_code_sentence.csv
  - reports/INEQUALITY.md
autonomous: true
requirements: [INEQ-01, INEQ-02, INEQ-03, OUT-01]

must_haves:
  truths:
    - "Running `python3 inequality.py` (from the mini-inequality study root) exits 0, computes GOLD/benchmark inequality only (NO system/result files read), writes the CSVs + reports/INEQUALITY.md, and runs the sanity CHECK by default (INEQ-01, INEQ-02, INEQ-03, OUT-01)."
    - "The per-sentence enrolled sad-code Gini reproduces eval.tex tab:sent_gini within 0.005: 0.331 (MediaStore), 0.448 (TeaStore), 0.645 (Teammates), 0.472 (BBB), 0.527 (JabRef); Top-3 % reproduces 27.1/35.2/20.3/21.3/70.0 (INEQ-02)."
    - "The SAM-CODE files-per-architectural-element fan-out reproduces eval.tex tab:samcode_skew: Gini 0.400/0.694/0.452/0.513/0.612 (within 0.005); AE counts 19/19/14/22/6; enrolled file counts 60/164/1,616/730/1,956; Max 16/64/348/94/972 (INEQ-01, structural amplification driver)."
    - "The enrollment expansion reproduces eval.tex tab:enrollment EXACTLY: raw total 525, enrolled total 18,660, average factor 35.5x; per-project enrolled 59/707/8,097/1,529/8,268 and factors 1.0x/10.1x/35.5x/11.6x/217.6x; sad-code only (INEQ-03)."
    - "The structural component->file amplification is reported as a GOLD property: one component decision expands to up to max|files(m)| file-level pairs (worst case 972 = JabRef, 348 = Teammates); aggregate amplification = the enrollment factor. The engine reads NO results/ files and contains NO TransArc-specific logic (INEQ-03)."
    - "The Gini of #distinct-sentences-per-component is computed for sad-code (component level, mapped-only universe) and sad-sam using the copied `_gini`, establishing by-construction agreement with component_suite.gold_gini (INEQ-01)."
    - "On any sanity mismatch the engine prints a diff table and exits non-zero; `python3 inequality.py --check-only` runs just the gate (OUT-01)."
    - "Zero imports from src/ or mini-src/ (or any non-stdlib module); an AST scan finds only stdlib imports (OUT-01)."
  artifacts:
    - path: "inequality.py"
      provides: "stdlib-only, self-contained, GOLD-only inequality engine: gold concentration (Gini/Lorenz/top-k/min-median-max), enrollment expansion, structural component->file fan-out amplification, + sanity gate"
      min_lines: 320
      contains: "argparse"
    - path: "reports/inequality_sad_code.csv"
      provides: "per-project + AVG sad-code inequality (per-sentence enrolled Gini/Top-3/min-median-max + per-component + per-file)"
    - path: "reports/inequality_sad_sam.csv"
      provides: "per-project + AVG sad-sam per-component inequality (Gini/top-k/min-median-max)"
    - path: "reports/inequality_samcode_skew.csv"
      provides: "SAM-CODE files-per-AE fan-out (AEs, enrolled, min/median/max, Gini, Top-3 conc) reproducing tab:samcode_skew — the structural amplification driver"
    - path: "reports/inequality_expansion.csv"
      provides: "enrollment expansion (raw, dir entries, enrolled, factor) per project + Total reproducing tab:enrollment"
    - path: "reports/lorenz_sad_code_sentence.csv"
      provides: "pgfplots-friendly Lorenz curve points (cum_pop_pct, cum_mass_pct) for the per-sentence enrolled sad-code distribution"
    - path: "reports/INEQUALITY.md"
      provides: "tables-first markdown report with one-line headline prose per section + the CHECK results"
  key_links:
    - from: "inequality.py"
      to: "mini-src/metrics.py definitions (enroll, normalize_path, load_code_model_files, gold loaders, GS path maps)"
      via: "COPY verbatim into inequality.py (NO import — isolation rule)"
      pattern: "def enroll("
    - from: "inequality.py"
      to: "src/bias/component_suite.py `_gini` + `_code_inputs` mapped-only universe rule"
      via: "COPY `_gini` verbatim; mirror the mapped-only collapse (drop files with no SAM-CODE component)"
      pattern: "def _gini("
---

<objective>
Create `inequality.py` at the mini-inequality study root: a self-contained,
stdlib-only engine that measures ONLY the GOLD trace-link concentration
inequality of the ARDoCo benchmark (a property of the dataset) and reproduces the
paper's Chapter-1 distributional-inequality tables, then self-checks every
reproduced number against the frozen `eval.tex` literals and fails loud on any
mismatch.

Scope re-pivot (per user directive): the engine uses NO TransArc-/system-specific
inputs — it reads no `results/` files. INEQ-03's "component->file cascade" is the
GOLD-structural amplification driver (the SAM-CODE files-per-component fan-out),
not a measured cascade over a system's errors.

Output (all relative to the study root `mini-inequality/`):
- `inequality.py` (new)
- `reports/inequality_sad_code.csv`, `reports/inequality_sad_sam.csv`,
  `reports/inequality_samcode_skew.csv`, `reports/inequality_expansion.csv`,
  `reports/lorenz_sad_code_sentence.csv` (generated)
- `reports/INEQUALITY.md` (generated)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**ISOLATION (HARD) — read before executing any task:**
- This phase belongs to the isolated `mini-inequality/` sub-project. The study
  root is `/mnt/hostshare/ardoco-home/transarc-emp/mini-inequality`.
- Run every command with that working directory (each `<verify>` starts with
  `cd /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality`).
- NEVER read, write, or commit under the repo-root `.planning/` (tracks the
  unrelated active **v1.2** milestone). Commit only `mini-inequality/**`.
- `inequality.py` lives at the study root (path `inequality.py`, NOT
  `mini-inequality/inequality.py`). Do not create a nested
  `mini-inequality/mini-inequality/` path.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-inequality-engine-ineq-01-ineq-02-ineq-03-out-01/01-CONTEXT.md

CONSTRAINTS:
- Stdlib only: `csv, json, math, os, sys, argparse` + `from collections import
  defaultdict, Counter` + `from pathlib import Path`. No third-party deps.
- ZERO imports from `src/`, `mini-src/`, or any project module. Definitions are
  COPIED verbatim from the reference files (READ-ONLY, one level up from the
  study root): `../mini-src/metrics.py`, `../src/bias/component_suite.py`,
  `../writing/eval.tex`.
- GOLD ONLY: the engine reads benchmark gold standards + `.acm` code models. It
  reads NO `results/` files and contains NO TransArc-/system-specific logic.
- No benchmark-derived word lists — distributional stats only.
- Root resolution mirrors mini-src/metrics.py: `_ARDOCO_HOME =
  Path(__file__).resolve().parents[2]`;
  `BENCHMARK = $TRANSARC_BENCHMARK or _ARDOCO_HOME/"ardoco/core/tests-base/src/main/resources/benchmark"`;
  `REPORTS = Path(__file__).resolve().parent / "reports"`. (No RESULTS constant.)

DEFINITIONS TO COPY (verbatim, then verify agreement — do NOT import):
From `../mini-src/metrics.py`:
  PROJECTS; GS_SAD_SAM, GS_SAM_CODE, GS_SAD_CODE, ACM_FILES (path dicts, lines 69-96);
  normalize_path (146-149); enroll (152-162); _cell (167-173);
  load_code_model_files (176-190); load_gs_sad_sam (193-196);
  load_gs_sad_code_raw (199-203); load_file_to_comps pattern (206-216).
From `../src/bias/component_suite.py`:
  _gini(values) (156-162); the `_code_inputs` mapped-only universe rule (208-232):
  collapse (sentence, file) -> (sentence, component) via the enrolled SAM-CODE map
  and DROP files with no SAM-CODE component (apply consistently so sad-code
  component-level work agrees with gold_gini).

NEW SELF-CONTAINED LOADER (write in inequality.py, mirroring load_file_to_comps):
  load_sam_code(project, code_files) -> (names: dict ae_id->ae_name,
    sam_enrolled: set[(ae_id, file)]) by reading GS_SAM_CODE,
    `raw.add((ae_id, normalize_path(ce_ids or ce_id)))`, then `enroll(raw, code_files)`.
    Derive model_to_files = defaultdict(set) (ae->files) and
    file_to_comps = defaultdict(set) (file->{names.get(ae,ae)}).
  (There is NO result loader — the engine does not read results/.)

GENERIC INEQUALITY HELPERS (write in inequality.py):
  summary_stats(values) -> dict(n, total, min, median, max).
  top_k_share(values, k) -> sum of k largest / total (0.0 if total==0).
  palma(values) -> top-10% mass / bottom-40% mass; bottom 0 -> float('inf') ("inf").
  lorenz_points(values) -> list[(cum_pop_pct, cum_mass_pct)] over ascending values,
    starting (0,0), ending (1,1).

FROZEN eval.tex literals the engine MUST reproduce (embed as EXPECTED constants
for the CHECK; cite the source table in a comment). ALL are GOLD/benchmark:
  tab:sent_gini (per-sentence enrolled sad-code links): Gini = {mediastore:0.331,
    teastore:0.448, teammates:0.645, bigbluebutton:0.472, jabref:0.527};
    Top-3 % = {27.1, 35.2, 20.3, 21.3, 70.0}.
  tab:samcode_skew (SAM-CODE files per AE — the amplification driver):
    Gini = {0.400,0.694,0.452,0.513,0.612}; AEs = {19,19,14,22,6};
    Enrolled = {60,164,1616,730,1956}; Max = {16,64,348,94,972}.
  tab:enrollment: Raw = {57,70,228,132,38} (total 525); Enrolled =
    {59,707,8097,1529,8268} (total 18,660); Factor = {1.0,10.1,35.5,11.6,217.6};
    average factor 35.5.
  (DROPPED: tab:amplification 36->3,457 — TransArc-specific, out of scope.)
  NOTE: load_gs_sad_code_raw returns a SET so duplicate rows collapse; the CHECK
  compares ENROLLED totals/factors exactly (raw is informational). These targets
  were pre-verified reproducible from the copied metrics.py definitions (probe
  2026-06-21) — they WILL pass if the copy is faithful.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffolding — copied primitives, gold loaders, generic inequality helpers, CLI skeleton (GOLD-only)</name>
  <read_first>
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-src/metrics.py (lines 53-96 roots+path dicts, 146-216 normalize_path/enroll/_cell/loaders/load_file_to_comps, 358-396 CLI/main idiom)
    - /mnt/hostshare/ardoco-home/transarc-emp/src/bias/component_suite.py (lines 156-162 `_gini`, 208-232 `_code_inputs` mapped-only universe, 274-296 table/csv writers)
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality/.planning/phases/01-inequality-engine-ineq-01-ineq-02-ineq-03-out-01/01-CONTEXT.md (locked decisions, re-pivot)
  </read_first>
  <files>inequality.py</files>
  <action>
Create `inequality.py` at the study root. Build shared scaffolding; leave
per-project metric computation as stubs filled in Tasks 2-4 so the file runs.

1. Module docstring: self-contained stdlib-only GOLD-inequality engine; mirrors
   mini-src/metrics.py; copies definitions (no imports); GOLD/benchmark only (no
   results/); sanity-checked vs writing/eval.tex Ch1 tables.
2. Stdlib imports only: `csv, json, math, os, sys, argparse`,
   `from collections import defaultdict, Counter`, `from pathlib import Path`.
3. Root constants (mirror metrics.py 57-65): `_ARDOCO_HOME =
   Path(__file__).resolve().parents[2]`; `BENCHMARK` with env override;
   `REPORTS = Path(__file__).resolve().parent / "reports"` (mkdir at write time).
   Do NOT define a RESULTS constant — the engine reads no results.
4. COPY verbatim from metrics.py: `PROJECTS`, `GS_SAD_SAM`, `GS_SAM_CODE`,
   `GS_SAD_CODE`, `ACM_FILES`, `normalize_path`, `enroll`, `_cell`,
   `load_code_model_files`, `load_gs_sad_sam`, `load_gs_sad_code_raw`. COPY `_gini`
   verbatim from component_suite.py.
5. Write `load_sam_code(project, code_files)` per <context> (names, sam_enrolled;
   derive model_to_files + file_to_comps). NO result loader.
6. Write generic helpers: `summary_stats`, `top_k_share`, `palma`, `lorenz_points`,
   and `_fmt(v)` (3-dp float / str passthrough).
7. Stub per-project fns returning `{"project": project}`:
   `compute_sad_code_dist`, `compute_sad_sam_dist`, `compute_samcode_skew`,
   `compute_expansion`.
8. Write `write_csv(path, header, rows)` (stdlib csv; header + per-project rows +
   an AVG/Total row where meaningful — comment which cols are summed vs averaged).
9. `main()`: argparse `--task` (choices `sad-code, sad-sam, both`; default `both`),
   `--project` (validate vs PROJECTS else `sys.exit(2)`), `--check-only`,
   `--no-check`. Default: compute all, write all CSVs + INEQUALITY.md, then CHECK
   (Task 4) unless `--no-check`. Wire control flow with stubs so it runs/exits 0.
   `if __name__ == "__main__": main()`.
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality && python3 inequality.py --task both --no-check && python3 -c "
import ast
t=ast.parse(open('inequality.py').read()); mods=set()
for n in ast.walk(t):
    if isinstance(n,ast.Import):
        for a in n.names: mods.add(a.name.split('.')[0])
    if isinstance(n,ast.ImportFrom) and n.module: mods.add(n.module.split('.')[0])
allowed={'csv','json','math','os','sys','argparse','collections','pathlib'}
assert not (mods-allowed), ('non-stdlib imports: %s'%(mods-allowed))
src=open('inequality.py').read()
assert 'results' not in src.lower() or 'RESULTS' not in src, 'no RESULTS constant expected'
print('IMPORTS_OK', sorted(mods))
" && grep -q 'def _gini' inequality.py && grep -q 'def enroll' inequality.py && grep -q 'def load_sam_code' inequality.py && ! grep -qi 'sadSamTlr\|load_result' inequality.py && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python3 inequality.py --task both --no-check` exits 0 from the study root.
    - AST scan finds only {csv,json,math,os,sys,argparse,collections,pathlib}.
    - Defines `_gini, enroll, normalize_path, load_code_model_files, load_gs_sad_sam, load_gs_sad_code_raw, load_sam_code, summary_stats, top_k_share, palma, lorenz_points` (grep each).
    - Contains NO `sadSamTlr`, no `load_result`, no result-reading path (grep returns nothing).
    - `python3 inequality.py --project bogus` exits non-zero (2).
  </acceptance_criteria>
  <done>Engine imports/runs stdlib-only with copied primitives + gold loaders + helpers + CLI; no result-reading code.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: INEQ-01 + INEQ-02 gold distributions (per-sentence, per-component, per-file, samcode-skew) + Lorenz</name>
  <read_first>
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality/inequality.py (extend stub computation fns)
    - /mnt/hostshare/ardoco-home/transarc-emp/src/bias/component_suite.py (lines 165-192 gold_by_c idiom, 208-232 `_code_inputs` mapped-only collapse)
    - /mnt/hostshare/ardoco-home/transarc-emp/writing/eval.tex (lines 241-260 tab:sent_gini; 191-210 tab:samcode_skew)
  </read_first>
  <files>inequality.py</files>
  <action>
A. `compute_sad_code_dist(project)`:
   - `code_files = load_code_model_files(project)`;
     `enrolled = enroll(load_gs_sad_code_raw(project), code_files)` -> set[(sentence,file)].
   - PER-SENTENCE (INEQ-02, tab:sent_gini headline): `per_sent =
     Counter(s for (s,f) in enrolled)`; values=list(...); emit n_sents, summary_stats,
     gini=_gini(values), top3_pct=100*top_k_share(values,3), palma=palma(values).
   - PER-FILE (INEQ-02, supplementary): `per_file = Counter(f for (s,f) in enrolled)`;
     same stat bundle.
   - PER-COMPONENT (INEQ-01, component_suite universe): build file_to_comps via
     load_sam_code; collapse enrolled to (sentence, component) DROPPING files with
     no component (mirror `_code_inputs`); canonical unit = #distinct sentences per
     component -> gini + top-1/top-3 + summary_stats. Supplementary units over the
     same collapsed set: #links per component and #files per component (extra cols).
   - Return one flat dict for the CSV.
B. `compute_sad_sam_dist(project)`:
   - `gold = load_gs_sad_sam(project)` -> set[(modelElementID, sentence)].
   - names from load_sam_code (ae_id->ae_name) for name-collapse, else fall back to
     id (mirror component_suite `names.get(c,c)`). #distinct sentences per component
     -> gini, top-1/top-3, summary_stats (INEQ-01; by-construction gold_gini agreement).
C. `compute_samcode_skew(project)` (INEQ-01 + INEQ-03 driver, tab:samcode_skew):
   - `names, sam_enrolled = load_sam_code(project, load_code_model_files(project))`;
     model_to_files; values=[len(fs) for fs in model_to_files.values()]; emit
     AEs=len(values), enrolled=sum(values), summary_stats (min/median/max),
     gini=_gini(values), top3_conc_pct=100*top_k_share(values,3).
D. Wire `main()` to write: `reports/inequality_sad_code.csv` (per-project + AVG;
   per-sentence + per-file + per-component cols), `reports/inequality_sad_sam.csv`,
   `reports/inequality_samcode_skew.csv`, and `reports/lorenz_sad_code_sentence.csv`
   (= lorenz_points of per-sentence enrolled values, all projects, cols
   `project,cum_pop_pct,cum_mass_pct`). Round Gini/shares to 3 dp; counts as ints.
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality && python3 inequality.py --task both --no-check && python3 -c "
import csv
sc={r['project']:r for r in csv.DictReader(open('reports/inequality_sad_code.csv'))}
sk={r['project']:r for r in csv.DictReader(open('reports/inequality_samcode_skew.csv'))}
exp_sent={'mediastore':0.331,'teastore':0.448,'teammates':0.645,'bigbluebutton':0.472,'jabref':0.527}
exp_skew={'mediastore':0.400,'teastore':0.694,'teammates':0.452,'bigbluebutton':0.513,'jabref':0.612}
exp_max={'mediastore':16,'teastore':64,'teammates':348,'bigbluebutton':94,'jabref':972}
def col(row,must):
    cs=[k for k in row if all(t in k.lower() for t in must)]
    assert cs, ('no col %s in %s'%(must,list(row))); return cs[0]
gs=col(next(iter(sc.values())),['sent','gini'])
for p,g in exp_sent.items(): assert abs(float(sc[p][gs])-g)<=0.005,(p,sc[p][gs],g)
gk=col(next(iter(sk.values())),['gini']); mk=col(next(iter(sk.values())),['max'])
for p,g in exp_skew.items(): assert abs(float(sk[p][gk])-g)<=0.005,(p,sk[p][gk],g)
for p,m in exp_max.items(): assert int(float(sk[p][mk]))==m,(p,sk[p][mk],m)
import os; assert os.path.exists('reports/lorenz_sad_code_sentence.csv')
print('DIST_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - sad-code per-sentence Gini = 0.331/0.448/0.645/0.472/0.527 (±0.005).
    - samcode_skew Gini = 0.400/0.694/0.452/0.513/0.612 (±0.005); Max = 16/64/348/94/972; AEs 19/19/14/22/6; enrolled 60/164/1616/730/1956.
    - inequality_sad_code.csv has per-sentence + per-file + per-component cols; inequality_sad_sam.csv has a per-component Gini col; both have an AVG row.
    - lorenz_sad_code_sentence.csv exists (project,cum_pop_pct,cum_mass_pct), final mass ~1.0.
  </acceptance_criteria>
  <done>Gold distributions reproduce tab:sent_gini and tab:samcode_skew (incl. Max fan-out) within tolerance; per-component + per-file dists + Lorenz emitted.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: INEQ-03 — enrollment expansion (tab:enrollment) + structural component->file amplification (GOLD fan-out, no results)</name>
  <read_first>
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality/inequality.py (extend compute_expansion stub)
    - /mnt/hostshare/ardoco-home/transarc-emp/writing/eval.tex (lines 50-78 tab:enrollment + prose; 105-108 illusory-precision; 234-260 sentence-level prose)
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-src/metrics.py (lines 152-162 enroll, 199-216 raw sad-code loader)
  </read_first>
  <files>inequality.py</files>
  <action>
A. `compute_expansion(project)` (reproduce tab:enrollment, sad-code ONLY):
   - `raw = load_gs_sad_code_raw(project)`; `raw_n = len(raw)`.
   - `dir_entries = sum(1 for (s,p) in raw if p.endswith("/"))`;
     `dir_pct = 100*dir_entries/raw_n`.
   - `code_files = load_code_model_files(project)`; `enrolled = enroll(raw, code_files)`;
     `enrolled_n = len(enrolled)`; `factor = enrolled_n / raw_n`.
   Emit cols: project, raw, dir_entries, dir_pct, enrolled, factor.
   "Total" row: raw=sum, enrolled=sum, factor = total_enrolled/total_raw (35.5x).
   Write `reports/inequality_expansion.csv`.
B. Structural amplification (GOLD, NO results — derive from samcode fan-out of
   Task 2's compute_samcode_skew, reusing model_to_files via load_sam_code):
   For each project compute `mean_fanout = enrolled_files / n_AEs`,
   `max_fanout = max(|files(m)|)`, and note `aggregate_amplification = enrollment
   factor` (the dataset-level rate at which one component decision becomes many
   file pairs). Provide a function `amplification_summary(project) -> dict(project,
   n_components, mean_fanout, max_fanout, enrollment_factor)`. This is the
   re-pivoted INEQ-03 "component->file" driver — purely structural. It will be
   rendered in INEQUALITY.md by Task 4 (no separate CSV needed; if convenient,
   include mean_fanout/max_fanout as extra columns in inequality_samcode_skew.csv).
   Add a top-of-file comment: "INEQ-03 cascade re-pivoted to GOLD structural
   amplification (files-per-component fan-out); NO system results, NO TransArc
   error attribution (user directive 2026-06-21)."
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality && python3 inequality.py --task both --no-check && python3 -c "
import csv
ex={r['project']:r for r in csv.DictReader(open('reports/inequality_expansion.csv'))}
def tot(d):
    for k in ('Total','AVG','Average'):
        if k in d: return d[k]
    raise SystemExit('no total row')
ec=[c for c in next(iter(ex.values())) if 'enroll' in c.lower()][0]
assert int(float(tot(ex)[ec]))==18660, tot(ex)[ec]
exp_enr={'mediastore':59,'teastore':707,'teammates':8097,'bigbluebutton':1529,'jabref':8268}
for p,v in exp_enr.items(): assert int(float(ex[p][ec]))==v,(p,ex[p][ec],v)
rc=[c for c in next(iter(ex.values())) if c.lower()=='raw'][0]
assert sum(int(float(ex[p][rc])) for p in exp_enr)==525
fc=[c for c in next(iter(ex.values())) if 'factor' in c.lower()][0]
assert abs(float(ex['jabref'][fc])-217.6)<0.2, ex['jabref'][fc]
print('EXPANSION_OK')
" && grep -qi 're-pivot\|structural amplification\|NO system results\|no.*results' inequality.py && echo TASK3_OK</automated>
  </verify>
  <acceptance_criteria>
    - inequality_expansion.csv: per-project enrolled 59/707/8097/1529/8268; Total enrolled 18,660; raw total 525; Total factor ≈ 35.5; jabref factor ≈ 217.6; sad-code only (no sad-sam row).
    - A structural amplification summary (mean/max fan-out per project, aggregate = enrollment factor) is computed from GOLD fan-out (max 972 JabRef / 348 Teammates).
    - inequality.py reads NO results/ files (grep finds no `sadSamTlr`/`results/`/`load_result`) and carries the re-pivot comment.
  </acceptance_criteria>
  <done>Enrollment expansion reproduces tab:enrollment exactly (18,660 / 35.5x / 217.6x) and the component->file amplification is reported as a pure GOLD structural fan-out (max 972/348); no system results read.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: INEQUALITY.md report (tables-first + headline prose) + sanity CHECK gate (fail loud) + --check-only</name>
  <read_first>
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality/inequality.py (add report + check)
    - /mnt/hostshare/ardoco-home/transarc-emp/writing/eval.tex (lines 51-78, 234-260 prose headlines to mirror)
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-src/check.py (workspace sanity-check pattern)
  </read_first>
  <files>inequality.py</files>
  <action>
A. `write_report()` -> `reports/INEQUALITY.md`: tables-first markdown, one section
   per family, each prefixed by a single headline line, e.g.:
   - "## Per-sentence gold concentration — Gini 0.331 (MediaStore) → 0.645 (Teammates)"
   - "## Enrollment expansion — 525 raw decisions → 18,660 enrolled file links (35.5× avg, up to 217.6× on JabRef)"
   - "## Structural component→file amplification — files-per-component Gini 0.400 → 0.694; one decision expands to up to 972 file pairs (JabRef logic), 348 (Teammates ui)"
   - "## Per-component & per-file concentration — supplementary skew views"
   Each section embeds a small markdown table from the matching CSV (per-project +
   aggregate). Add a "Sanity check" section: PASS/FAIL + tolerance. Note any raw-count
   discrepancy. State explicitly that NO system results were used and that
   eval.tex tab:amplification (36→3,457) is a TransArc empirical quantity excluded
   from this gold-only study.
B. `EXPECTED` dict — embed the frozen GOLD literals from <context> (tab:sent_gini
   Gini + Top-3 %; tab:samcode_skew Gini + AEs + enrolled + Max; tab:enrollment
   enrolled per-project + total 18,660 + factors), each annotated with its source
   table label in a comment. (Do NOT include tab:amplification.)
C. `run_check()` -> bool: recompute each engine value and compare:
   - Gini comparisons: abs diff ≤ 0.005.
   - Integer counts (enrolled per-project + total 18,660; AE counts; samcode
     enrolled + Max; raw total 525): EXACT equality.
   - Build + print a diff table (metric, project, expected, computed, delta, ok);
     return False if any row fails.
D. Wire `main()`:
   - default: after writing CSVs + report, call `run_check()`; if False, print
     "SANITY CHECK FAILED" and `sys.exit(1)`.
   - `--check-only`: run ONLY `run_check()` (recompute in-memory) and exit 0/1.
   - `--no-check`: skip the gate.
   On success print "SANITY CHECK PASSED (tol: Gini≤0.005, counts exact)".
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality && python3 inequality.py && test -f reports/INEQUALITY.md && grep -q '0.331' reports/INEQUALITY.md && (grep -q '18,660' reports/INEQUALITY.md || grep -q '18660' reports/INEQUALITY.md) && grep -q '972' reports/INEQUALITY.md && python3 inequality.py --check-only && echo CHECK_ONLY_OK && ! grep -qi 'sadSamTlr\|load_result' inequality.py && echo NO_RESULTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python3 inequality.py` (no flags) exits 0 AND prints "SANITY CHECK PASSED"; reports/INEQUALITY.md exists with headline numbers 0.331, 18,660 (or 18660), 972.
    - `python3 inequality.py --check-only` exits 0 and prints the diff/PASS table.
    - run_check enforces Gini tol ≤0.005 and EXACT integer counts (compares enrolled total to 18660 with `==`).
    - INEQUALITY.md is tables-first with one-line headlines, records the tolerance, states no system results were used, and notes tab:amplification is excluded.
  </acceptance_criteria>
  <done>Engine writes a paper-facing tables-first GOLD-only report with headline prose and self-validates every reproduced number against the frozen eval.tex GOLD literals, failing loud on mismatch; --check-only runs just the gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| filesystem → script | Reads project-owned benchmark gold CSVs + `.acm` code models only. All local, offline. No system/result inputs. |
| CLI args → script | `--task` (argparse `choices`), `--project` (validated vs PROJECTS), boolean flags. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-01-01 | Tampering/DoS | missing/malformed gold CSV | mitigate | Loaders are defensive; a missing benchmark file raises a clear error (offline, project-owned). |
| T-01-02 | Spoofing/Integrity | reproduced numbers silently drift from the paper | mitigate | The sanity CHECK fails loud (exit 1) on any Gini drift >0.005 or count mismatch — the core integrity control. |
| T-01-03 | Elevation/Injection | `--project` used in dict lookup | mitigate | Validated against PROJECTS allow-list; exit 2 otherwise; `--task` constrained by choices. |

Surface is minimal: offline stdlib script, local read-only GOLD inputs, writes only under the study `reports/`. No high-severity threats.
</threat_model>

<verification>
- `python3 inequality.py` exits 0, writes the 5 CSVs + reports/INEQUALITY.md, prints "SANITY CHECK PASSED".
- Per-sentence enrolled sad-code Gini = tab:sent_gini (0.331/0.448/0.645/0.472/0.527) ±0.005; Top-3 % matches.
- SAM-CODE files-per-AE = tab:samcode_skew (Gini 0.400/0.694/0.452/0.513/0.612 ±0.005; AEs + enrolled + Max exact).
- Enrollment expansion = tab:enrollment exactly (enrolled 59/707/8097/1529/8268; total 18,660; 35.5× avg; JabRef 217.6×).
- Structural component→file amplification reported from GOLD fan-out (max 972/348); aggregate = enrollment factor.
- Per-component (#sentences) Gini computed for sad-code (mapped-only) and sad-sam via copied `_gini`.
- `--check-only` runs just the gate; mismatch -> exit 1 (fail loud).
- AST scan: stdlib-only imports; engine reads NO results/ files (no `sadSamTlr`/`load_result`).
</verification>

<success_criteria>
- INEQ-01: per-component gold inequality (Gini/Lorenz/top-k/min-median-max) for sad-code & sad-sam, 5 projects, CSV. ✓ (Task 2)
- INEQ-02: per-file concentration + per-sentence links/sentence distribution (sad-code), incl. 0.331→0.645. ✓ (Task 2)
- INEQ-03: per-project enrollment expansion factor + structural component→file amplification (GOLD fan-out; TransArc cascade dropped per re-pivot). ✓ (Task 3)
- OUT-01: self-contained stdlib-only engine + reports, no cross-module imports, no system results, sanity-checked vs canonical GOLD numbers. ✓ (Tasks 1+4)
</success_criteria>

<output>
After completion, create
`.planning/phases/01-inequality-engine-ineq-01-ineq-02-ineq-03-out-01/01-01-SUMMARY.md`
</output>
