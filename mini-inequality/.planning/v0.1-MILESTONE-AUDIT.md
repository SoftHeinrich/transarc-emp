---
milestone: v0.1
milestone_name: Data-Inequality Mini-Study
audited: 2026-06-21
status: passed
score: 9/9 requirements delivered & verified
---

# Milestone Audit — v0.1 Data-Inequality Mini-Study

**Verdict: PASSED.** All 9 requirements delivered, each backed by an executed,
gate-verified artifact. The study is self-contained (stdlib only, no `src/`/
`mini-src/` imports), gold/benchmark-only (no system results), and every reported
number is sanity-checked against the frozen `writing/eval.tex` Chapter-1 literals.

## Requirement Coverage

| Req | Description | Phase | Evidence | Status |
|-----|-------------|-------|----------|--------|
| INEQ-01 | Per-component gold inequality (Gini/Lorenz/top-k/min-median-max), both tasks, 5 projects → CSV | 1 | `inequality.py` + `inequality_sad_code.csv`/`inequality_sad_sam.csv`/`inequality_samcode_skew.csv`/`lorenz_*.csv` | ✓ |
| INEQ-02 | Per-file concentration + per-sentence links/sentence distribution (sad-code) | 1 | per-sentence Gini 0.331→0.645 reproduced exactly; per-file cols | ✓ |
| INEQ-03 | Enrollment expansion + component→file amplification | 1 | `inequality_expansion.csv` (525→18,660, 217.6×) + structural fan-out (max 972); TransArc cascade re-pivoted out per user | ✓ (re-scoped) |
| OUT-01 | Self-contained stdlib engine + reports, sanity-checked | 1 | fail-loud gate reproduces all eval.tex Ch1 gold literals; AST stdlib-only | ✓ |
| CLAIM-01 | Claims checklist with source locations | 2 | `CLAIM_CHECK.md` Claims table (C1–C8) | ✓ |
| CLAIM-02 | MATCH/MISMATCH/STALE labels, paper vs computed | 2 | 6 MATCH, 1 PARTIAL (C7, documented), 1 SYSTEM-SPECIFIC (cascade) | ✓ |
| CLAIM-03 | Resolve gold-derivable `XX` placeholders | 2 | 5 projects, 4 metrics, 70% (JabRef); + trivial-baseline F1 0.353 (Phase 3) | ✓ |
| MOTIV-01 | Top-3/random baselines show micro-F1 inflation → suite needed | 3 | `MOTIVATION.md`: Top-3 0.353/0.381 > random; per-comp macro ~0.19 exposes it | ✓ |
| OUT-02 | Paper-ready Gini/Lorenz table (+figure) | 3 | `out02_concentration.tex`/`.csv` + `out02_lorenz.tex` (pgfplots) | ✓ |

## Accepted deviations (logged, intentional)

1. **INEQ-03 re-pivot (user directive):** the TransArc actual-error cascade
   (`tab:amplification`, 36→3,457) is system-specific and was replaced by the
   gold-structural files-per-component amplification (max fan-out 972). Recorded in
   01-CONTEXT.md, 01-01-SUMMARY.md, and labelled SYSTEM-SPECIFIC in CLAIM_CHECK.md.
2. **C7 PARTIAL:** the top-AE 44-48% claim reproduces only for JabRef; the paper's
   coarse single-AE grouping differs from the engine's multi-mapped universe.
   Documented honestly rather than forced to MATCH.
3. **Deferred placeholders:** strongest-pipeline / `\approach` file-F1 require
   published system scores — out of this gold-only study.

## Quality gates

- Isolation (HARD): every commit on `gsd/mini-data-inequality` touched only
  `mini-inequality/**`; the active repo-root v1.2 milestone was never modified. ✓
- Reproducibility: all numbers regenerate from the bundled benchmark; baselines
  seeded (`random.Random(0)`). ✓
- Faithfulness: fail-loud sanity gate (Gini ±0.005, counts exact) reproduces every
  eval.tex Ch1 gold literal. ✓
