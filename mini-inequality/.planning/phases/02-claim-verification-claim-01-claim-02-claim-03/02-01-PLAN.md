---
phase: 02-claim-verification-claim-01-claim-02-claim-03
plan: 02-01
type: execute
wave: 1
depends_on: []
files_modified:
  - claim_check.py
  - CLAIM_CHECK.md
autonomous: true
requirements: [CLAIM-01, CLAIM-02, CLAIM-03]

must_haves:
  truths:
    - "Running `python3 claim_check.py` (from the mini-inequality study root) exits 0 and writes mini-inequality/CLAIM_CHECK.md (CLAIM-01/02/03)."
    - "CLAIM_CHECK.md enumerates every gold distributional-inequality claim with its exact source location (alinker metric.tex/eval.tex/intro.tex + local writing/eval.tex Ch1) — CLAIM-01."
    - "Each claim is labelled MATCH / MISMATCH / STALE (or SYSTEM-SPECIFIC for the excluded cascade) with the paper value and the engine-computed value side by side — CLAIM-02."
    - "The gold-derivable XX placeholders are resolved with paste-ready values: 5 projects, 4 metrics, 70% gold mass on 3 sentences (JabRef); baseline/pipeline-F1 placeholders are listed as 'deferred → Phase 3' — CLAIM-03."
    - "claim_check.py reuses the study's own engine (`import inequality`) and/or reports/*.csv — it adds NO new dataset measurement and imports nothing from src/ or mini-src/."
    - "The script self-checks: any claim expected to MATCH that does not MATCH causes a non-zero exit (fail loud)."
  artifacts:
    - path: "claim_check.py"
      provides: "stdlib-only claim-audit script: hand-authored claim->source map, reuses inequality.py, emits CLAIM_CHECK.md, fail-loud self-check"
      min_lines: 120
      contains: "import inequality"
    - path: "CLAIM_CHECK.md"
      provides: "paper-claim audit: MATCH/MISMATCH/STALE table + resolved XX placeholders"
  key_links:
    - from: "claim_check.py"
      to: "inequality.py (compute_* + EXPECTED)"
      via: "import inequality (self-contained reuse, NOT a src/ or mini-src/ import)"
      pattern: "import inequality"
---

<objective>
Create `claim_check.py` at the mini-inequality study root: a stdlib-only audit that
maps every gold distributional-inequality claim in the paper to its Phase-1
computed value, labels it MATCH / MISMATCH / STALE (or SYSTEM-SPECIFIC for the
excluded TransArc cascade), resolves the gold-derivable `XX` placeholders in
`intro.tex`, and writes `mini-inequality/CLAIM_CHECK.md`. It reuses the study's own
engine (`import inequality`) — no new measurement, no `src/`/`mini-src/` imports.

Output (relative to the study root):
- `claim_check.py` (new)
- `CLAIM_CHECK.md` (generated)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**ISOLATION (HARD):** study root = `/mnt/hostshare/ardoco-home/transarc-emp/mini-inequality`.
Run every command from there. Never touch repo-root `.planning/`. `claim_check.py`
lives at the study root (NOT `mini-inequality/mini-inequality/`).
</execution_context>

<context>
@.planning/phases/02-claim-verification-claim-01-claim-02-claim-03/02-CONTEXT.md

CONSTRAINTS:
- Stdlib only; `import inequality` (the study's own engine) IS allowed — it is
  self-contained reuse. Do NOT import from `src/` or `mini-src/`.
- Reuse Phase-1 numbers: call `inequality.compute_sad_code_dist/_sad_sam_dist/
  _samcode_skew/_compute expansion` and/or read `reports/*.csv`. No new dataset stats.
- The claim->source map is HAND-AUTHORED (see 02-CONTEXT.md "Claim inventory").
- Paper sources (read-only, for the source-location strings & paper values):
  `/mnt/hostshare/ardoco-home/alinker-paper/sections/{metric,eval,intro}.tex`,
  `../writing/eval.tex`.

CLAIM INVENTORY (from 02-CONTEXT.md — author as a list of dicts):
  1. Enrollment expansion 1.0×→217.6×  | metric.tex:11; eval.tex tab:enrollment | MATCH
  2. One dir decision → hundreds of pairs (JabRef) | metric.tex:11 | MATCH (JabRef max fan-out 972)
  3. Per-component skew / long-tail BOTH tasks | metric.tex:14-16; alinker eval.tex:23,25 | MATCH
  4. Per-sentence Gini 0.331→0.645 | writing/eval.tex tab:sent_gini | MATCH
  5. 3 sentences = 70% of gold (JabRef) | writing/eval.tex:258 | MATCH (also XX resolution)
  6. SAM-CODE files-per-AE Gini 0.400→0.694; JabRef top-3 98.6% | writing/eval.tex tab:samcode_skew | MATCH
  7. SAD-CODE top AE 44-48% of gold links | writing/eval.tex tab:sadcode_conc | MATCH
  8. Cascade 36→3,457 / block correlation | writing/eval.tex tab:amplification | SYSTEM-SPECIFIC (out of scope)

PLACEHOLDERS (intro.tex) to resolve now: "XX projects" (L40) = 5; "XX complementary
metrics" (L79) = 4; "XX% concentration of gold mass on three sentences of one
project" (L64) = 70% (JabRef). DEFER (Phase 3): L17 strongest-pipeline file F1 +
"roughly XX unrecovered"; L54 \approach F1 + "XX percentage points"; L64
trivial-baseline file F1 + "within XX points" / "within XX point on one project".

LABEL RUBRIC: MATCH = computed agrees with paper (Gini ±0.005, counts exact,
factors 1-dp, % ±0.5); MISMATCH = contradicted; STALE = unfilled placeholder /
superseded; SYSTEM-SPECIFIC = TransArc actual-error quantity, recorded not audited.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: claim_check.py — audit claims, resolve placeholders, emit CLAIM_CHECK.md, self-check</name>
  <read_first>
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality/inequality.py (compute_* signatures + EXPECTED dict)
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality/.planning/phases/02-claim-verification-claim-01-claim-02-claim-03/02-CONTEXT.md (claim inventory + decisions)
    - /mnt/hostshare/ardoco-home/alinker-paper/sections/intro.tex (XX placeholder lines)
    - /mnt/hostshare/ardoco-home/transarc-emp/writing/eval.tex (lines 156-179 cascade context; 199-260 skew/sent tables for paper values)
  </read_first>
  <files>claim_check.py</files>
  <action>
Create `claim_check.py` at the study root.

1. `import inequality as ineq` (the study's own engine — self-contained reuse).
   `import sys`. Use `ineq.PROJECTS`, `ineq.EXPECTED`, and the `compute_*` fns.

2. Compute the engine values once:
   `sc = {p: ineq.compute_sad_code_dist(p) for p in ineq.PROJECTS}`;
   `sk = {p: ineq.compute_samcode_skew(p) for p in ineq.PROJECTS}`;
   `sm = {p: ineq.compute_sad_sam_dist(p) for p in ineq.PROJECTS}`;
   `ex = {p: ineq.compute_expansion(p) for p in ineq.PROJECTS}`.
   For the SAD-CODE top-AE share (claim 7), derive the top-1 component link share
   from the engine loaders: rebuild comp link counts via ineq.load_code_model_files
   + ineq.enroll(ineq.load_gs_sad_code_raw(p), code_files) + ineq.load_sam_code, then
   `ineq.top_k_share(list(comp_links.values()), 1)` (mirror compute_sad_code_dist's
   per-component collapse). Report the MAX top-1 share across projects ~0.44-0.48.

3. Author `CLAIMS` as a list of dicts, one per inventory row:
   {id, text, source, paper_value, computed (string built from the engine values),
    expect (MATCH/SYSTEM-SPECIFIC)}. Build `computed` from sc/sk/sm/ex/EXPECTED
    (e.g. expansion range = f"{ex['mediastore']['factor']:.1f}×→{ex['jabref']['factor']:.1f}×";
    per-sentence Gini range = min/max over sc[*]['sent_gini']; JabRef 3-sentence
    share = f"{sc['jabref']['sent_top3_pct']:.1f}%"; samcode Gini range; JabRef
    samcode top3_conc = f"{sk['jabref']['top3_conc_pct']:.1f}%").

4. `label(claim)` -> one of MATCH/MISMATCH/SYSTEM-SPECIFIC: for SYSTEM-SPECIFIC
   claims return as-authored. For the rest, apply the rubric numerically where a
   numeric paper_value exists (Gini ±0.005, % ±0.5, factor 1-dp, counts exact);
   for qualitative claims (skew/long-tail) MATCH iff the supporting computed metric
   is present and in the expected direction (e.g. Gini>0 and >0 for both tasks).

5. `PLACEHOLDERS` list: each {intro_loc, text, resolved_value or "deferred → Phase 3"}.
   Resolve: "XX projects"=5; "XX complementary metrics"=4; "XX% gold mass on three
   sentences (JabRef)"=f"{sc['jabref']['sent_top3_pct']:.0f}%". Mark the
   baseline/pipeline-F1 placeholders "deferred → Phase 3 (needs system scores)".

6. `write_report()` -> `CLAIM_CHECK.md` (study root): a "## Claims" markdown table
   (ID | Claim | Source | Paper value | Computed | Label) + a "## Resolved XX
   placeholders" table (intro.tex loc | placeholder | resolved value) + a short
   "## Excluded (system-specific)" note for the cascade. Include a one-line summary
   (n MATCH / n MISMATCH / n deferred / n system-specific).

7. Self-check: after labelling, if any claim with `expect == "MATCH"` is NOT labelled
   MATCH, print the offending rows and `sys.exit(1)` (fail loud). Else print
   "CLAIM CHECK OK (n MATCH, 0 unexpected MISMATCH)" and exit 0.

8. `if __name__ == "__main__":` run the audit + write_report + self-check.
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality && python3 claim_check.py && echo "RC=$?" && test -f CLAIM_CHECK.md && grep -q 'MATCH' CLAIM_CHECK.md && grep -qiE 'system.specific' CLAIM_CHECK.md && grep -q '70' CLAIM_CHECK.md && grep -qiE 'defer' CLAIM_CHECK.md && python3 -c "
import ast
t=ast.parse(open('claim_check.py').read()); m=set()
for n in ast.walk(t):
    if isinstance(n,ast.Import):
        for a in n.names: m.add(a.name.split('.')[0])
    if isinstance(n,ast.ImportFrom) and n.module: m.add(n.module.split('.')[0])
assert 'inequality' in m, 'must reuse the engine'
bad=m-{'inequality','sys','csv','json','os','argparse','collections','pathlib','math'}
assert not bad, ('unexpected imports: %s'%bad)
print('IMPORTS_OK', sorted(m))
" && echo CLAIM_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python3 claim_check.py` exits 0 and writes CLAIM_CHECK.md.
    - CLAIM_CHECK.md has a claims table with every inventory row, each with paper value + computed value + a label; the cascade row is labelled SYSTEM-SPECIFIC.
    - Resolved placeholders include 5 projects, 4 metrics, and 70% (JabRef); baseline/pipeline-F1 placeholders shown as deferred → Phase 3.
    - claim_check.py `import inequality` (reuse) and imports nothing from src/ or mini-src/.
    - Self-check is present: an expected-MATCH claim failing to match exits non-zero.
  </acceptance_criteria>
  <done>claim_check.py audits all gold inequality claims against the Phase-1 engine, resolves the gold-derivable placeholders, writes CLAIM_CHECK.md, and fails loud on an unexpected mismatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| filesystem → script | Imports the local engine + reads project-owned gold via it; reads no untrusted/network input. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-01 | Integrity | claim silently mislabelled | mitigate | Fail-loud self-check: an expected-MATCH claim that does not match exits non-zero. |
| T-02-02 | Scope | auditing a system-specific claim as if gold | mitigate | The cascade claim is explicitly labelled SYSTEM-SPECIFIC and excluded from MATCH/MISMATCH. |

Offline stdlib script; no network; minimal surface.
</threat_model>

<verification>
- `python3 claim_check.py` exits 0, writes CLAIM_CHECK.md.
- Every gold inequality claim appears with source loc + paper value + computed value + label; cascade = SYSTEM-SPECIFIC.
- XX placeholders 5 / 4 / 70% resolved; baseline/pipeline F1 deferred → Phase 3.
- Reuses `import inequality`; no src/ or mini-src/ imports.
- Self-check fails loud on an unexpected mismatch.
</verification>

<success_criteria>
- CLAIM-01: claims checklist with exact source locations. ✓
- CLAIM-02: MATCH/MISMATCH/STALE labels with paper vs computed side by side. ✓
- CLAIM-03: gold-derivable XX placeholders resolved (paste-ready); rest deferred. ✓
</success_criteria>

<output>
After completion, create
`.planning/phases/02-claim-verification-claim-01-claim-02-claim-03/02-01-SUMMARY.md`
</output>
