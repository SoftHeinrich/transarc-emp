---
phase: 01-inequality-engine-ineq-01-ineq-02-ineq-03-out-01
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - inequality.py
  - reports/inequality_sad_code.csv
  - reports/inequality_sad_sam.csv
  - reports/inequality_samcode_skew.csv
  - reports/inequality_expansion.csv
  - reports/inequality_cascade.csv
  - reports/lorenz_sad_code_sentence.csv
  - reports/INEQUALITY.md
autonomous: true
requirements: [INEQ-01, INEQ-02, INEQ-03, OUT-01]

must_haves:
  truths:
    - "Running `python3 inequality.py` (from the mini-inequality study root) exits 0, emits per-project + average inequality for sad-code (component & per-sentence & per-file) and sad-sam across all 5 projects, writes the CSVs + reports/INEQUALITY.md, and runs the sanity CHECK by default (INEQ-01, INEQ-02, OUT-01)."
    - "The per-sentence enrolled sad-code Gini reproduces eval.tex tab:sent_gini within 0.005: 0.331 (MediaStore), 0.448 (TeaStore), 0.645 (Teammates), 0.472 (BBB), 0.527 (JabRef); Top-3 % reproduces 27.1/35.2/20.3/21.3/70.0 (INEQ-02)."
    - "The SAM-CODE files-per-architectural-element Gini reproduces eval.tex tab:samcode_skew within 0.005: 0.400/0.694/0.452/0.513/0.612; AE counts 19/19/14/22/6; enrolled file counts 60/164/1,616/730/1,956 (INEQ-01)."
    - "The enrollment expansion reproduces eval.tex tab:enrollment EXACTLY: raw total 525, enrolled total 18,660, average factor 35.5x; per-project enrolled 59/707/8,097/1,529/8,268 and factors 1.0x/10.1x/35.5x/11.6x/217.6x (INEQ-03)."
    - "The component->file cascade reproduces eval.tex tab:amplification EXACTLY: 36 sad-sam FPs -> 3,457 induced file-level FPs (96.0x aggregate); per-project induced 1/0/2,395/71/990, using the bundled TransArc sad-sam results (INEQ-03)."
    - "The Gini of #distinct-sentences-per-component is computed for sad-code (component level) and sad-sam using the copied `_gini` over the same universe component_suite uses (mapped-only for sad-code), establishing by-construction agreement with component_suite.gold_gini (INEQ-01)."
    - "On any sanity mismatch the engine prints a diff table and exits non-zero; `python3 inequality.py --check-only` runs just the gate (OUT-01)."
    - "Zero imports from src/ or mini-src/ (or any non-stdlib module); `python3 -c \"import ast; ...\"` finds only stdlib imports (OUT-01)."
  artifacts:
    - path: "inequality.py"
      provides: "stdlib-only, self-contained inequality engine: gold concentration (Gini/Lorenz/top-k/min-median-max), enrollment expansion, component->file cascade, + sanity gate"
      min_lines: 350
      contains: "argparse"
    - path: "reports/inequality_sad_code.csv"
      provides: "per-project + AVG sad-code inequality (per-sentence enrolled Gini/Top-3/min-median-max + per-component + per-file)"
    - path: "reports/inequality_sad_sam.csv"
      provides: "per-project + AVG sad-sam per-component inequality (Gini/top-k/min-median-max)"
    - path: "reports/inequality_samcode_skew.csv"
      provides: "SAM-CODE files-per-AE distribution (AEs, enrolled, min/max, Gini, Top-3 conc) reproducing tab:samcode_skew"
    - path: "reports/inequality_expansion.csv"
      provides: "enrollment expansion (raw, dir entries, enrolled, factor) per project reproducing tab:enrollment"
    - path: "reports/inequality_cascade.csv"
      provides: "component->file cascade (sad-sam FPs, induced file FPs, mean amplif, max single) reproducing tab:amplification"
    - path: "reports/lorenz_sad_code_sentence.csv"
      provides: "pgfplots-friendly Lorenz curve points (cum_pop_pct, cum_mass_pct) for the per-sentence enrolled sad-code distribution"
    - path: "reports/INEQUALITY.md"
      provides: "tables-first markdown report with one-line headline prose per section + the CHECK results"
  key_links:
    - from: "inequality.py"
      to: "mini-src/metrics.py definitions (enroll, normalize_path, load_code_model_files, gold loaders, GS path maps)"
      via: "COPY verbatim into inequality.py (NO import — isolation rule)"
      pattern: "def enroll("
    - from: "inequality.py"
      to: "src/bias/component_suite.py `_gini` + `_code_inputs` universe rule"
      via: "COPY `_gini` verbatim; mirror the mapped-only collapse (drop files with no SAM-CODE component)"
      pattern: "def _gini("
    - from: "inequality.py"
      to: "TransArc sad-sam results results/<project>/sad-sam/sadSamTlr_<project>.csv"
      via: "read for the cascade FP computation only"
      pattern: "sadSamTlr_"
---

<objective>
Create `inequality.py` at the mini-inequality study root: a self-contained,
stdlib-only engine that measures the GOLD trace-link concentration inequality of
the ARDoCo benchmark and reproduces the paper's Chapter-1 distributional-inequality
tables, then self-checks every reproduced number against the frozen `eval.tex`
literals and fails loud on any mismatch.

Purpose: produce the faithful, reproducible inequality numbers (Gini, Lorenz,
top-k, enrollment expansion, component->file cascade) that ground every paper
claim — with zero cross-module imports — so Phase 2 can audit claims and Phase 3
can motivate the metric suite from these numbers.

Output (all relative to the study root `mini-inequality/`):
- `inequality.py` (new)
- `reports/inequality_sad_code.csv`, `reports/inequality_sad_sam.csv`,
  `reports/inequality_samcode_skew.csv`, `reports/inequality_expansion.csv`,
  `reports/inequality_cascade.csv`, `reports/lorenz_sad_code_sentence.csv` (generated)
- `reports/INEQUALITY.md` (generated)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**ISOLATION (HARD) — read before executing any task:**
- This phase belongs to the isolated `mini-inequality/` sub-project. The study
  root is `/mnt/hostshare/ardoco-home/transarc-emp/mini-inequality`.
- Run every command with that as the working directory (every `<verify>` block
  starts with `cd /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality`).
- NEVER read, write, or commit anything under the repo-root `.planning/`
  (that tracks the unrelated active **v1.2** milestone). Commit only
  `mini-inequality/**` on branch `gsd/mini-data-inequality`.
- `inequality.py` lives at the study root: path is `inequality.py`, NOT
  `mini-inequality/inequality.py` (you are already inside `mini-inequality/`).
  Do not create a nested `mini-inequality/mini-inequality/` path.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-inequality-engine-ineq-01-ineq-02-ineq-03-out-01/01-CONTEXT.md

CONSTRAINTS:
- Stdlib only: `csv, json, math, os, sys, argparse` + `from collections import
  defaultdict, Counter` + `from pathlib import Path`. No third-party deps, no
  requirements.txt.
- ZERO imports from `src/`, `mini-src/`, or any project module. Definitions are
  COPIED verbatim from the reference files and sanity-checked for agreement. The
  reference files are READ-ONLY sources (one level up from the study root):
  `../mini-src/metrics.py`, `../src/bias/component_suite.py`, `../writing/eval.tex`.
- No benchmark-derived word lists (workspace leakage rule) — distributional
  stats only.
- Root resolution mirrors mini-src/metrics.py: `_ARDOCO_HOME =
  Path(__file__).resolve().parents[2]` (study-root file -> ardoco-home).
  `BENCHMARK = $TRANSARC_BENCHMARK or _ARDOCO_HOME/"ardoco/core/tests-base/src/main/resources/benchmark"`.
  `RESULTS = $TRANSARC_RESULTS_DIR or _ARDOCO_HOME/"transarc-emp/results"`.
  `REPORTS = Path(__file__).resolve().parent / "reports"` (study-root reports/).

DEFINITIONS TO COPY (verbatim, then verify agreement — do NOT import):
From `../mini-src/metrics.py`:
  PROJECTS = ["mediastore","teastore","teammates","bigbluebutton","jabref"]
  GS_SAD_SAM, GS_SAM_CODE, GS_SAD_CODE, ACM_FILES   (the path dicts, lines 69-96)
  normalize_path(path)            (line 146-149: strips leading "Implementation/")
  enroll(gold, code_files)        (line 152-162: expands trailing-"/" dir entries)
  load_code_model_files(project)  (line 176-190: parse .acm JSON -> set of files)
  load_gs_sad_sam(project)        (line 193-196: set[(modelElementID, sentence)])
  load_gs_sad_code_raw(project)   (line 199-203: set[(sentenceID, normalized codeID)])
  _cell(row, keys)                (line 167-173: first non-empty col value)
  the sad-sam result column keys  (_SADSAM_COMPONENT_KEYS, _SADSAM_SENTENCE_KEYS, lines 106-107)
From `../src/bias/component_suite.py`:
  _gini(values)                   (line 156-162: sorted cumulative Gini)
  the `_code_inputs` mapped-only universe rule (line 208-232): collapse
    (sentence, file) -> (sentence, component) via the enrolled SAM-CODE map and
    DROP files with no SAM-CODE component (apply the same drop to all sad-code
    component-level work so it agrees with gold_gini).

NEW SELF-CONTAINED LOADERS (write these in inequality.py, mirroring metrics.py
`load_file_to_comps` lines 206-216):
  load_sam_code(project, code_files) ->
    returns (names: dict ae_id->ae_name, sam_enrolled: set[(ae_id, file)])
    by reading GS_SAM_CODE, `raw.add((ae_id, normalize_path(ce_ids or ce_id)))`,
    then `enroll(raw, code_files)`. Derive both:
      model_to_files = defaultdict(set); for (ae,fp): model_to_files[ae].add(fp)
      file_to_comps  = defaultdict(set); for (ae,fp): file_to_comps[fp].add(names.get(ae,ae))
  load_result_sad_sam(project) -> set[(component_id, sentence)] from
    `RESULTS/<project>/sad-sam/sadSamTlr_<project>.csv` using the sad-sam column
    keys (component first, sentence second). Return set() if the file is absent.

GENERIC INEQUALITY HELPERS (write in inequality.py):
  summary_stats(values) -> dict(n, total, min, median, max) over a list of ints.
  top_k_share(values, k) -> sum of the k largest / total (0.0 if total==0).
  palma(values) -> (sum of top 10% mass) / (sum of bottom 40% mass); if the
    bottom-40% mass is 0, return float('inf') as a sentinel and format as "inf".
  lorenz_points(values) -> list[(cum_pop_pct, cum_mass_pct)] over values sorted
    ascending, starting at (0.0, 0.0); cumulative population and mass as fractions
    of totals (so the curve ends at (1.0, 1.0)).

REFERENCE NUMBERS the engine MUST reproduce (frozen eval.tex literals — embed
these as expected constants for the CHECK; cite the source table in a comment):
  tab:sent_gini (per-sentence enrolled sad-code links): Gini = {mediastore:0.331,
    teastore:0.448, teammates:0.645, bigbluebutton:0.472, jabref:0.527};
    Top-3 % = {27.1, 35.2, 20.3, 21.3, 70.0}; Sents = {25,23,92,45,10}.
  tab:samcode_skew (SAM-CODE files per AE): Gini = {0.400,0.694,0.452,0.513,0.612};
    AEs = {19,19,14,22,6}; Enrolled = {60,164,1616,730,1956}; Top-3 Conc % =
    {43.3,68.9,52.4,38.4,98.6}.
  tab:enrollment: Raw = {57,70,228,132,38} (total 525); Enrolled =
    {59,707,8097,1529,8268} (total 18,660); Factor = {1.0,10.1,35.5,11.6,217.6};
    average factor 35.5.
  tab:amplification: sad-sam FPs = {1,0,28,5,2} (total 36); Induced file FPs =
    {1,0,2395,71,990} (total 3,457); aggregate 96.0x.
  NOTE on raw counts: load_gs_sad_code_raw returns a SET, so duplicate
  (sentence, path) rows collapse. If a raw count is off by a small amount vs the
  table (e.g. Teammates 228 vs a set-deduped 226), the CHECK should compare the
  ENROLLED totals and factors (which are the load-bearing numbers) exactly, and
  treat raw as informational. Document any raw discrepancy in INEQUALITY.md.

CASCADE construction (reproduce tab:amplification): for each project,
  gold = load_gs_sad_sam(project); res = load_result_sad_sam(project);
  fps = res - gold   (sad-sam false-positive (component_id, sentence) pairs);
  model_to_files from load_sam_code(project, code_files);
  induced = sum(len(model_to_files.get(m, ())) for (m, s) in fps);
  max_single = max((len(model_to_files.get(m, ())) for (m,s) in fps), default=0).
  Aggregate: total_fps = sum of per-project fps counts (expect 36);
  total_induced = sum of per-project induced (expect 3,457); 96.0x = total_induced/total_fps.
  If total_induced != 3457 with this Sum|files(m)| reading, the executor must
  try the deduplicated variant `len({(s, f) for (m, s) in fps for f in model_to_files.get(m, ())})`
  and keep whichever reproduces 3,457 (record which in INEQUALITY.md). The CHECK
  enforces the exact totals so a wrong reading fails loud.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffolding — copied primitives, loaders, generic inequality helpers, CLI skeleton</name>
  <read_first>
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-src/metrics.py (lines 53-96 roots+path dicts, 146-216 normalize_path/enroll/_cell/loaders/load_file_to_comps, 244-251 result_path, 358-396 CLI/main idiom)
    - /mnt/hostshare/ardoco-home/transarc-emp/src/bias/component_suite.py (lines 156-162 `_gini`, 208-232 `_code_inputs` mapped-only universe, 274-296 table/csv writers)
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality/.planning/phases/01-inequality-engine-ineq-01-ineq-02-ineq-03-out-01/01-CONTEXT.md (locked decisions)
  </read_first>
  <files>inequality.py</files>
  <action>
Create `inequality.py` at the study root. Build the shared scaffolding; leave the
per-project metric computation as stubs filled in Tasks 2-4 so the file imports
and runs end-to-end.

1. Module docstring stating: self-contained stdlib-only gold-inequality engine,
   mirrors mini-src/metrics.py, copies definitions (no imports), sanity-checked
   vs writing/eval.tex Ch1 tables. List the env overrides.

2. Stdlib imports only: `csv, json, math, os, sys, argparse`,
   `from collections import defaultdict, Counter`, `from pathlib import Path`.

3. Root constants (mirror metrics.py lines 57-65): `_ARDOCO_HOME =
   Path(__file__).resolve().parents[2]`; `BENCHMARK`, `RESULTS` with env
   overrides; `REPORTS = Path(__file__).resolve().parent / "reports"`. Create
   REPORTS dir at write time (`REPORTS.mkdir(parents=True, exist_ok=True)`).

4. COPY verbatim from metrics.py: `PROJECTS`, `GS_SAD_SAM`, `GS_SAM_CODE`,
   `GS_SAD_CODE`, `ACM_FILES`, `normalize_path`, `enroll`, `_cell`,
   `load_code_model_files`, `load_gs_sad_sam`, `load_gs_sad_code_raw`, and the
   sad-sam result column-key tuples. COPY `_gini` verbatim from component_suite.py.

5. Write the new self-contained loaders described in <context>:
   `load_sam_code(project, code_files)` (returns names, sam_enrolled; derive
   model_to_files + file_to_comps from it), and `load_result_sad_sam(project)`.

6. Write the generic helpers from <context>: `summary_stats`, `top_k_share`,
   `palma`, `lorenz_points`. Add `_fmt(v)` -> 3-decimal string for floats, str
   passthrough otherwise.

7. Stub the per-project computation functions returning empty/placeholder dicts:
   `compute_sad_code_dist(project)`, `compute_sad_sam_dist(project)`,
   `compute_samcode_skew(project)`, `compute_expansion(project)`,
   `compute_cascade(project)`. Each returns `{"project": project}` for now.

8. Write generic output helpers: `write_csv(path, header, rows)` (stdlib csv,
   header + per-project rows + an AVG/Total row where meaningful — average the
   float columns, sum the count columns; document which columns are summed vs
   averaged in a comment).

9. `main()`: argparse with `--task` (choices `sad-code, sad-sam, both`; default
   `both`), `--project` (validated against PROJECTS, else `sys.exit(2)`),
   `--check-only` (run only the sanity gate, see Task 4), `--no-check` (skip the
   gate). Default run: compute all distributions for all projects, write all
   CSVs + INEQUALITY.md, then run the CHECK (Task 4) unless `--no-check`. For now
   wire the control flow with the stubs so the script runs and exits 0.
   Guard with `if __name__ == "__main__": main()`.
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality && python3 inequality.py --task both --no-check && python3 -c "
import ast,sys
t=ast.parse(open('inequality.py').read())
mods=set()
for n in ast.walk(t):
    if isinstance(n,ast.Import):
        for a in n.names: mods.add(a.name.split('.')[0])
    if isinstance(n,ast.ImportFrom) and n.module: mods.add(n.module.split('.')[0])
allowed={'csv','json','math','os','sys','argparse','collections','pathlib'}
bad=mods-allowed
assert not bad, ('non-stdlib imports: %s'%bad)
print('IMPORTS_OK', sorted(mods))
" && grep -q 'def _gini' inequality.py && grep -q 'def enroll' inequality.py && grep -q 'argparse' inequality.py && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python3 inequality.py --task both --no-check` exits 0 (run from the study root).
    - The AST import scan finds ONLY modules in {csv,json,math,os,sys,argparse,collections,pathlib} — zero src/ or mini-src/ imports.
    - `inequality.py` defines `_gini`, `enroll`, `normalize_path`, `load_code_model_files`, `load_gs_sad_sam`, `load_gs_sad_code_raw`, `load_sam_code`, `load_result_sad_sam`, `summary_stats`, `top_k_share`, `palma`, `lorenz_points` (grep each).
    - `python3 inequality.py --project bogus` exits non-zero (2).
  </acceptance_criteria>
  <done>The engine imports and runs end-to-end with stdlib-only imports, all copied primitives + new loaders + generic helpers present, CLI flags wired, placeholder computations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: INEQ-01 + INEQ-02 gold distributions (per-sentence, per-component, per-file, samcode-skew) + Lorenz</name>
  <read_first>
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality/inequality.py (extend the stub computation fns)
    - /mnt/hostshare/ardoco-home/transarc-emp/src/bias/component_suite.py (lines 208-232 `_code_inputs` mapped-only collapse; 165-192 component_suite gold_by_c idiom)
    - /mnt/hostshare/ardoco-home/transarc-emp/writing/eval.tex (lines 241-260 tab:sent_gini; 191-210 tab:samcode_skew)
  </read_first>
  <files>inequality.py</files>
  <action>
Implement the gold-distribution computations.

A. `compute_sad_code_dist(project)`:
   - `code_files = load_code_model_files(project)`;
     `enrolled = enroll(load_gs_sad_code_raw(project), code_files)` -> set[(sentence,file)].
   - PER-SENTENCE (INEQ-02, the tab:sent_gini headline): `per_sent =
     Counter(s for (s,f) in enrolled)`; values = list(per_sent.values());
     emit n_sents=len(values), min/median/max (summary_stats), gini=_gini(values),
     top3_pct = 100*top_k_share(values,3), palma=palma(values).
   - PER-FILE (INEQ-02): `per_file = Counter(f for (s,f) in enrolled)`; same stat
     bundle (gini/top-k/min-median-max). (Supplementary, no frozen target.)
   - PER-COMPONENT (INEQ-01, component_suite.gold_gini universe): build
     file_to_comps via load_sam_code; collapse enrolled to (sentence, component)
     DROPPING files with no component (mirror `_code_inputs`); then for the
     canonical unit count #distinct sentences per component -> gini_sentences.
     ALSO compute supplementary units over the same collapsed set: #links (pairs)
     per component, and #files per component (from file_to_comps inverse).
     Emit gini + top-1/top-3 + min/median/max for the #sentences unit; include
     the supplementary gini values as extra columns.
   - Return one flat dict keyed for the CSV.

B. `compute_sad_sam_dist(project)`:
   - `gold = load_gs_sad_sam(project)` -> set[(modelElementID, sentence)].
   - Build names from load_sam_code (ae_id->ae_name) for name-collapse where
     available, else fall back to the id (mirror component_suite `names.get(c,c)`).
   - #distinct sentences per component (by collapsed name): gini, top-1/top-3,
     min/median/max. (INEQ-01; by-construction agreement with gold_gini.)

C. `compute_samcode_skew(project)` (INEQ-01, reproduces tab:samcode_skew):
   - `code_files = load_code_model_files(project)`;
     `names, sam_enrolled = load_sam_code(project, code_files)`;
     `model_to_files` from sam_enrolled. values = [len(fs) for fs in
     model_to_files.values()]; emit AEs=len(values), enrolled=sum(values),
     min/max, gini=_gini(values), top3_conc_pct=100*top_k_share(values,3).

D. Wire `main()` to write:
   - `reports/inequality_sad_code.csv` (per-project + AVG; per-sentence +
     per-file + per-component columns),
   - `reports/inequality_sad_sam.csv`,
   - `reports/inequality_samcode_skew.csv`,
   - `reports/lorenz_sad_code_sentence.csv` = lorenz_points of the per-sentence
     enrolled values, ALL projects, columns `project,cum_pop_pct,cum_mass_pct`.

Round CSV floats to 3 decimals for Gini/shares; keep integer counts as ints.
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality && python3 inequality.py --task both --no-check && python3 -c "
import csv
sc={r['project']:r for r in csv.DictReader(open('reports/inequality_sad_code.csv'))}
sk={r['project']:r for r in csv.DictReader(open('reports/inequality_samcode_skew.csv'))}
exp_sent={'mediastore':0.331,'teastore':0.448,'teammates':0.645,'bigbluebutton':0.472,'jabref':0.527}
exp_skew={'mediastore':0.400,'teastore':0.694,'teammates':0.452,'bigbluebutton':0.513,'jabref':0.612}
def gcol(row):
    # find the per-sentence gini column by name
    for k in row:
        if 'sent' in k and 'gini' in k.lower(): return float(row[k])
    raise SystemExit('no per-sentence gini column: %s'%list(row))
for p,g in exp_sent.items():
    got=gcol(sc[p]); assert abs(got-g)<=0.005, (p,got,g)
for p,g in exp_skew.items():
    row=sk[p]; col=[k for k in row if 'gini' in k.lower()][0]
    assert abs(float(row[col])-g)<=0.005, (p,row[col],g)
import os; assert os.path.exists('reports/lorenz_sad_code_sentence.csv')
print('DIST_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - reports/inequality_sad_code.csv per-sentence Gini reproduces 0.331/0.448/0.645/0.472/0.527 within 0.005.
    - reports/inequality_samcode_skew.csv Gini reproduces 0.400/0.694/0.452/0.513/0.612 within 0.005; AE counts 19/19/14/22/6; enrolled 60/164/1616/730/1956.
    - reports/inequality_sad_code.csv contains per-sentence, per-file, AND per-component columns; reports/inequality_sad_sam.csv contains a per-component Gini column; both have an AVG row.
    - reports/lorenz_sad_code_sentence.csv exists with columns project,cum_pop_pct,cum_mass_pct and final mass point ~1.0 per project.
  </acceptance_criteria>
  <done>The gold-distribution metrics reproduce tab:sent_gini and tab:samcode_skew within tolerance; per-component (sad-code & sad-sam) and per-file distributions plus the Lorenz curve are emitted.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: INEQ-03 enrollment expansion (tab:enrollment) + component->file cascade (tab:amplification)</name>
  <read_first>
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality/inequality.py (extend compute_expansion / compute_cascade stubs)
    - /mnt/hostshare/ardoco-home/transarc-emp/writing/eval.tex (lines 50-74 tab:enrollment; 149-179 tab:amplification + the Sum|files(m)| description)
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-src/metrics.py (lines 152-162 enroll, 199-216 raw sad-code + sam-code loaders)
  </read_first>
  <files>inequality.py</files>
  <action>
A. `compute_expansion(project)` (reproduce tab:enrollment, sad-code ONLY per
   Area-4 Q4 override — no sad-sam row):
   - `raw = load_gs_sad_code_raw(project)` (set of (sentence, normalized path));
     `raw_n = len(raw)`.
   - dir_entries = number of raw entries whose path ends with "/"; also compute
     dir_pct = 100*dir_entries/raw_n.
   - `code_files = load_code_model_files(project)`;
     `enrolled = enroll(raw, code_files)`; `enrolled_n = len(enrolled)`.
   - factor = enrolled_n / raw_n.
   Emit columns: project, raw, dir_entries, dir_pct, enrolled, factor.
   Aggregate row "Total": raw=sum, enrolled=sum, factor = total_enrolled/total_raw
   (the 35.5x average). Write `reports/inequality_expansion.csv`.

B. `compute_cascade(project)` (reproduce tab:amplification):
   - `gold = load_gs_sad_sam(project)`; `res = load_result_sad_sam(project)`.
   - `fps = res - gold` (sad-sam FP (component_id, sentence) pairs); `n_fps = len(fps)`.
   - `code_files = load_code_model_files(project)`;
     `_, sam_enrolled = load_sam_code(project, code_files)`;
     `model_to_files = defaultdict(set)`; for (ae,fp) in sam_enrolled: add.
   - PRIMARY reading: `induced = sum(len(model_to_files.get(m, set())) for (m,s) in fps)`;
     `max_single = max((len(model_to_files.get(m,set())) for (m,s) in fps), default=0)`.
   - mean_amplif = induced/n_fps if n_fps else None.
   Emit columns: project, sadsam_fps, induced_file_fps, mean_amplif, max_single.
   Aggregate "Total": fps=sum (expect 36), induced=sum (expect 3,457),
   aggregate amplif = total_induced/total_fps (expect 96.0x).
   FALLBACK: if the Total induced != 3457, recompute induced as the DEDUPED
   distinct-(sentence,file) count `len({(s,f) for (m,s) in fps for f in
   model_to_files.get(m,set())})` per project, and use whichever reading makes the
   Total equal 3,457. Record which reading was used in a top-of-file comment and
   in INEQUALITY.md. Write `reports/inequality_cascade.csv`.
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality && python3 inequality.py --task both --no-check && python3 -c "
import csv
ex={r['project']:r for r in csv.DictReader(open('reports/inequality_expansion.csv'))}
ca={r['project']:r for r in csv.DictReader(open('reports/inequality_cascade.csv'))}
def tot(d):
    for k in ('Total','AVG','Average'):
        if k in d: return d[k]
    raise SystemExit('no total row: %s'%list(d))
ec=[c for c in next(iter(ex.values())) if 'enroll' in c.lower()][0]
assert int(float(tot(ex)[ec]))==18660, tot(ex)[ec]
exp_enr={'mediastore':59,'teastore':707,'teammates':8097,'bigbluebutton':1529,'jabref':8268}
for p,v in exp_enr.items(): assert int(float(ex[p][ec]))==v, (p,ex[p][ec],v)
fc=[c for c in next(iter(ca.values())) if 'fps' in c.lower() and 'induced' not in c.lower()][0]
ic=[c for c in next(iter(ca.values())) if 'induced' in c.lower()][0]
assert int(float(tot(ca)[fc]))==36, tot(ca)[fc]
assert int(float(tot(ca)[ic]))==3457, tot(ca)[ic]
exp_ind={'mediastore':1,'teastore':0,'teammates':2395,'bigbluebutton':71,'jabref':990}
for p,v in exp_ind.items(): assert int(float(ca[p][ic]))==v, (p,ca[p][ic],v)
print('CASCADE_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - reports/inequality_expansion.csv: per-project enrolled = 59/707/8097/1529/8268, Total enrolled = 18,660, Total factor ≈ 35.5; sad-code only (no sad-sam row).
    - reports/inequality_cascade.csv: per-project sad-sam FPs = 1/0/28/5/2 (Total 36); induced file FPs = 1/0/2395/71/990 (Total 3,457); aggregate amplif ≈ 96.0.
    - The reading used for `induced` (Sum|files(m)| vs deduped) is recorded in a comment in inequality.py.
  </acceptance_criteria>
  <done>Enrollment expansion reproduces tab:enrollment exactly (18,660 / 35.5x / 217.6x) and the cascade reproduces tab:amplification exactly (36 -> 3,457 / 96.0x) from the bundled TransArc sad-sam results.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: INEQUALITY.md report (tables-first + headline prose) + the sanity CHECK gate (fail loud) + --check-only</name>
  <read_first>
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality/inequality.py (add report + check)
    - /mnt/hostshare/ardoco-home/transarc-emp/writing/eval.tex (lines 234-260 the prose headlines to mirror; 105-108, 175-179 the cascade/CI prose)
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-src/check.py (the workspace sanity-check pattern)
  </read_first>
  <files>inequality.py</files>
  <action>
A. `write_report()` -> `reports/INEQUALITY.md`: tables-first markdown, one
   section per metric family, each prefixed by a single headline line stating the
   claim (mirror eval.tex prose), e.g.:
   - "## Per-sentence gold concentration — Gini 0.331 (MediaStore) → 0.645 (Teammates)"
   - "## Enrollment expansion — 525 raw decisions → 18,660 enrolled file links (35.5× avg, up to 217.6× on JabRef)"
   - "## Component→file cascade — 36 sad-sam FPs cascade to 3,457 file-level FPs (96.0×)"
   - "## SAM-CODE file skew — files-per-component Gini 0.400 → 0.694"
   Each section embeds a small markdown table built from the corresponding CSV
   rows (per-project + aggregate). Add a short "Sanity check" section summarizing
   PASS/FAIL and the tolerance used. Note any raw-count discrepancy and which
   cascade reading was used.

B. `EXPECTED` dict — embed the frozen eval.tex literals from <context> (tab:sent_gini
   Gini + Top-3 %, tab:samcode_skew Gini + AEs + enrolled, tab:enrollment enrolled
   totals + factors, tab:amplification FP/induced totals + per-project induced),
   each annotated with its source table label in a comment.

C. `run_check()` -> bool: recompute each engine value and compare to EXPECTED:
   - Gini comparisons: abs diff ≤ 0.005.
   - Integer counts (enrolled per-project + total 18,660; cascade fps total 36;
     induced per-project + total 3,457; AE counts; samcode enrolled counts):
     EXACT equality.
   - Build a diff table (metric, project, expected, computed, delta, ok). Print
     it. Return False if any row fails.

D. Wire `main()`:
   - default run: after writing all CSVs + report, call `run_check()`; if it
     returns False, print "SANITY CHECK FAILED" and `sys.exit(1)`.
   - `--check-only`: run ONLY `run_check()` (recompute in-memory, compare; do not
     require re-writing CSVs) and exit 0/1 accordingly.
   - `--no-check`: skip the gate (used by earlier tasks' verifies).
   On success print "SANITY CHECK PASSED (tol: Gini≤0.005, counts exact)".
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality && python3 inequality.py && echo "RC=$?" && test -f reports/INEQUALITY.md && grep -q '0.331' reports/INEQUALITY.md && grep -q '18,660\|18660' reports/INEQUALITY.md && grep -q '3,457\|3457' reports/INEQUALITY.md && python3 inequality.py --check-only && echo CHECK_ONLY_OK && python3 -c "
import subprocess,sys
# fail-loud test: corrupt an expected constant by monkey-patching is overkill; instead assert exit 0 on clean run already shown. Verify --check-only exits 0.
print('REPORT_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 inequality.py` (no flags) exits 0 AND prints "SANITY CHECK PASSED"; reports/INEQUALITY.md exists and contains the headline numbers 0.331, 18,660 (or 18660), 3,457 (or 3457).
    - `python3 inequality.py --check-only` exits 0 and prints the diff/PASS table.
    - The CHECK enforces Gini tolerance ≤0.005 and EXACT integer counts (verify by reading run_check: it compares enrolled total to 18660 and induced total to 3457 with `==`).
    - INEQUALITY.md is tables-first with a one-line headline per section and records the tolerance + cascade reading.
  </acceptance_criteria>
  <done>The engine writes a paper-facing tables-first report with headline prose and self-validates every reproduced number against the frozen eval.tex literals, failing loud (exit 1) on mismatch; --check-only runs just the gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| filesystem → script | Reads project-owned benchmark gold CSVs, `.acm` code models, and bundled TransArc sad-sam result CSVs. All local, project-owned, offline. |
| CLI args → script | `--task` (argparse `choices`), `--project` (validated against PROJECTS), boolean flags. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-01-01 | Tampering/DoS | missing/malformed result or gold CSV | mitigate | Loaders return set() on absent files; cascade for a project with no result yields 0 FPs (matches TeaStore). Engine does not crash. |
| T-01-02 | Spoofing/Integrity | reproduced numbers silently drift from the paper | mitigate | The sanity CHECK fails loud (exit 1) on any Gini drift >0.005 or any integer-count mismatch — the core integrity control. |
| T-01-03 | Elevation/Injection | `--project` used in dict lookup | mitigate | Validated against PROJECTS allow-list; exit 2 otherwise. `--task` constrained by choices. |
| T-01-04 | Information Disclosure | output paths | accept | Hardcoded study-relative `reports/`; offline, no network, no untrusted path component. |

Surface is minimal: offline stdlib script, local read-only inputs, writes only under the study `reports/`. No high-severity threats.
</threat_model>

<verification>
- `python3 inequality.py` (from the study root) exits 0, writes all 6 CSVs +
  reports/INEQUALITY.md, and prints "SANITY CHECK PASSED".
- Per-sentence enrolled sad-code Gini = tab:sent_gini (0.331/0.448/0.645/0.472/0.527) ±0.005; Top-3 % matches.
- SAM-CODE files-per-AE Gini = tab:samcode_skew (0.400/0.694/0.452/0.513/0.612) ±0.005; AEs + enrolled exact.
- Enrollment expansion = tab:enrollment exactly (per-project enrolled 59/707/8097/1529/8268; total 18,660; 35.5× avg; JabRef 217.6×).
- Cascade = tab:amplification exactly (FPs 36; induced 3,457; 96.0×; per-project induced 1/0/2395/71/990).
- Per-component (#sentences) Gini computed for sad-code (mapped-only universe) and sad-sam via the copied `_gini`.
- `--check-only` runs just the gate; mismatch -> exit 1 (fail loud).
- AST scan: stdlib-only imports; zero src/ or mini-src/ imports.
</verification>

<success_criteria>
- INEQ-01: per-component gold inequality (Gini/Lorenz/top-k/min-median-max) for sad-code & sad-sam, 5 projects, CSV. ✓ (Tasks 2; Lorenz Task 2)
- INEQ-02: per-file concentration + per-sentence links/sentence distribution (sad-code), incl. the 0.331→0.645 Gini. ✓ (Task 2)
- INEQ-03: per-project enrollment expansion factor + aggregate component-FP → file-FP cascade. ✓ (Task 3)
- OUT-01: self-contained stdlib-only engine + reports (MD/CSV), no cross-module imports, sanity-checked vs canonical numbers within stated tolerance. ✓ (Tasks 1+4)
</success_criteria>

<output>
After completion, create
`.planning/phases/01-inequality-engine-ineq-01-ineq-02-ineq-03-out-01/01-01-SUMMARY.md`
</output>
