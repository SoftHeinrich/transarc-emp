---
phase: 03-motivation-paper-hooks-motiv-01-out-02
plan: 03-01
type: execute
wave: 1
depends_on: []
files_modified:
  - motivation.py
  - reports/MOTIVATION.md
  - reports/baselines.csv
  - reports/out02_concentration.csv
  - reports/out02_concentration.tex
  - reports/out02_lorenz.tex
autonomous: true
requirements: [MOTIV-01, OUT-02]

must_haves:
  truths:
    - "Running `python3 motivation.py` (from the study root) exits 0, scores Top-3 + random baselines on sad-code (file) and sad-sam (link) for all 5 projects, writes reports/MOTIVATION.md + reports/baselines.csv, and emits the OUT-02 table/figure source (MOTIV-01, OUT-02)."
    - "Top-3 baseline micro-F1 > random baseline micro-F1 on both tasks (the exploitation claim); the script self-checks this and exits non-zero otherwise (MOTIV-01)."
    - "For each baseline the report shows micro-F1 alongside the 4-metric suite (per-component macro F1, sentence coverage, noise rate, file F1) — demonstrating the suite discriminates the content-blind Top-3 where micro-F1 does not (MOTIV-01)."
    - "MOTIVATION.md contains an explicit driver->metric map (enrollment->file-F1 caveat; concentration->per-component F1; long-tail->coverage; narrative->noise) and reports the resolved trivial-baseline file-F1 value, with strongest-pipeline/approach placeholders left deferred (MOTIV-01)."
    - "OUT-02: a paper-ready Gini/top-k concentration table is emitted as booktabs .tex + CSV (columns matching tab:sent_gini/tab:samcode_skew), plus a pgfplots \\addplot Lorenz snippet over reports/lorenz_sad_code_sentence.csv (OUT-02)."
    - "motivation.py reuses `import inequality` + copies metric/baseline defs from mini-src/metrics.py and rq2_doc_to_model_prestudy.py; imports nothing else outside stdlib; randomness is seeded (deterministic)."
  artifacts:
    - path: "motivation.py"
      provides: "stdlib-only baseline-exploitation + paper-hooks script: Top-3/random baselines scored with micro-F1 + 4-metric suite, MOTIVATION.md, OUT-02 table/figure, fail-loud self-check"
      min_lines: 180
      contains: "random.Random(0)"
    - path: "reports/MOTIVATION.md"
      provides: "baseline results + driver->metric map + resolved trivial-baseline file-F1"
    - path: "reports/baselines.csv"
      provides: "per-project Top-3/random micro-F1 + suite metrics, both tasks"
    - path: "reports/out02_concentration.tex"
      provides: "booktabs Gini/top-k concentration table (paper-ready)"
    - path: "reports/out02_concentration.csv"
      provides: "the same concentration table as CSV"
    - path: "reports/out02_lorenz.tex"
      provides: "pgfplots \\addplot snippet over lorenz_sad_code_sentence.csv"
  key_links:
    - from: "motivation.py"
      to: "inequality.py loaders (load_gs_sad_sam, load_gs_sad_code_raw, enroll, load_code_model_files, load_sam_code) + compute_* for OUT-02"
      via: "import inequality (self-contained reuse)"
      pattern: "import inequality"
    - from: "motivation.py"
      to: "mini-src/metrics.py prf/coverage/noise + rq2_doc_to_model_prestudy baselines"
      via: "COPY verbatim (no import)"
      pattern: "def baseline_top3"
---

<objective>
Create `motivation.py` at the study root: a stdlib-only script that scores trivial
Top-3 + random baselines on the GOLD benchmark for both tasks, shows file-/link-
level micro-F1 is inflated by the inequality while the 4-metric suite is not,
writes `reports/MOTIVATION.md` (+ `reports/baselines.csv`), and emits the OUT-02
paper-ready Gini/Lorenz table/figure source. Reuses `import inequality`; copies
metric/baseline defs; reads no system results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**ISOLATION (HARD):** study root = `/mnt/hostshare/ardoco-home/transarc-emp/mini-inequality`.
Run every command from there. Never touch repo-root `.planning/`. `motivation.py`
at the study root (not nested).
</execution_context>

<context>
@.planning/phases/03-motivation-paper-hooks-motiv-01-out-02/03-CONTEXT.md

CONSTRAINTS:
- Stdlib only; `import inequality` allowed (own engine). COPY (no import):
  `prf`/`sentence_coverage`/`noise_rate` from `../mini-src/metrics.py`;
  `baseline_top3_by_gold_links`, `baseline_random`, `per_component_macro_f1`,
  `measure` from `../src/bias/rq2_doc_to_model_prestudy.py`.
- `random` is stdlib; seed via `random.Random(0)` over SORTED lists (reproducible).
- NO system/result files; gold only.
- sad-sam orientation: `inequality.load_gs_sad_sam` returns (modelElementID,
  sentence) — FLIP to (sentence, component) for the baseline/metric helpers.

BASELINE / METRIC DEFINITIONS (mirror rq2_doc_to_model_prestudy.py:181-328):
  baseline_random(gold_sents, all_targets, target_size, rng): sample distinct
    (s, t) from sorted(gold_sents) x sorted(all_targets) until target_size or 10x cap.
  baseline_top3_by_gold_links(gold_sents, gold, k=3): top-k targets by gold-link
    count; predict {(s, t) for s in gold_sents for t in top_k}.
  per_component_macro_f1(gold, result): macro F1 over targets (binary per target).
  prf(gold, res) (from metrics.py): micro (p, r, f1); use [2] as micro-F1.
  sentence_coverage(gold_by_s, res_by_s) / noise_rate(...) (metrics.py) OR the
    pair-set forms in prestudy:284-316 — pick one form and keep it consistent.

PER-TASK GOLD (reuse inequality):
  sad-sam: gold = {(s, c) for (c, s) in ineq.load_gs_sad_sam(p)}; targets = all
    component ids = {c for (c, s) in raw}. target_size = len(gold).
  sad-code: code=ineq.load_code_model_files(p); gold = ineq.enroll(
    ineq.load_gs_sad_code_raw(p), code) as (sentence, file); targets = code files;
    target_size = len(gold). For per-component macro on sad-code, collapse files
    -> components via ineq.load_sam_code's file_to_comps (mapped-only).

REFERENCE: doc-to-model Top-3 micro-F1 ~0.38 vs random ~0.12 (3.1x)
  (reports/RQ2_DOC_TO_MODEL_PRESTUDY.md). Top-3 must beat random on both tasks.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: motivation.py — Top-3/random baselines + 4-metric suite + MOTIVATION.md + self-check (MOTIV-01)</name>
  <read_first>
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality/inequality.py (loaders to reuse)
    - /mnt/hostshare/ardoco-home/transarc-emp/src/bias/rq2_doc_to_model_prestudy.py (lines 181-349 baselines/metrics/measure/run_project)
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-src/metrics.py (lines 114-143 prf/sentence_coverage/noise_rate)
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality/.planning/phases/03-motivation-paper-hooks-motiv-01-out-02/03-CONTEXT.md
  </read_first>
  <files>motivation.py</files>
  <action>
1. `import inequality as ineq`, `import random, sys`, `from collections import defaultdict, Counter`.
2. COPY the metric helpers: `prf(gold,res)` (metrics.py micro p/r/f1),
   `per_component_macro_f1(gold,result)`, `sentence_coverage(gold,result)`,
   `noise_rate(gold,result)` (pair-set forms from prestudy:284-316), and the two
   baselines `baseline_random`, `baseline_top3_by_gold_links`.
3. `gold_and_targets(project, task)` -> (gold:set[(s,t)], targets:list, file_to_comps_or_None):
   - sad-sam: raw=ineq.load_gs_sad_sam(p); gold={(s,c) for (c,s) in raw};
     targets=sorted({c for (c,s) in raw}); fc=None.
   - sad-code: code=ineq.load_code_model_files(p);
     gold=ineq.enroll(ineq.load_gs_sad_code_raw(p),code) (already (s,file));
     targets=sorted(code); names,sam=ineq.load_sam_code(p,code);
     fc=defaultdict(set); for ae,fp in sam: fc[fp].add(names.get(ae,ae)).
4. `measure(name, result, gold, fc)` -> dict(name, micro_f1=prf(gold,result)[2],
   comp_f1, coverage, noise, file_f1). For sad-code, comp_f1 = macro F1 on the
   file->component collapsed sets (collapse both gold and result via fc, drop
   unmapped); file_f1 = micro_f1 (file level). For sad-sam, comp_f1 = macro F1 on
   the (s,component) sets directly; file_f1 = NA.
5. For each project x task: build gold/targets; gold_sents={s for (s,_) in gold};
   rng=random.Random(0); random_bl=baseline_random(gold_sents,targets,len(gold),rng);
   top3=baseline_top3_by_gold_links(gold_sents,gold,3); also a "gold/oracle" row
   (result=gold) for the ceiling. measure each.
6. Write `reports/baselines.csv` (columns: task, project, baseline, micro_f1,
   comp_f1, coverage, noise, file_f1) per-project + an AVG row per (task, baseline).
7. Write `reports/MOTIVATION.md`: per-task table of AVG micro-F1 vs suite for
   Top-3 / random / gold; a headline ("Top-3 reaches micro-F1 X vs random Y") ; an
   explicit driver->metric map (enrollment->file-F1 caveat; concentration->
   per-component F1; long-tail->sentence coverage; narrative->noise rate); and a
   "Resolved placeholder" line giving the sad-code Top-3 file-level micro-F1 as the
   trivial-baseline file-F1 (intro.tex:64), with strongest-pipeline/approach left
   "deferred (needs published system scores)".
8. Self-check: for BOTH tasks, assert AVG Top-3 micro_f1 > AVG random micro_f1;
   if not, print the offending task and sys.exit(1). Else print
   "MOTIVATION OK (Top-3 > random on both tasks)".
9. `if __name__=="__main__": main()` running steps 5-8 + Task-2's OUT-02 emission.
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality && python3 motivation.py && test -f reports/MOTIVATION.md && test -f reports/baselines.csv && python3 -c "
import csv
rows=list(csv.DictReader(open('reports/baselines.csv')))
def avg(task,bl):
    v=[float(r['micro_f1']) for r in rows if r['task']==task and r['baseline']==bl and r['project']!='AVG']
    return sum(v)/len(v)
for task in ('sad-code','sad-sam'):
    t3=avg(task,'top3'); rd=avg(task,'random')
    assert t3>rd, (task,t3,rd)
    print(task,'top3',round(t3,3),'> random',round(rd,3))
print('BASELINES_OK')
" && grep -qiE 'driver|noise rate|coverage|per-component' reports/MOTIVATION.md && echo MOTIV_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python3 motivation.py` exits 0; reports/MOTIVATION.md + reports/baselines.csv exist.
    - For both tasks, AVG Top-3 micro-F1 > AVG random micro-F1 (printed); self-check enforces it.
    - baselines.csv has micro_f1 + comp_f1 + coverage + noise (+ file_f1 for sad-code) per project + AVG, for top3/random/gold.
    - MOTIVATION.md has the driver->metric map and the resolved trivial-baseline file-F1 value; pipeline/approach placeholders marked deferred.
  </acceptance_criteria>
  <done>Baselines scored on both tasks; Top-3 beats random; suite vs micro-F1 contrast + driver->metric map written; trivial-baseline file-F1 resolved; deterministic.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: OUT-02 paper-ready Gini/top-k concentration table (.tex + .csv) + pgfplots Lorenz snippet</name>
  <read_first>
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality/inequality.py (compute_sad_code_dist / compute_samcode_skew for the table values)
    - /mnt/hostshare/ardoco-home/transarc-emp/writing/eval.tex (lines 241-256 tab:sent_gini, 191-210 tab:samcode_skew — column layout to match)
    - /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality/reports/lorenz_sad_code_sentence.csv (figure data)
  </read_first>
  <files>motivation.py</files>
  <action>
Add OUT-02 emission to motivation.py (called from main after the baselines):
1. `write_out02_concentration()`: build a per-project table reusing
   `ineq.compute_sad_code_dist` (sent_n, sent_min/median/max, sent_gini,
   sent_top3_pct) and `ineq.compute_samcode_skew` (gini, top3_conc_pct). Write
   `reports/out02_concentration.csv` (project + those columns + an AVG row) AND a
   booktabs LaTeX table `reports/out02_concentration.tex` with `\toprule/\midrule/
   \bottomrule`, a caption, and `\label{tab:gold_concentration}` — columns matching
   tab:sent_gini (Project, Sents, Min, Median, Max, Gini, Top-3 %).
2. `write_out02_lorenz()`: read `reports/lorenz_sad_code_sentence.csv` and write
   `reports/out02_lorenz.tex` — a pgfplots `\begin{tikzpicture}...\addplot table`
   snippet plotting cum_pop_pct vs cum_mass_pct (one `\addplot` per project or a
   reference to the CSV via `\addplot table [x=cum_pop_pct,y=cum_mass_pct]`), plus
   the diagonal equality line. Keep it a self-contained, compilable snippet
   (assume pgfplots is available in the paper; do NOT run pdflatex here).
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp/mini-inequality && python3 motivation.py && test -f reports/out02_concentration.tex && test -f reports/out02_concentration.csv && test -f reports/out02_lorenz.tex && grep -q 'tabular' reports/out02_concentration.tex && grep -q 'bottomrule' reports/out02_concentration.tex && grep -q '0.331' reports/out02_concentration.tex && grep -qE 'addplot|tikzpicture|pgfplots' reports/out02_lorenz.tex && python3 -c "
import csv
r={x['project']:x for x in csv.DictReader(open('reports/out02_concentration.csv'))}
assert abs(float(r['mediastore']['sent_gini'])-0.331)<=0.005
assert abs(float(r['teammates']['sent_gini'])-0.645)<=0.005
print('OUT02_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - reports/out02_concentration.tex is a booktabs table (toprule/midrule/bottomrule, caption, label) reproducing the gold Gini/top-k columns (contains 0.331); reports/out02_concentration.csv matches.
    - reports/out02_lorenz.tex is a pgfplots snippet (\addplot / tikzpicture) over the Phase-1 Lorenz CSV.
    - Values reproduce the engine numbers (mediastore 0.331, teammates 0.645).
  </acceptance_criteria>
  <done>OUT-02 paper-ready concentration table (.tex + .csv) + Lorenz pgfplots snippet emitted from the Phase-1 numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| filesystem → script | Reuses the engine + reads project-owned gold; no untrusted/network input. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-01 | Integrity | non-reproducible baseline (random) | mitigate | Seeded RNG over sorted lists → deterministic; seed printed. |
| T-03-02 | Integrity | claim "baseline exploits inequality" unsupported | mitigate | Fail-loud self-check: Top-3 micro-F1 must exceed random on both tasks. |

Offline stdlib script; gold only; minimal surface.
</threat_model>

<verification>
- `python3 motivation.py` exits 0; writes MOTIVATION.md, baselines.csv, out02_concentration.{tex,csv}, out02_lorenz.tex.
- Top-3 micro-F1 > random on both tasks (self-checked).
- Suite metrics reported beside micro-F1; driver->metric map present; trivial-baseline file-F1 resolved; pipeline/approach deferred.
- OUT-02 table reproduces gold Gini (0.331/0.645); Lorenz pgfplots snippet present.
- Reuses `import inequality`; copied defs; deterministic; no system results.
</verification>

<success_criteria>
- MOTIV-01: Top-3 + random baselines scored; micro-F1 inflation shown; suite needed; per project + average. ✓ (Task 1)
- OUT-02: paper-ready Gini/Lorenz table (TeX + CSV) + Lorenz figure data. ✓ (Task 2)
</success_criteria>

<output>
After completion, create
`.planning/phases/03-motivation-paper-hooks-motiv-01-out-02/03-01-SUMMARY.md`
</output>
