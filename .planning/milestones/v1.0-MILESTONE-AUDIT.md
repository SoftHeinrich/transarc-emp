---
milestone: v1.0
milestone_name: TransArc-EMP two-pillar refactor
status: passed
date: 2026-05-30
auditor: gsd-autonomous (orchestrator, inline)
---

# Milestone v1.0 Audit — TransArc-EMP Two-Pillar Refactor

## Definition of Done (from PROJECT.md / REQUIREMENTS.md)

Narrow the sprawled research workspace to two pillars (TransArc empirical study +
Benchmark bias analysis), keep both reproducible, and align `writing/eval.tex` to
match (Ch1=TransArc, Ch2=Bias). 16 v1 requirements across 3 phases.

## Phase Verification Roll-up

| Phase | Goal | VERIFICATION | 
|-------|------|--------------|
| 1. Reorganize | Two-pillar src/ + archive | **passed** (5/5 success criteria) |
| 2. Verify | Both pillars run reproducibly | **passed** (13/13 scripts exit 0, lib importable, reports regenerate identically) |
| 3. Paper | eval.tex Ch1=TransArc, Ch2=Bias | **passed** (2 chapters, data-driven tables, structural validation) |

## Requirements Coverage (16/16)

| Group | IDs | Status |
|-------|-----|--------|
| Scope | SCOPE-01..05 | ✓ Phase 1 |
| Archive | ARCH-01..04 | ✓ Phase 1 |
| Reproducibility | REPRO-01..03 | ✓ Phase 2 |
| Paper | PAPER-01..04 | ✓ Phase 3 (SC4/PAPER-04 reframed — see below) |

All 16 v1 requirements covered and verified. No unmapped requirements.

## Cross-Phase Integration

Coherent forward chain, no upstream backflow:
- **P1 → P2**: the moved pillar scripts (`src/transarc/`, `src/bias/`, shared
  `src/lib/`) all import-resolve and run end-to-end after the reorg — the depth-2
  `parent.parent/lib` invariant held with zero code edits.
- **P2 → P3**: `src/paper/generate_tables.py` reads the very reports that Phase 2
  confirmed regenerate deterministically, so the paper's tables trace to verified,
  reproducible artifacts (PAPER-03 data lineage).
- **eval.tex** now: Ch1 `\input{ch1_transarc}` (TransArc study) + Ch2 merged bias
  chapter; exactly 2 chapters; balanced environments; all `\input` targets exist.

## Scope Change (accepted)

- **PAPER-04 / SC4 reframed by user**: the original criterion was "`pdflatex`
  compiles cleanly". No LaTeX toolchain is installed and the chapters are authored
  into a larger external paper, so the milestone target was changed to: *tables
  generated from data via a script + structural validation* (balanced env/braces,
  resolved refs, existing `\input` targets). Met. ROADMAP SC4 updated accordingly.

## Tech Debt / Deferred (non-blocking, → v2)

- DOC-01: top-level README mapping the two-pillar structure.
- DOC-02: per-pillar reproduce instructions.
- Rename `src/lib/transarc_error_analysis.py` → `data_loaders.py` (serves both
  pillars; needs a 9-file import sed).
- Actual `pdflatex` PDF build (handled by the external larger paper).

## Environment Note

Work ran on a hostshare mount with an intermittently failing git stat-cache and
flaky shell-output capture. Mitigations applied throughout: `git update-index
--refresh` before git-state reads; ground-truth via `git ls-files`/`git hash-object`;
sandbox-disabled `sed` reads when the Read tool corrupted output; all git operations
serialized inline in the main context (subagents wrote files only, never git).

## Verdict

**PASSED.** All 3 phases verified, all 16 requirements covered, cross-phase
integration coherent. One user-approved scope change (SC4). Ready for
`complete-milestone` → `cleanup`.
