---
phase: 06-handoff-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "A new reader opening the repo can identify the two pillars and where each pillar's code, reports, and paper chapter live (DOC-01)."
    - "A reader can regenerate Pillar 1's reports from scratch by running the documented `python3 src/<area>/<script>.py` commands top-to-bottom (DOC-02)."
    - "A reader can regenerate Pillar 2's reports/tables from scratch, including the Phase-4 metrics API and Phase-5 consequences study, using the documented commands (DOC-02)."
    - "A reader knows the prerequisites (Python 3, stdlib-only/no pip, the external benchmark data path) before running anything."
    - "A reader is pointed to CLAUDE.md and ../CLAUDE.md for deeper dev/build/leakage rules instead of duplicated content."
  artifacts:
    - path: "README.md"
      provides: "Top-level handoff README: two-pillar map table + per-pillar reproduce sections + prerequisites"
      min_lines: 60
      contains: "## Pillar 1"
  key_links:
    - from: "README.md"
      to: "src/lib/metrics_api.py, src/bias/consequences_study.py, src/paper/generate_tables.py"
      via: "documented runnable commands that match shipped scripts"
      pattern: "metrics_api.py"
    - from: "README.md"
      to: "CLAUDE.md"
      via: "markdown link for deeper rules"
      pattern: "CLAUDE.md"
---

<objective>
Create a single top-level `README.md` at the repo root that makes the workspace handoff-ready: it maps the two pillars (DOC-01) and gives per-pillar run/reproduce instructions a new reader can follow to regenerate either pillar's reports from scratch (DOC-02).

Purpose: A new researcher/reviewer must be able to open the repo, understand the two-pillar structure, and reproduce either pillar's outputs without reverse-engineering the scripts.
Output: `README.md` (the only new file). No code changes — all report-generating scripts already exist and run stdlib-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-handoff-docs/06-CONTEXT.md

# Project rules to LINK to (do not duplicate their content):
@CLAUDE.md

<repo_layout>
<!-- Verified live layout. transarc_error_analysis.py lives in src/lib but is a Pillar 1 STUDY script, run directly. -->
src/transarc/  : s12c_sadcode_comparison.py, sad_sam_actual_contribution.py, sad_sam_tp_gain_analysis.py, sam_code_cascade_analysis.py
src/bias/      : benchmark_bias_study.py, evaluation_critique.py, enrollment_bias_analysis.py, enrollment_distortion_analysis.py, extreme_baseline_analysis.py, stupid_baseline_analysis.py, holistic_metrics_analysis.py, creative_metrics_analysis.py, sam_code_distribution_analysis.py, consequences_study.py
src/lib/       : transarc_error_analysis.py (Pillar 1 study + shared loaders), new_metrics_analysis.py (shared lib), metrics_api.py (Phase 4 CLI)
src/paper/     : generate_tables.py
reports/       : generated md + csv (already present)
writing/       : eval.tex (Ch1 \input ch1_transarc.tex, Ch2 Benchmark bias), tables/*.tex
archive/       : archived material — archive/README.md is SEPARATE, do not touch it
</repo_layout>

<verified_script_output_map>
<!-- Use these VERBATIM in the reproduce tables. Output paths confirmed from OUTPUT_MD/OUTPUT_CSV constants in each script. -->
Pillar 1 (TransArc empirical study):
  python3 src/lib/transarc_error_analysis.py        -> reports/TRANSARC_EMPIRICAL_STUDY.md
  python3 src/transarc/sad_sam_actual_contribution.py -> reports/SAD_SAM_ACTUAL_CONTRIBUTION.md
  python3 src/transarc/sad_sam_tp_gain_analysis.py    -> reports/SAD_SAM_TP_GAIN_STUDY.md
  python3 src/transarc/sam_code_cascade_analysis.py   -> reports/SAM_CODE_CASCADE.md
  python3 src/transarc/s12c_sadcode_comparison.py     -> reports/S12C_VS_TRANSARC.csv

Pillar 2 (Benchmark bias & metrics):
  python3 src/bias/benchmark_bias_study.py          -> reports/BENCHMARK_BIAS_STUDY.md
  python3 src/bias/evaluation_critique.py           -> reports/EVALUATION_CRITIQUE.md
  python3 src/bias/enrollment_bias_analysis.py      -> reports/ENROLLMENT_BIAS_ANALYSIS.md
  python3 src/bias/extreme_baseline_analysis.py     -> reports/EXTREME_BASELINES.md
  python3 src/bias/stupid_baseline_analysis.py      -> reports/STUPID_BASELINES.md
  python3 src/bias/holistic_metrics_analysis.py     -> reports/HOLISTIC_METRICS.md
  python3 src/bias/creative_metrics_analysis.py     -> reports/CREATIVE_METRICS.md
  python3 src/bias/sam_code_distribution_analysis.py-> reports/SAM_CODE_DISTRIBUTION.md
  python3 src/lib/new_metrics_analysis.py           -> reports/NEW_METRICS_REPORT.md
  python3 src/lib/metrics_api.py --task sad-sam     -> reports/metrics_sad-sam.csv + writing/tables/metrics_sad-sam.tex
  python3 src/lib/metrics_api.py --task sad-code    -> reports/metrics_sad-code.csv + writing/tables/metrics_sad-code.tex
  python3 src/bias/consequences_study.py            -> reports/CONSEQUENCES_STUDY.md  (reads the two metrics_*.csv above)
  (supporting/intermediate, prints to stdout, no single report: python3 src/bias/enrollment_distortion_analysis.py)

Paper:
  python3 src/paper/generate_tables.py              -> writing/tables/*.tex  (\input by writing/eval.tex)

Prerequisites:
  - Python 3 (stdlib only — NO pip install, no requirements.txt)
  - External benchmark data path: /mnt/hostshare/ardoco-home/ardoco/core/tests-base/src/main/resources/benchmark/
  - TransArc run outputs already present under results/
  - Known limitation: pdflatex is NOT available locally — paper validated via generate_tables.py + structural checks, not a PDF build.
</verified_script_output_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write top-level README.md (pillar map + per-pillar reproduce sections)</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/06-handoff-docs/06-CONTEXT.md (locked decisions + verified script→output map)
    - CLAUDE.md (project rules to LINK to, not duplicate)
    - src/lib/metrics_api.py (lines 1-20, 280-290) to confirm the `--task sad-sam|sad-code` CLI surface
    - src/bias/consequences_study.py (lines 1-35) to confirm it reads reports/metrics_*.csv
    - src/lib/transarc_error_analysis.py (lines 1-25) to confirm it is the Pillar 1 study entrypoint writing TRANSARC_EMPIRICAL_STUDY.md
  </read_first>
  <action>
Create `README.md` at the repo ROOT (NOT archive/README.md — leave that untouched). Write the file with these sections, using the verified script→output map from the context VERBATIM. Use exact heading text where acceptance criteria require it.

# TransArc-EMP

(1) Intro paragraph: one-line description — a stdlib-only Python analysis workspace + LaTeX paper studying ARDoCo TransArc (transitive TLR) and its benchmark evaluation, organized into two pillars. State audience: a new researcher/reviewer.

(2) Section `## Prerequisites`
   - Python 3, stdlib only — no `pip install`, no `requirements.txt`.
   - External benchmark data path (write this exact string): `/mnt/hostshare/ardoco-home/ardoco/core/tests-base/src/main/resources/benchmark/` (5 projects: mediastore, teastore, teammates, bigbluebutton, jabref).
   - TransArc run outputs are under `results/`.
   - How to run any script: `python3 src/<area>/<script>.py` from the repo root; reports land in `reports/`, LaTeX tables in `writing/tables/`.
   - Known limitation: `pdflatex` is not available locally; the paper is validated via `src/paper/generate_tables.py` + structural checks, not a PDF build.
   - For deeper dev/build/leakage rules see the linked rule files (markdown links): `[CLAUDE.md](CLAUDE.md)` (project) and `[../CLAUDE.md](../CLAUDE.md)` (ARDoCo workspace). Do NOT duplicate their content.

(3) Section `## Two Pillars` containing a markdown TABLE with columns: Pillar | Code dirs | Key scripts | Output reports | Paper chapter. Two data rows:
   - Pillar 1 — TransArc empirical study | `src/transarc/`, `src/lib/` | transarc_error_analysis.py, sad_sam_actual_contribution.py, s12c_sadcode_comparison.py (+ tp_gain, cascade) | reports/TRANSARC_EMPIRICAL_STUDY.md (+ contribution/tp-gain/cascade/S12C reports) | writing/ch1_transarc.tex (eval.tex Ch1)
   - Pillar 2 — Benchmark bias & metrics | `src/bias/`, `src/lib/` (incl. `src/lib/metrics_api.py`), `src/bias/consequences_study.py`, `src/paper/generate_tables.py` | benchmark_bias_study.py, evaluation_critique.py, metrics_api.py, consequences_study.py, generate_tables.py (+ baselines, holistic/creative metrics) | reports/BENCHMARK_BIAS_STUDY.md, EVALUATION_CRITIQUE.md, metrics_*.csv, CONSEQUENCES_STUDY.md | writing/eval.tex Ch2

(4) Section `## Pillar 1 — Reproduce` with a short intro then a table or list mapping each Pillar 1 command to its output, VERBATIM from the map: transarc_error_analysis.py→TRANSARC_EMPIRICAL_STUDY.md; sad_sam_actual_contribution.py→SAD_SAM_ACTUAL_CONTRIBUTION.md; sad_sam_tp_gain_analysis.py→SAD_SAM_TP_GAIN_STUDY.md; sam_code_cascade_analysis.py→SAM_CODE_CASCADE.md; s12c_sadcode_comparison.py→S12C_VS_TRANSARC.csv. Present commands as `python3 src/<area>/<script>.py` (note transarc_error_analysis.py is run as `python3 src/lib/transarc_error_analysis.py`). State they are runnable top-to-bottom.

(5) Section `## Pillar 2 — Reproduce` with the Pillar 2 commands VERBATIM from the map, in this order so dependencies are satisfied: the standalone bias/baseline/metric scripts (benchmark_bias_study.py→BENCHMARK_BIAS_STUDY.md, evaluation_critique.py→EVALUATION_CRITIQUE.md, enrollment_bias_analysis.py→ENROLLMENT_BIAS_ANALYSIS.md, extreme_baseline_analysis.py→EXTREME_BASELINES.md, stupid_baseline_analysis.py→STUPID_BASELINES.md, holistic_metrics_analysis.py→HOLISTIC_METRICS.md, creative_metrics_analysis.py→CREATIVE_METRICS.md, sam_code_distribution_analysis.py→SAM_CODE_DISTRIBUTION.md, new_metrics_analysis.py→NEW_METRICS_REPORT.md), THEN the Phase-4 metrics API: `python3 src/lib/metrics_api.py --task sad-sam` and `python3 src/lib/metrics_api.py --task sad-code` → reports/metrics_<task>.csv + writing/tables/metrics_<task>.tex, THEN `python3 src/bias/consequences_study.py` → reports/CONSEQUENCES_STUDY.md (note: reads the two metrics_*.csv, so run the API first). List `python3 src/bias/enrollment_distortion_analysis.py` as a supporting/intermediate script (prints to stdout, no single report file).

(6) Section `## Paper` (or subsection): `python3 src/paper/generate_tables.py` regenerates `writing/tables/*.tex`, which are `\input` by `writing/eval.tex` (Ch1 TransArc study, Ch2 Benchmark bias). Repeat the pdflatex-unavailable limitation note.

Keep tone concise — this is a handoff map, not a tutorial. Do NOT invent scripts or outputs beyond the verified map. Do NOT document the deferred pdflatex build or the deferred transarc_error_analysis.py rename as if available.
  </action>
  <verify>
    <automated>test -f README.md && grep -q "## Two Pillars" README.md && grep -q "## Pillar 1" README.md && grep -q "## Pillar 2" README.md && grep -q "metrics_api.py --task sad-sam" README.md && grep -q "consequences_study.py" README.md && grep -q "generate_tables.py" README.md && grep -q "/mnt/hostshare/ardoco-home/ardoco/core/tests-base/src/main/resources/benchmark/" README.md && grep -q "TRANSARC_EMPIRICAL_STUDY.md" README.md && grep -q "BENCHMARK_BIAS_STUDY.md" README.md && grep -q "(CLAUDE.md)" README.md && grep -q "(../CLAUDE.md)" README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at repo root (and `archive/README.md` is unchanged).
    - Contains heading `## Two Pillars` and a table mapping each pillar to code dirs / key scripts / output reports / paper chapter.
    - Contains headings `## Pillar 1` and `## Pillar 2` reproduce sections.
    - Contains the exact strings `metrics_api.py --task sad-sam`, `consequences_study.py`, `generate_tables.py`.
    - Contains the benchmark data path string `/mnt/hostshare/ardoco-home/ardoco/core/tests-base/src/main/resources/benchmark/`.
    - Contains report names `TRANSARC_EMPIRICAL_STUDY.md` and `BENCHMARK_BIAS_STUDY.md`.
    - Contains markdown links `(CLAUDE.md)` and `(../CLAUDE.md)`.
    - Notes pdflatex is unavailable.
  </acceptance_criteria>
  <done>README.md at repo root maps both pillars in a table and lists each pillar's reproduce commands verbatim from the verified map, with prerequisites and CLAUDE.md links; all grep checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Spot-validate one representative reproduce command per pillar</name>
  <files>README.md</files>
  <read_first>
    - README.md (the reproduce sections written in Task 1 — to pick the representative command and confirm the documented command string matches what you run)
  </read_first>
  <action>
Validate that the documented commands actually produce the named outputs — one representative script per pillar. Do NOT edit the scripts; if a documented command does not match reality, FIX the README to match the working command.

Pillar 2 (run first — fast, deterministic): run the exact documented metrics API command and confirm both outputs appear:
  `python3 src/lib/metrics_api.py --task sad-sam`
  then confirm `reports/metrics_sad-sam.csv` and `writing/tables/metrics_sad-sam.tex` exist.

Pillar 1: run the documented S12C comparison (fast, single CSV output):
  `python3 src/transarc/s12c_sadcode_comparison.py`
  then confirm `reports/S12C_VS_TRANSARC.csv` exists.

If either command errors or the named output is not produced, correct the README command/output text so it matches the working invocation, then re-run. Record in the summary which representative scripts were spot-run.
  </action>
  <verify>
    <automated>python3 src/lib/metrics_api.py --task sad-sam && test -f reports/metrics_sad-sam.csv && test -f writing/tables/metrics_sad-sam.tex && python3 src/transarc/s12c_sadcode_comparison.py && test -f reports/S12C_VS_TRANSARC.csv && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `python3 src/lib/metrics_api.py --task sad-sam` exits 0 and produces `reports/metrics_sad-sam.csv` + `writing/tables/metrics_sad-sam.tex`.
    - `python3 src/transarc/s12c_sadcode_comparison.py` exits 0 and produces `reports/S12C_VS_TRANSARC.csv`.
    - The commands as written in README.md match the commands that succeeded (README corrected if any mismatch).
  </acceptance_criteria>
  <done>One representative reproduce command per pillar runs successfully and produces its documented output; README commands confirmed accurate.</done>
</task>

</tasks>

<threat_model>
Documentation-only phase: README.md is a static Markdown file with no executable, network, or input-handling surface. No trust boundaries crossed; no high-severity STRIDE threats. The only relevant concern is documenting correct, non-destructive commands — mitigated by Task 2 spot-running the representative commands to confirm they behave as documented and do not damage existing reports.
</threat_model>

<verification>
- `README.md` exists at repo root; `archive/README.md` untouched.
- Structural greps (Task 1 verify) all pass: pillar table, both reproduce sections, key script names, benchmark path, report names, CLAUDE.md links, pdflatex note.
- Representative commands (Task 2 verify) run clean and produce their named outputs.
</verification>

<success_criteria>
- DOC-01: A new reader can read README.md and identify both pillars and where each pillar's code, reports, and paper chapter live (pillar map table present).
- DOC-02: A reader can regenerate either pillar's reports by following the per-pillar reproduce sections, including the Phase-4 metrics API and Phase-5 consequences study, with documented prerequisites.
- All documented commands match the shipped scripts (validated for one representative per pillar).
</success_criteria>

<output>
After completion, create `.planning/phases/06-handoff-docs/06-01-SUMMARY.md`
</output>
