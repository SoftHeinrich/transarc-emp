---
phase: 05-consequences-study-converged-metrics
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/paper/generate_tables.py
  - writing/tables/consequences.tex
  - writing/tables/converged_framework.tex
  - writing/eval.tex
autonomous: true
requirements: [STUDY-04]
tags: [paper, latex, booktabs, generate-tables, eval-tex, converged-metrics]

must_haves:
  truths:
    - "Running `python3 src/paper/generate_tables.py` exits 0 and writes writing/tables/consequences.tex and writing/tables/converged_framework.tex"
    - "Both new tables are booktabs floats (\\begin{table}/\\begin{tabular}/\\toprule/\\bottomrule) with \\fone-bearing headers passed via header_override"
    - "writing/eval.tex gains a new Ch2 motivation \\section with a \\label and \\input{tables/consequences}, placed after sec:comprehensive-metrics, covering misleading F1 for BOTH tasks (STUDY-04)"
    - "sec:eval:protocol is extended with the converged Decision+Component recommendation and \\input{tables/converged_framework}; the existing enumerate list is preserved"
    - "Every new \\Cref/\\ref target is defined by a \\label at or before the reference; source-notes cite only retained reports/scripts"
  artifacts:
    - path: "src/paper/generate_tables.py"
      provides: "two new CSV-backed builders (t_consequences, t_converged_framework) registered in main() builders list"
      contains: "t_consequences"
    - path: "writing/tables/consequences.tex"
      provides: "generated headline-vs-honest table (both tasks)"
      contains: "tabular"
    - path: "writing/tables/converged_framework.tex"
      provides: "generated converged Decision+Component table"
      contains: "tabular"
    - path: "writing/eval.tex"
      provides: "new Ch2 motivation section + extended protocol"
      contains: "input{tables/consequences}"
  key_links:
    - from: "src/paper/generate_tables.py"
      to: "reports/metrics_sad-sam.csv + reports/metrics_sad-code.csv"
      via: "_load_metrics(task) parameterized DictReader"
      pattern: "metrics_sad-(sam|code).csv"
    - from: "writing/eval.tex"
      to: "writing/tables/consequences.tex"
      via: "\\input{tables/consequences}"
      pattern: "input\\{tables/consequences\\}"
    - from: "writing/eval.tex"
      to: "writing/tables/converged_framework.tex"
      via: "\\input{tables/converged_framework}"
      pattern: "input\\{tables/converged_framework\\}"
---

<objective>
Turn the Plan-01 evidence into paper material (STUDY-04): add two CSV-backed table generators to `src/paper/generate_tables.py` (emitting `writing/tables/consequences.tex` and `writing/tables/converged_framework.tex`), then extend `writing/eval.tex` with (a) a new Chapter-2 motivation section on misleading \fone across BOTH tasks and (b) an extension to `sec:eval:protocol` presenting the converged Decision+Component framework.

Purpose: make the consequences study a citable, structurally-valid part of the paper without a pdflatex toolchain. Tables are single-sourced from the Phase-4 CSVs; prose cites only retained reports plus the new `reports/CONSEQUENCES_STUDY.md`.

Output: 2 new builders + 2 generated `.tex` tables + 2 edits to `eval.tex`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-consequences-study-converged-metrics/05-CONTEXT.md
@.planning/phases/05-consequences-study-converged-metrics/05-PATTERNS.md
@.planning/phases/05-consequences-study-converged-metrics/05-01-SUMMARY.md

<interfaces>
<!-- generate_tables.py helpers already in-file (do NOT redefine): -->
<!--   ROOT (line 28), REPORTS (29), OUT (30); render_table(rows, caption, label, note=None, header_override=None) (136); -->
<!--   write_table(name, content) -> writing/tables/<name>.tex (169); colspec/latex_escape (108-133). -->
<!-- CSV-backed builder analog: t_s12c_four_level (353-397) and t_dashboard (400-447). -->
<!-- CRITICAL idioms: -->
<!--   * header_override=header is MANDATORY when a header cell contains a macro (\fone) — else latex_escape mangles the backslash (render_table 139-144). -->
<!--   * The Phase-4 metrics_*.csv store F1 as 0–1 DECIMALS already (e.g. 0.943). Do NOT divide by 100 (unlike _fmt_f1 at 346-350). Format with "%.3f" % float(v). -->
<!--   * Treat the "—" em-dash cell as "--" (LaTeX N/A), matching metrics_api.NA_TEX. -->
<!--   * The metrics_*.csv already contains an `Average` row — read it directly; do NOT recompute the average. -->
<!-- Phase-4 CSV header (both files): -->
project,link_f1,sentence_f1,decision_f1,component_f1,file_f1,weighted_f1,mcc,map,acf1,ndg,hus
<!-- sad-sam Average: link_f1=0.799 sentence_f1=0.875 decision_f1=0.799 component_f1=0.799; teammates sentence_f1=0.916 link_f1=0.710. -->
<!-- sad-code Average: file_f1=0.803 decision_f1=0.596 component_f1=0.714; jabref file_f1=0.943 decision_f1=0.394. -->

<!-- eval.tex anchors: -->
<!--   Ch2 opens at \section{...}\label{sec:comprehensive-metrics}\label{ch:eval} (lines 389-391), intro paragraph ends ~line 401. -->
<!--   \section{\fone Variants}\label{sec:eval:f1} at 405/409. -->
<!--   \section{Recommended Evaluation Protocol}\label{sec:eval:protocol} at 986/987, with a \begin{enumerate}...\end{enumerate} (992-1004). -->
<!--   \end{document} at line 1007. -->
<!--   Macros defined (16-21): \sadcode \sadsam \samcode \transarc \fone \acfone (all take NO argument). cleveref loaded (\Cref). -->
<!--   Generated tables enter via \input{tables/<name>} (ch1_transarc.tex precedent). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add t_consequences + t_converged_framework builders and register them</name>
  <files>src/paper/generate_tables.py, writing/tables/consequences.tex, writing/tables/converged_framework.tex</files>
  <read_first>
    - src/paper/generate_tables.py lines 25-30 (ROOT/REPORTS/OUT), 136-173 (render_table/write_table), 336-350 (_load_csv/_fmt_f1), 353-447 (t_s12c_four_level + t_dashboard analogs), 451-470 (main builders list)
    - reports/metrics_sad-sam.csv and reports/metrics_sad-code.csv (full, 7 lines each)
    - .planning/phases/05-consequences-study-converged-metrics/05-PATTERNS.md lines 121-189 (builder pattern, header_override + no-/100 + Average-row caveats, registry)
  </read_first>
  <action>
    Edit `src/paper/generate_tables.py`. Near the existing `_load_csv` (line 339), add a parameterized metrics-CSV reader (do NOT touch the existing `_load_csv`/`CSV_FILE`/`_fmt_f1`):
    ```python
    METRICS_CSV = {
        "sad-sam":  REPORTS / "metrics_sad-sam.csv",
        "sad-code": REPORTS / "metrics_sad-code.csv",
    }

    def _load_metrics(task):
        with METRICS_CSV[task].open(encoding="utf-8") as f:
            return list(csv.DictReader(f))  # includes the Average row

    def _fmt_dec(v):
        """metrics_*.csv stores F1 as a 0–1 decimal; em-dash = N/A."""
        if v in ("—", "", None):
            return "--"
        return "%.3f" % float(v)
    ```

    Add builder `def t_consequences():` (headline-vs-honest, BOTH tasks). Read both CSVs with `_load_metrics`. Build ONE table; pass `header_override` (every \fone cell is a macro). Columns:
    `header = ["Task", "Project", "Headline \\fone", "Decision \\fone", "Component \\fone", "Sentence \\fone"]`
    - SAD-SAM block: for each row (5 projects then the `Average` row, in CSV order), append `["\\sadsam", r["project"], _fmt_dec(r["link_f1"]), _fmt_dec(r["decision_f1"]), _fmt_dec(r["component_f1"]), _fmt_dec(r["sentence_f1"])]`. (link_f1 is the SAD-SAM headline.)
    - SAD-CODE block: for each row (5 projects then `Average`), append `["\\sadcode", r["project"], _fmt_dec(r["file_f1"]), _fmt_dec(r["decision_f1"]), _fmt_dec(r["component_f1"]), _fmt_dec(r["sentence_f1"])]`. (file_f1 is the headline; sentence_f1 is "—" → renders "--".)
    - caption: `"Headline versus honest \\fone for \\sadsam and \\sadcode: the headline metric (link for \\sadsam, file for \\sadcode) against decision-, component-, and sentence-level \\fone"`
    - label: `"tab:consequences"`
    - note: `"Source: \\texttt{reports/metrics\\_sad-sam.csv}, \\texttt{reports/metrics\\_sad-code.csv}, and \\texttt{reports/CONSEQUENCES\\_STUDY.md}. JabRef is best on file \\fone (0.943) yet worst on decision \\fone (0.394)."`
    - Return `write_table("consequences", render_table([header] + data, caption=<caption>, label="tab:consequences", note=<note>, header_override=header))`.

    Add builder `def t_converged_framework():` (STUDY-03). Read both CSVs; grab the `Average` row of each (the dict where `r["project"] == "Average"`). Build a 2-row table; pass `header_override`:
    `header = ["Task", "Headline metric", "Headline \\fone", "Decision \\fone", "Component \\fone", "Headline$-$Decision $\\Delta$"]`
    - Compute deltas from the average cells: `sam_delta = "%.3f" % (float(sam_avg["link_f1"]) - float(sam_avg["decision_f1"]))`; `code_delta = "%.3f" % (float(code_avg["file_f1"]) - float(code_avg["decision_f1"]))` (≈0.207).
    - Row 1: `["\\sadsam", "link", _fmt_dec(sam_avg["link_f1"]), _fmt_dec(sam_avg["decision_f1"]), _fmt_dec(sam_avg["component_f1"]), sam_delta]`
    - Row 2: `["\\sadcode", "file", _fmt_dec(code_avg["file_f1"]), _fmt_dec(code_avg["decision_f1"]), _fmt_dec(code_avg["component_f1"]), code_delta]`
    - caption: `"Converged evaluation axis: Decision- and Component-level \\fone as the common honest story across \\sadsam and \\sadcode (cross-project averages)"`
    - label: `"tab:converged-framework"`
    - note: `"Source: \\texttt{reports/metrics\\_sad-sam.csv}, \\texttt{reports/metrics\\_sad-code.csv}. The converged axis extends, not replaces, the corrected metrics in \\texttt{reports/EVALUATION\\_CRITIQUE.md}."`
    - Return `write_table("converged_framework", render_table([header] + data, caption=<caption>, label="tab:converged-framework", note=<note>, header_override=header))`.

    Register both in `main()` `builders` list (lines 451-465) under the Chapter-2 comment, immediately after `t_dashboard`:
    ```python
        # Chapter 2 (Benchmark bias / metrics)
        t_dashboard,
        t_consequences,         # STUDY-01/02 headline-vs-honest, both tasks
        t_converged_framework,  # STUDY-03 converged Decision+Component
    ```

    Do NOT add benchmark word lists; project names flow through as opaque CSV data (consistent with the file's existing stance, lines 10-14). Use `_fmt_dec`, never `_fmt_f1`/`/ 100`, for the new builders.
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && python3 src/paper/generate_tables.py && test -f writing/tables/consequences.tex && test -f writing/tables/converged_framework.tex && grep -q "tabular" writing/tables/consequences.tex && grep -q "tab:converged-framework" writing/tables/converged_framework.tex && grep -q "0.394" writing/tables/consequences.tex && grep -q "0.207\|0.20" writing/tables/converged_framework.tex && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `python3 src/paper/generate_tables.py` exits 0 and prints `TOTAL` >= 12 (10 prior + 2 new).
    - `writing/tables/consequences.tex` contains `\begin{tabular}`, `\toprule`, `\bottomrule`, `\fone`, `0.394`, and `0.916`.
    - `writing/tables/converged_framework.tex` contains `tab:converged-framework`, a `\Delta`, and `0.207` (or `0.20`).
    - `grep -n "t_consequences\|t_converged_framework" src/paper/generate_tables.py` shows both defined AND both appended in the builders list.
    - The two new builders use `_fmt_dec` (no `/ 100`, no `_fmt_f1`).
  </acceptance_criteria>
  <done>Both builders defined, registered, run clean; both .tex tables are valid booktabs floats with correct decimals (no /100) and the validated anchors.</done>
</task>

<task type="auto">
  <name>Task 2: Add Ch2 motivation section + extend sec:eval:protocol in eval.tex</name>
  <files>writing/eval.tex</files>
  <read_first>
    - writing/eval.tex lines 14-22 (macros — all take NO argument), 386-415 (Ch2 open + sec:comprehensive-metrics intro + sec:eval:f1 + \Cref idiom), 985-1007 (sec:eval:protocol + enumerate + \end{document})
    - writing/tables/consequences.tex and writing/tables/converged_framework.tex (generated in Task 1 — confirm labels tab:consequences / tab:converged-framework)
    - reports/CONSEQUENCES_STUDY.md (generated in Plan 01 — for the prose numbers)
    - .planning/phases/05-consequences-study-converged-metrics/05-PATTERNS.md lines 192-228 (section/label conventions, \input vs inline, citations scope)
  </read_first>
  <action>
    Edit `writing/eval.tex`. TWO insertions; preserve ALL existing content (read-modify-write carefully — do not delete or reorder existing sections). Use the Edit tool with surrounding-context anchors so nothing else shifts.

    INSERTION A — new motivation section in Chapter 2. Insert immediately AFTER the `sec:comprehensive-metrics` intro paragraph (ends at line 401, just before the blank line preceding `\section{\fone Variants}` at line 405). Author exactly:
    ```latex

    %% --------------------------------------------------------------------
    \section{Misleading \fone Across Both Tasks}
    \label{sec:eval:misleading-both}

    The headline \fone reported for each task hides task-specific distortions.
    For \sadcode, file-level enrollment inflates a handful of annotator
    decisions into hundreds of equally-weighted file pairs; for \sadsam, the
    single link \fone obscures its disagreement with sentence-, component-, and
    quality-oriented views. \Cref{tab:consequences} contrasts each task's
    headline metric against decision-, component-, and (for \sadsam)
    sentence-level \fone.

    For \sadsam, the average link \fone $=0.799$ understates per-sentence
    retrieval: on Teammates, sentence \fone is $0.916$ against a link \fone of
    $0.710$, a $0.206$ gap the headline conceals. For \sadcode, JabRef attains
    the best file \fone ($0.943$) yet the worst decision \fone ($0.394$)---the
    same trace links, reordered by aggregation level. Across projects the
    \sadcode average drops from $0.803$ (file) to $0.596$ (decision), a $0.207$
    enrollment-driven gap.

    \input{tables/consequences}
    ```

    INSERTION B — extend `sec:eval:protocol`. Insert AFTER the existing `\end{enumerate}` (line 1004) and BEFORE `\end{document}` (line 1007). Do NOT modify the existing enumerate items. Author exactly:
    ```latex

    \paragraph{A converged axis for both tasks.}
    The recommendations above harden \sadcode file-level scoring, but \sadsam
    and \sadcode are still reported on incommensurable headline metrics. We
    therefore add one unifying recommendation, layered atop---not replacing---the
    corrected metrics above: report \emph{decision-level} and
    \emph{component-level} \fone for both tasks. For \sadsam the decision is the
    \texttt{(model element, sentence)} link and the component aggregates by model
    element; for \sadcode the decision is the raw pre-enrollment
    \texttt{(sentence, file-or-directory)} pair and the component aggregates by
    architecture component. \Cref{tab:converged-framework} shows that on this
    converged axis the two tasks tell one coherent story, exposing the \sadcode
    file-versus-decision gap ($0.207$) as enrollment inflation rather than
    genuine quality.

    \input{tables/converged_framework}
    ```

    After editing, ensure: `\label{sec:eval:misleading-both}` is defined before any reference; both `\input{tables/...}` lines immediately follow the prose that `\Cref`s the corresponding table; no existing `\label`/`\section`/`\end{document}` removed; `\end{document}` remains the final line of the file.

    Citations: only retained reports/scripts are referenced (the table `note=` source lines from Task 1 already carry `reports/CONSEQUENCES_STUDY.md`, `reports/EVALUATION_CRITIQUE.md`, `reports/metrics_*.csv`). Do NOT reference archived/removed material.
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && grep -q "sec:eval:misleading-both" writing/eval.tex && grep -q "input{tables/consequences}" writing/eval.tex && grep -q "input{tables/converged_framework}" writing/eval.tex && grep -q "Cref{tab:consequences}" writing/eval.tex && grep -q "Cref{tab:converged-framework}" writing/eval.tex && grep -q "tab:consequences" writing/tables/consequences.tex && grep -q "tab:converged-framework" writing/tables/converged_framework.tex && tail -1 writing/eval.tex | grep -q "end{document}" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "begin{document}" writing/eval.tex` == 1 and `grep -c "end{document}" writing/eval.tex` == 1 (document envelope intact).
    - `grep -n "sec:comprehensive-metrics" writing/eval.tex` and `grep -n "sec:eval:f1" writing/eval.tex` still present (existing sections preserved).
    - `\section{Misleading \fone Across Both Tasks}` and `\label{sec:eval:misleading-both}` present; `\input{tables/consequences}` present and appears after its `\Cref{tab:consequences}`.
    - `\paragraph{A converged axis for both tasks.}` present after the protocol `\end{enumerate}`; `\input{tables/converged_framework}` present after its `\Cref{tab:converged-framework}`.
    - Every `\Cref{tab:...}` target (`tab:consequences`, `tab:converged-framework`) has a matching `\label` in the corresponding generated `writing/tables/*.tex`.
    - The last non-empty line of `writing/eval.tex` is `\end{document}`.
  </acceptance_criteria>
  <done>eval.tex has the new Ch2 motivation section (both tasks) and an extended protocol with the converged framework; both \input tables resolve, all \Cref targets are labeled, document envelope and prior sections intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem read | Reads committed CSVs + existing eval.tex; no untrusted/network input |
| filesystem write | Generates two table .tex files and edits eval.tex (read-modify-write) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | eval.tex read-modify-write | mitigate | Anchored Edit insertions only (no full rewrite); acceptance criteria assert prior sections + single document envelope survive |
| T-05-05 | Information disclosure | benchmark leakage / archived citations | mitigate | Only retained reports cited; project names are opaque CSV data; no benchmark word lists |
| T-05-06 | Tampering | table path/label collision | accept | New table names (consequences, converged_framework) do not collide with the existing 12; offline generator, deterministic output |
</threat_model>

<verification>
- `python3 src/paper/generate_tables.py` exits 0, prints TOTAL >= 12, writes both new `.tex` tables (booktabs, correct decimals, anchors 0.394/0.916/0.207).
- `writing/eval.tex` contains the new section + extended protocol; all new `\Cref` targets resolve to `\label`s; `\input{tables/consequences}` and `\input{tables/converged_framework}` present.
- Document envelope intact (one `\begin{document}`, one `\end{document}` as last line); prior sections preserved.
- All source-notes cite only retained reports/scripts.
</verification>

<success_criteria>
- STUDY-04: consequences findings + converged framework written into Ch2 of eval.tex as a motivation section + protocol extension, citing only retained material, tables regenerating from the Phase-4 CSVs, eval.tex structurally valid (no pdflatex needed).
</success_criteria>

<output>
After completion, create `.planning/phases/05-consequences-study-converged-metrics/05-02-SUMMARY.md`
</output>
