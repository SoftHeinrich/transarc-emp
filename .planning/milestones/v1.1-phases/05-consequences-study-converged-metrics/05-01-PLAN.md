---
phase: 05-consequences-study-converged-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bias/consequences_study.py
  - reports/CONSEQUENCES_STUDY.md
autonomous: true
requirements: [STUDY-01, STUDY-02, STUDY-03]
tags: [consequences, sad-sam, sad-code, converged-metrics, stdlib, report]

must_haves:
  truths:
    - "Running `python3 src/bias/consequences_study.py` exits 0 and (re)writes reports/CONSEQUENCES_STUDY.md"
    - "The report quantifies SAD-SAM pure-F1 disagreement: link_f1 vs sentence_f1/component_f1 and vs MCC/MAP/HUS, with per-project deltas (STUDY-01)"
    - "The report consolidates SAD-CODE file-level consequences with the validated anchors (JabRef File 0.943 vs Decision 0.394; avg File 0.803 / Decision 0.596 / Component 0.714) read from the CSV, no recomputation (STUDY-02)"
    - "The report proposes a converged Decision+Component framework as the common honest axis across both tasks (STUDY-03)"
    - "No benchmark-derived word lists appear in the script (stopwords only; project names read as opaque CSV data)"
  artifacts:
    - path: "src/bias/consequences_study.py"
      provides: "stdlib analysis script: loads both Phase-4 metrics CSVs, derives headline-vs-honest deltas + ranking disagreements, writes the markdown report"
      min_lines: 120
    - path: "reports/CONSEQUENCES_STUDY.md"
      provides: "generated motivation report covering STUDY-01/02/03"
      contains: "0.394"
  key_links:
    - from: "src/bias/consequences_study.py"
      to: "reports/metrics_sad-sam.csv"
      via: "csv.DictReader load_metrics_csv('sad-sam')"
      pattern: "metrics_sad-sam.csv"
    - from: "src/bias/consequences_study.py"
      to: "reports/metrics_sad-code.csv"
      via: "csv.DictReader load_metrics_csv('sad-code')"
      pattern: "metrics_sad-code.csv"
    - from: "src/bias/consequences_study.py"
      to: "reports/CONSEQUENCES_STUDY.md"
      via: "out() accumulator single write at end"
      pattern: "CONSEQUENCES_STUDY.md"
---

<objective>
Build `src/bias/consequences_study.py`: a stdlib-only analysis script that reads the two Phase-4 metrics CSVs (`reports/metrics_sad-sam.csv`, `reports/metrics_sad-code.csv`) and emits `reports/CONSEQUENCES_STUDY.md` — a motivation report covering STUDY-01 (SAD-SAM pure-F1 consequences, NEW content), STUDY-02 (SAD-CODE file-level consequences, CONSOLIDATED from existing bias findings — NO recompute), and STUDY-03 (the converged Decision+Component framework).

Purpose: produce the numeric evidence backbone the Phase-5 paper write-up (Plan 02) cites. The CSVs are the single source of truth; this script differences headline columns against the honest (decision/component) columns and counts ranking disagreements — it does NOT recompute any metric.

Output: `src/bias/consequences_study.py` (new) + `reports/CONSEQUENCES_STUDY.md` (generated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-consequences-study-converged-metrics/05-CONTEXT.md
@.planning/phases/05-consequences-study-converged-metrics/05-PATTERNS.md

<interfaces>
<!-- Phase-4 CSV schema (header, 5 project rows, then an Average row; "—" em-dash = N/A cell). -->
<!-- reports/metrics_sad-sam.csv and reports/metrics_sad-code.csv share this header: -->
project,link_f1,sentence_f1,decision_f1,component_f1,file_f1,weighted_f1,mcc,map,acf1,ndg,hus

<!-- sad-sam: link_f1 == decision_f1 == component_f1 here numerically equal in CSV (link is the SAD-SAM headline);
     sentence_f1, mcc, map, hus populated; file_f1/weighted_f1/acf1/ndg are "—". -->
<!-- sad-code: file_f1 is the headline; decision_f1/component_f1/weighted_f1/mcc/acf1/ndg/hus populated;
     link_f1/sentence_f1/map are "—". -->

<!-- VALIDATED ANCHOR NUMBERS already present in the committed CSVs (the script must reproduce them by reading, not recompute): -->
<!-- sad-sam Average: link_f1=0.799, sentence_f1=0.875; teammates sentence_f1=0.916 vs link_f1=0.710. -->
<!-- sad-code Average: file_f1=0.803, decision_f1=0.596, component_f1=0.714; jabref file_f1=0.943, decision_f1=0.394. -->

<!-- Report-writer analog: src/bias/evaluation_critique.py
     - module header + sys.path.insert(...,"lib") bootstrap (lines 1-35); for THIS script the heavy
       transarc_error_analysis import is UNNEEDED (pure CSV numeric analysis) — import only
       csv, sys, pathlib.Path, collections as needed.
     - out() accumulator + final single write idiom (lines 1289-1321):
         def main():
             md_lines = []
             def out(s=""):
                 print(s); md_lines.append(s)
             ...
             with open(OUTPUT_MD, "w") as f:
                 f.write("\n".join(md_lines)); f.write("\n")
     - section-function signature `def part_x(out, ...)` emitting markdown pipe tables, plus the
       "compute avg, find best, flag flips" pattern (part7_synthesis, lines 1152-1253):
         best = max(vals, key=vals.get)
         if len(set(winners)) > 1: flips += 1
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSV loaders + module scaffold + SAD-SAM and SAD-CODE consequence sections</name>
  <files>src/bias/consequences_study.py, reports/CONSEQUENCES_STUDY.md</files>
  <read_first>
    - src/bias/evaluation_critique.py lines 1-35 (header + sys.path bootstrap) and lines 1289-1321 (out() accumulator + final write)
    - src/bias/evaluation_critique.py lines 1152-1253 (part7_synthesis: markdown pipe-table emission, max(vals,key=vals.get) winner, flip counter)
    - reports/metrics_sad-sam.csv (full — 7 lines) and reports/metrics_sad-code.csv (full — 7 lines)
    - .planning/phases/05-consequences-study-converged-metrics/05-PATTERNS.md lines 27-117 (analogs A+B, anchor numbers)
  </read_first>
  <action>
    Create `src/bias/consequences_study.py` (stdlib only: `csv`, `sys`, `from pathlib import Path`; add `from collections import defaultdict` only if used). Do NOT import `transarc_error_analysis` — this is pure CSV numeric analysis.

    Module-level constants (absolute-path convention, matching metrics_api/evaluation_critique):
    - `REPORTS = Path("/mnt/hostshare/ardoco-home/transarc-emp/reports")`
    - `OUTPUT_MD = REPORTS / "CONSEQUENCES_STUDY.md"`
    - `PROJECT_ORDER = ["mediastore", "teastore", "teammates", "bigbluebutton", "jabref"]` (display order; read from the CSV, this is only ordering — opaque data, NOT a benchmark word list).

    Add CSV reader helpers (copy the idiom from PATTERNS lines 96-114):
    ```python
    def load_metrics_csv(task):
        """task in {'sad-sam','sad-code'}; returns (rows_by_project, avg_row)."""
        rows, avg = {}, None
        with (REPORTS / f"metrics_{task}.csv").open(encoding="utf-8") as f:
            for r in csv.DictReader(f):
                if r["project"] == "Average":
                    avg = r
                else:
                    rows[r["project"]] = r
        return rows, avg

    def num(v):
        return None if v in ("—", "", None) else float(v)
    ```

    Implement the SAD-SAM section (STUDY-01) as `def part_sad_sam(out, rows, avg):`
    - Headline = `link_f1`. Honest comparators = `sentence_f1`, `component_f1`, plus `mcc`, `map`, `hus`.
    - Emit a markdown pipe table with columns: `Project | Link F1 (headline) | Sentence F1 | Component F1 | MCC | MAP | HUS | Link−Sentence Δ`, one row per project in `PROJECT_ORDER` plus an `Average` row read from `avg` (do NOT recompute the average — read it from the CSV `Average` row; only the Δ column is computed as `link_f1 - sentence_f1`).
    - After the table, emit prose quantifying the distortion: the largest |Link−Sentence| gap (call out teammates: sentence 0.916 vs link 0.710 → the headline understates per-sentence retrieval by ~0.206), and a sentence noting where link_f1 and the MCC/HUS quality views diverge. Frame: "the single SAD-SAM headline link \fone hides disagreement with sentence-, component-, and quality-oriented (MCC/MAP/HUS) views."

    Implement the SAD-CODE section (STUDY-02) as `def part_sad_code(out, rows, avg):`
    - Headline = `file_f1`. Honest comparators = `decision_f1`, `component_f1`.
    - Emit a pipe table: `Project | File F1 (headline) | Decision F1 | Component F1 | File−Decision Δ`, per project + `Average` row from `avg`. Only the Δ column is computed.
    - Emit the consolidation prose with the validated anchors read from the CSV: JabRef is best on File F1 (0.943) yet worst on Decision F1 (0.394) — a ranking flip; cross-project Average File 0.803 vs Decision 0.596 vs Component 0.714. State that these consolidate prior findings in `reports/EVALUATION_CRITIQUE.md` and `reports/BENCHMARK_BIAS_STUDY.md` (enrollment inflation ≈35.5x, block homogeneity, ranking flips) — explicitly NO recomputation here.

    Wire `main()` using the out()-accumulator idiom: title `# Consequences of Misleading F1 Across SAD-SAM and SAD-CODE`, a one-line subtitle, then call `part_sad_sam(out, *load_metrics_csv("sad-sam"))` and `part_sad_code(out, *load_metrics_csv("sad-code"))`. (Task 2 adds the converged section before the final write.) For THIS task, end main with the single write block and the `if __name__ == "__main__": main()` guard so the script runs standalone.

    NO benchmark-derived word lists: project names are opaque ordering data only; no component names, aliases, or synonym maps. No third-party imports.
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && python3 src/bias/consequences_study.py && grep -q "0.394" reports/CONSEQUENCES_STUDY.md && grep -q "0.916" reports/CONSEQUENCES_STUDY.md && grep -qi "SAD-SAM" reports/CONSEQUENCES_STUDY.md && grep -qi "SAD-CODE" reports/CONSEQUENCES_STUDY.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `python3 src/bias/consequences_study.py` exits 0.
    - `reports/CONSEQUENCES_STUDY.md` exists and contains the strings `0.394`, `0.943`, `0.916`, `0.803`, `0.596`, `0.714` (all read from the CSVs).
    - `grep -c "import" src/bias/consequences_study.py` shows only stdlib imports; `grep -n "transarc_error_analysis" src/bias/consequences_study.py` returns nothing.
    - Report contains a SAD-SAM pipe table with a `Link` and `Sentence` column and a SAD-CODE pipe table with `File` and `Decision` columns.
  </acceptance_criteria>
  <done>Script runs standalone, regenerates CONSEQUENCES_STUDY.md with both task consequence sections containing the validated anchors; zero non-stdlib imports.</done>
</task>

<task type="auto">
  <name>Task 2: Converged Decision+Component framework section (STUDY-03)</name>
  <files>src/bias/consequences_study.py, reports/CONSEQUENCES_STUDY.md</files>
  <read_first>
    - src/bias/consequences_study.py (the file just created in Task 1)
    - src/bias/evaluation_critique.py lines 1219-1253 (the "| Metric | ... | Best |" table + ranking-flip counter idiom)
    - .planning/phases/05-consequences-study-converged-metrics/05-CONTEXT.md lines 28-34 (converged framework definition: decision=link / component=model-elem for sad-sam; decision=raw / component=arch-comp for sad-code)
  </read_first>
  <action>
    Add `def part_converged(out, sam_rows, sam_avg, code_rows, code_avg):` to `src/bias/consequences_study.py` and call it in `main()` between `part_sad_code(...)` and the final write block.

    The converged framework (STUDY-03) presents Decision-level + Component-level as the ONE common honest axis across both tasks, layered atop existing corrected metrics (it does not replace them). Emit:
    - A short framing paragraph stating the mapping per CONTEXT lines 28-32:
      SAD-SAM: decision == link `(modelElementID, sentence)` (no enrollment inflation); component == aggregate by model element.
      SAD-CODE: decision == raw pre-enrollment `(sentence, dir-or-file)`; component == aggregate by architecture component.
    - A comparison pipe table with columns: `Task | Headline F1 | Headline metric | Decision F1 | Component F1 | Headline−Decision Δ`, with two data rows:
        SAD-SAM row: headline = `link_f1` Average (0.799), headline metric = `link`, decision = `decision_f1` Average, component = `component_f1` Average. (For sad-sam these coincide — state that the SAD-SAM headline is already at the honest decision granularity, so its distortion is the *sentence/quality* disagreement shown above, not enrollment.)
        SAD-CODE row: headline = `file_f1` Average (0.803), headline metric = `file`, decision = `decision_f1` Average (0.596), component = `component_f1` Average (0.714). Δ = 0.803 − 0.596 = 0.207.
      Read all values from the `avg` rows; only the Δ column is computed.
    - A ranking-disagreement count for SAD-CODE: reuse the flip pattern (evaluation_critique lines 1238-1251) — for each project compare which project ranks highest under File vs under Decision; report how many of the 5 projects change their relative standing (concretely: JabRef ranks #1 by File but last by Decision). Emit a sentence like "Under the converged Decision+Component axis, the two tasks tell one coherent story: report Decision and Component \fone alongside any headline number."
    - Close with a one-line recommendation that this converged view extends (not replaces) the corrected metrics in `reports/EVALUATION_CRITIQUE.md` / `reports/NEW_METRICS_REPORT.md`.

    Keep stdlib-only; no benchmark word lists.
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && python3 src/bias/consequences_study.py && grep -qi "converged" reports/CONSEQUENCES_STUDY.md && grep -qi "Decision" reports/CONSEQUENCES_STUDY.md && grep -qi "Component" reports/CONSEQUENCES_STUDY.md && grep -q "0.207\|0.20" reports/CONSEQUENCES_STUDY.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `python3 src/bias/consequences_study.py` exits 0 after the change.
    - `reports/CONSEQUENCES_STUDY.md` contains a section whose heading matches `grep -i "converged"` and a table with both `Decision` and `Component` columns.
    - The SAD-CODE File−Decision delta `0.207` (or `0.20...`) appears in the report.
    - `grep -n "transarc_error_analysis" src/bias/consequences_study.py` still returns nothing (no new non-stdlib import).
  </acceptance_criteria>
  <done>The report has three sections (SAD-SAM consequences, SAD-CODE consequences, converged framework); converged section maps both tasks to Decision+Component, shows the headline-vs-honest deltas, and states the unified recommendation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem read | Reads two committed CSVs under reports/; no untrusted/network input |
| filesystem write | Writes one markdown file under reports/ (hardcoded absolute path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | output path | accept | Output path is a hardcoded absolute constant under reports/; no user-supplied path, no traversal surface |
| T-05-02 | Information disclosure | benchmark leakage | mitigate | No benchmark-derived word lists; project names read as opaque CSV data only (acceptance criteria grep-verifies stdlib-only imports) |
| T-05-03 | Denial of service | malformed CSV | accept | CSVs are local committed artifacts produced by the Phase-4 API; offline single-run script, no availability concern |
</threat_model>

<verification>
- `python3 src/bias/consequences_study.py` exits 0 and regenerates `reports/CONSEQUENCES_STUDY.md`.
- Report contains validated anchors: `0.394`, `0.943`, `0.916`, `0.803`, `0.596`, `0.714`, `0.207`.
- Report has three sections: SAD-SAM consequences (STUDY-01), SAD-CODE consequences (STUDY-02), converged framework (STUDY-03).
- Script imports are stdlib only; no `transarc_error_analysis` import; no hardcoded component/project vocabulary beyond opaque ordering.
</verification>

<success_criteria>
- STUDY-01: SAD-SAM pure-F1 consequences quantified (link vs sentence/component/MCC/MAP/HUS deltas) from the CSV.
- STUDY-02: SAD-CODE file-level consequences consolidated with validated anchors, no recompute.
- STUDY-03: converged Decision+Component framework proposed across both tasks with headline-vs-honest deltas + ranking disagreement.
- Generated `reports/CONSEQUENCES_STUDY.md` is the citable evidence artifact for Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/05-consequences-study-converged-metrics/05-01-SUMMARY.md`
</output>
