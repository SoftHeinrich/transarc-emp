---
phase: 09-paper-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/paper/generate_tables.py
  - src/paper/check_eval_structure.py
  - writing/tables/component_suite.tex
  - writing/tables/metric_fitness.tex
  - writing/eval.tex
autonomous: true
requirements: [CMP-05]
must_haves:
  truths:
    - "A new Ch2 section in writing/eval.tex presents the level-agnostic component suite (one object, both granularities), the universe-reconciliation correction, and the cross-system comparison with the artemis long-tail finding — citing ONLY retained scripts/reports (component_suite.py, metric_fitness.py, reports/COMPONENT_SUITE.md, reports/METRIC_FITNESS.md, reports/COMPONENT_UNIVERSE_RECONCILIATION.md). (SC1)"
    - "Every number in the new prose and the two new tables is read from / matches the committed reports/COMPONENT_SUITE_{sad-code,sad-model}.csv and reports/METRIC_FITNESS.csv — no hand-typed metric values that diverge from the CSVs. (SC1)"
    - "Two new tables (tab:component-suite, tab:metric-fitness) are emitted by src/paper/generate_tables.py into writing/tables/ via new t_* builders registered in main(); re-running the generator is byte-identical on the 2nd run (deterministic). (SC2)"
    - "The stale Sentence-F1 example at eval.tex ~L416-418 (buggy 0.916 / 0.206 gap on Teammates) is re-anchored to the corrected data (BigBlueButton 0.876 vs link 0.793, or the AVG 0.825 > 0.799), matching writing/tables/metrics_sad-sam.tex. (SC1, resolves STATE open-item #1)"
    - "A new stdlib src/paper/check_eval_structure.py validates writing/eval.tex structurally: every \\ref/\\Cref/\\cref target has a matching \\label, and every \\input{tables/X} file exists; exits 0 on success, non-zero with a report on failure. (SC3)"
    - "generate_tables.py and check_eval_structure.py are stdlib-only and contain no benchmark-derived word lists (project/column names read as opaque data from reports). Invariant carried from prior phases."
  artifacts:
    - path: "src/paper/generate_tables.py"
      provides: "Two new Ch2 table builders (t_component_suite, t_metric_fitness) reading the suite + fitness CSVs, registered in main()"
      contains: "COMPONENT_SUITE"
    - path: "writing/tables/component_suite.tex"
      provides: "3-system x 2-level suite comparison (micro/macro/gap/min_comp/pct_missed), AVG rows"
      contains: "tab:component-suite"
    - path: "writing/tables/metric_fitness.tex"
      provides: "Fitness scorecard verdict table (column x four axes + headline/diagnostic verdict)"
      contains: "tab:metric-fitness"
    - path: "src/paper/check_eval_structure.py"
      provides: "Stdlib structural validator for eval.tex (label/ref resolution + \\input existence)"
      contains: "Cref"
    - path: "writing/eval.tex"
      provides: "New Ch2 suite/tail/reconciliation section + corrected Sentence-F1 example"
      contains: "tab:component-suite"
  key_links:
    - from: "writing/eval.tex new section"
      to: "writing/tables/component_suite.tex + writing/tables/metric_fitness.tex"
      via: "\\input{tables/component_suite} + \\input{tables/metric_fitness} and \\Cref to their labels"
      pattern: "tab:component-suite|tab:metric-fitness"
    - from: "t_component_suite / t_metric_fitness"
      to: "reports/COMPONENT_SUITE_{sad-code,sad-model}.csv + reports/METRIC_FITNESS.csv"
      via: "csv.DictReader over the committed CSVs (AVG rows for the suite; verdict rows for fitness)"
      pattern: "COMPONENT_SUITE_|METRIC_FITNESS"
    - from: "src/paper/check_eval_structure.py"
      to: "writing/eval.tex + writing/tables/*.tex"
      via: "regex scan of \\label/\\ref/\\Cref/\\cref + \\input existence check"
      pattern: "label|input"
---

<objective>
Deliver CMP-05 — publish the v1.2 component suite into the paper. Three changes, all
tracing to retained artifacts:

1. **Two generated tables** (extend `src/paper/generate_tables.py`):
   - `t_component_suite()` → `writing/tables/component_suite.tex` (`tab:component-suite`):
     read both `reports/COMPONENT_SUITE_{sad-code,sad-model}.csv`, emit the AVG rows for the
     three systems at both levels with columns micro / macro / gap / min_comp / pct_missed.
   - `t_metric_fitness()` → `writing/tables/metric_fitness.tex` (`tab:metric-fitness`):
     read `reports/METRIC_FITNESS.csv`, emit per-column separation / validity / stability /
     degeneracy + the verdict (pooled, or both levels), matching METRIC_FITNESS.md.
   Register both in `main()` under the Ch2 group. Follow the `t_consequences` /
   `t_converged_framework` idiom exactly (render_table/write_table, csv.DictReader, a
   `Source:` note naming the CSV). No hardcoded metric values.

2. **New Ch2 section** in `writing/eval.tex`, appended after the converged-framework table
   (current chapter end), titled e.g. "A Level-Agnostic Component Suite and the Long-Tail
   Discriminator". It must make exactly the three claims in 09-CONTEXT D-14 (suite-as-one-
   object; universe-reconciliation correction = aggregation is second-order; tail coverage
   is the level-stable discriminator that indicts artemis), `\input` both new tables and
   `\Cref` them, and cite only retained scripts/reports. Honesty guard: do not oversell
   micro-vs-macro. Also re-anchor the stale Sentence-F1 example (D-16).

3. **Structural validator** `src/paper/check_eval_structure.py` (stdlib): assert no dangling
   refs and all `\input{tables/X}` exist; used as the SC3 gate in place of pdflatex.
</objective>

<tasks>

### Task 1 — Two table builders in generate_tables.py (CMP-05, SC2)
- Add `SUITE_CSV = {"sad-code": REPORTS/"COMPONENT_SUITE_sad-code.csv", "sad-model": REPORTS/"COMPONENT_SUITE_sad-model.csv"}` and `FITNESS_CSV = REPORTS/"METRIC_FITNESS.csv"`.
- `t_component_suite()`: for each level, pull the three AVG rows (system in {swattr/transarc, s20linker, artemis}); emit a table grouped by level (a Level column or two stacked blocks) with columns System, micro, macro, gap, min_comp, pct_missed. 4-dp from CSV → 3-dp display via the existing `%.3f` idiom. Caption + `tab:component-suite` + Source note citing both CSVs and `reports/COMPONENT_SUITE.md`.
- `t_metric_fitness()`: read METRIC_FITNESS.csv; emit per-column (micro/macro/gap/min_comp/pct_missed) the four axis scores. Use the pooled both-level view OR show both levels; include the verdict (headline/diagnostic) column. Caption + `tab:metric-fitness` + Source note citing the CSV and `reports/METRIC_FITNESS.md`.
- Register both in `main()` Ch2 group. Run `python3 src/paper/generate_tables.py`; confirm WROTE lines for the two new files and a clean run.
- **Determinism:** run the generator twice; `git diff`/`cmp` the two new `.tex` must be byte-identical.

### Task 2 — New Ch2 section + stale-prose fix in eval.tex (CMP-05, SC1)
- Insert the new section after `\input{tables/converged_framework}` (L1043), before `\end{document}`.
- Prose = the three D-14 claims, numbers consistent with the CSVs/tables:
  - Suite: one object, 5 columns, both granularities (cite `src/bias/component_suite.py`, `reports/COMPONENT_SUITE.md`).
  - Universe-reconciliation correction: swattr/transarc avg micro/macro gap −0.099→−0.019; aggregation second-order (cite `reports/COMPONENT_UNIVERSE_RECONCILIATION.md`).
  - Tail discriminator: artemis competitive on micro/link F1 (2nd) but last on worst real-component coverage (min_comp 0.345 sad-code / 0.279 sad-model), abandons real gold components (min_comp=0 on bbb+jabref both levels); SWATTR more uniform. Per fitness scorecard, headline = micro/macro/min_comp, diagnostic = gap/pct_missed (cite `reports/METRIC_FITNESS.md`). `\Cref{tab:component-suite}` + `\Cref{tab:metric-fitness}`; `\input` both.
- Re-anchor the Sentence-F1 example at ~L416-418: replace the buggy "Teammates … 0.916 … 0.206 gap" with the corrected BigBlueButton example (sentence \fone 0.876 vs link 0.793) or the AVG (0.825 vs 0.799). Verify against `writing/tables/metrics_sad-sam.tex`.

### Task 3 — Structural validator + SC3 gate (CMP-05, SC3)
- Write `src/paper/check_eval_structure.py` (stdlib re/pathlib): collect `\label{...}` and all `\ref/\Cref/\cref{...}` (handle comma lists) from `writing/eval.tex` + its `\input` files; report any reference with no matching label; check each `\input{tables/X}` resolves to a file. Exit non-zero on any failure with a readable list; exit 0 clean.
- Run it; fix any dangling ref/label introduced by the new section. Re-run generator + validator as the SC3 evidence.

</tasks>

<verification>
- SC1: new Ch2 section present, three claims made, only retained artifacts cited, numbers match the CSVs; Sentence-F1 example corrected.
- SC2: `python3 src/paper/generate_tables.py` emits component_suite.tex + metric_fitness.tex; 2nd run byte-identical.
- SC3: `python3 src/paper/check_eval_structure.py` exits 0 (no dangling refs; all \input resolve).
- Invariants: stdlib-only; no benchmark word lists; data-traced tables.
</verification>
