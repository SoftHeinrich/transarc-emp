---
phase: 08-multi-system-comparison-fitness-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - results_artemis_gpt54/
  - reports/ARTEMIS_PROVENANCE.md
autonomous: true
requirements: [CMP-03]
must_haves:
  truths:
    - "results_artemis_gpt54/ (ARTEMIS_LOCAL canonical sad-code links source) is committed to git so a suite re-run is bit-for-bit deterministic on the artemis sad-code rows."
    - "A reports/ARTEMIS_PROVENANCE.md states which artemis directory is authoritative (results_artemis_gpt54/, read by component_suite._artemis_model at sad-code) and records that reports_artemis_gpt54_* and paper-result/ are secondary/superseded, never read by the suite."
    - "component_suite._artemis_model resolves its links from the now-committed ARTEMIS_LOCAL path for all 5 projects (no untracked dependency for the canonical sad-code source)."
    - "git status no longer reports results_artemis_gpt54/ as untracked."
  artifacts:
    - path: "results_artemis_gpt54"
      provides: "Canonical artemis sad-code links (sadSamTlr_<proj>.csv per project) read by _artemis_model"
      contains: "sad-code"
    - path: "reports/ARTEMIS_PROVENANCE.md"
      provides: "Authoritative-source ledger for the three artemis dirs (D-10)"
      contains: "results_artemis_gpt54"
  key_links:
    - from: "component_suite._artemis_model (ARTEMIS_LOCAL)"
      to: "results_artemis_gpt54/<proj>/sad-code/sadSamTlr_<proj>.csv"
      via: "committed canonical sad-code links source"
      pattern: "ARTEMIS_LOCAL|results_artemis_gpt54"
    - from: "reports/ARTEMIS_PROVENANCE.md"
      to: "reports_artemis_gpt54_* and paper-result/"
      via: "superseded/secondary notice (non-destructive)"
      pattern: "supersed|secondary|authoritative"
---

<objective>
Resolve the artemis-provenance ambiguity (D-10) so every downstream determinism/regen
claim in Phase 8 can hold. Today three artemis-related directories are UNTRACKED:
`results_artemis_gpt54/` (the canonical `ARTEMIS_LOCAL` sad-code links source that
`component_suite._artemis_model` actually reads), `reports_artemis_gpt54_20260605_030524/`
(a derived reports/tables snapshot), and `paper-result/` (a pre-computed metrics summary
`artemis-gpt5.csv` + `s20U.csv`). Because the canonical links source is untracked, a fresh
clone cannot reproduce the artemis sad-code rows — the comparison (08-02) and the scorecard
(08-03) cannot honestly assert determinism until this is pinned.

This plan:
  (1) COMMITS `results_artemis_gpt54/` (the canonical `ARTEMIS_LOCAL` source, ~1.9M, 25
      files: `<proj>/sad-code/sadSamTlr_<proj>.csv` + `<proj>/sad-sam/...` for all 5
      projects) so the suite's artemis sad-code rows become reproducible.
  (2) WRITES `reports/ARTEMIS_PROVENANCE.md` — an authoritative-source ledger stating that
      `results_artemis_gpt54/` is canonical (named by `_artemis_model` via `ARTEMIS_LOCAL`),
      that the sad-code doc-code side stays the external `ARTEMIS_DOC_CODE`
      (`sota/recovered-links/doc-code/artemis-<proj>-gpt-5.4.csv`, NOT vendored), and that
      `reports_artemis_gpt54_*` and `paper-result/` are SECONDARY/SUPERSEDED snapshots the
      suite never reads (non-destructive — documented, not deleted).

Purpose: this is the reproducibility prerequisite for the whole phase — it pins the only
artemis input under version control so 08-02/08-03 regen claims are deterministic.
Output: committed canonical source + the provenance ledger.

Discovery level: 0 (no new code; commits existing data + writes one markdown ledger; no
new dependencies, no F1 math, no script changes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-multi-system-comparison-fitness-validation/08-CONTEXT.md
@.planning/milestones/v1.2-ROADMAP.md
@.planning/milestones/v1.2-REQUIREMENTS.md
@CLAUDE.md

This plan is Wave 1 — it has no plan dependencies and unblocks 08-02 and 08-03 (both rely
on the artemis source being pinned for their determinism claims).

Project rules enforced: stdlib-only (no code added here at all); non-destructive (the two
secondary artemis dirs are DOCUMENTED as superseded/secondary, never deleted — CLAUDE.md);
no benchmark-derived word lists (none introduced); reports are generated markdown kept
consistent with reality.

<interfaces>
<!-- The exact paths + the suite site that consumes the canonical source. -->
src/bias/component_suite.py (DO NOT EDIT in this plan — read for the path contract):
  ARTEMIS_LOCAL    = <repo>/results_artemis_gpt54        # canonical sad-code source (PIN THIS)
  ARTEMIS_DOC_CODE = /mnt/hostshare/ardoco-home/sota/recovered-links/doc-code   # external; stays unvendored
  _artemis_model(p): reads  ARTEMIS_LOCAL / p / "sad-code" / f"sadSamTlr_{p}.csv"
                     keys: a=(modelElementID, component_id), b=(sentence)
  _artemis_code(p):  reads  ARTEMIS_DOC_CODE / f"artemis-{p}-gpt-5.4.csv"  (external root)

Directory facts confirmed on disk:
  results_artemis_gpt54/                 -> 5 project dirs; each has sad-code/ + sad-sam/
                                            sadSamTlr_<proj>.csv  (~1.9M total, 25 files) — CANONICAL
  reports_artemis_gpt54_20260605_030524/ -> reports/ + tables/ derived snapshot (~576K) — SECONDARY
  paper-result/                          -> artemis-gpt5.csv (precomputed link_p/r/f1 summary)
                                            + s20U.csv — SUPERSEDED summary, NOT a links source

PROJECTS (from transarc_error_analysis): mediastore, teastore, teammates, bigbluebutton, jabref.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the canonical artemis source (results_artemis_gpt54/) under version control</name>
  <files>results_artemis_gpt54/</files>
  <read_first>
    - src/bias/component_suite.py (lines ~84 ARTEMIS_LOCAL and ~144-152 _artemis_model/_artemis_code — confirm _artemis_model reads ARTEMIS_LOCAL / p / "sad-code" / f"sadSamTlr_{p}.csv"; this is WHY results_artemis_gpt54/ is the canonical pin and the doc-code external root is NOT vendored)
    - .planning/phases/08-multi-system-comparison-fitness-validation/08-CONTEXT.md (D-10 — pin results_artemis_gpt54 as canonical ARTEMIS_LOCAL; resolve the untracked dirs so a re-run is deterministic; doc-code stays ARTEMIS_DOC_CODE)
    - CLAUDE.md (non-destructive convention; phase commits its own artifacts atomically)
  </read_first>
  <action>
    `git add results_artemis_gpt54/` to bring the canonical `ARTEMIS_LOCAL` source under
    version control (the directory `component_suite._artemis_model` reads its sad-code links
    from). Do NOT add `reports_artemis_gpt54_20260605_030524/` or `paper-result/` to git in
    this task — those are handled as documentation-only in Task 2 (they are derived/secondary
    and need not be vendored). Do NOT vendor the external `ARTEMIS_DOC_CODE`
    (`sota/recovered-links/doc-code/`) — it stays an external guarded root per D-10 and the
    skip-with-notice policy. Confirm all 5 projects' sad-code link files
    (`results_artemis_gpt54/<proj>/sad-code/sadSamTlr_<proj>.csv` for mediastore, teastore,
    teammates, bigbluebutton, jabref) are now staged. Do not modify any file content — this
    is a pure version-control pin of existing data.
  </action>
  <acceptance_criteria>
    - `git ls-files results_artemis_gpt54/ | wc -l` returns the full file count (was 0 before;
      now matches the 25 files on disk, including all 5 `sad-code/sadSamTlr_<proj>.csv`).
    - All 5 projects' canonical sad-code link files are tracked: `git ls-files
      "results_artemis_gpt54/*/sad-code/sadSamTlr_*.csv" | wc -l` returns 5.
    - `git status --short results_artemis_gpt54/` shows staged (A) entries, NOT `??` untracked.
    - No file content changed — only version-control state (the data is committed as-is).
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && git add results_artemis_gpt54/ && test "$(git ls-files 'results_artemis_gpt54/*/sad-code/sadSamTlr_*.csv' | wc -l)" = "5" && test -z "$(git status --short results_artemis_gpt54/ | grep '^??')" && echo ARTEMIS_LOCAL_PINNED</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Write reports/ARTEMIS_PROVENANCE.md (authoritative-source ledger, non-destructive)</name>
  <files>reports/ARTEMIS_PROVENANCE.md</files>
  <read_first>
    - src/bias/component_suite.py (ARTEMIS_LOCAL vs ARTEMIS_DOC_CODE constants + _artemis_model / _artemis_code — name exactly which dir each reads, so the ledger is accurate to the code)
    - reports/COMPONENT_SUITE.md (the Caveats/open-items section + the "External result roots ... absent systems are skipped" note — keep the provenance ledger consistent with that narrative)
    - .planning/phases/08-multi-system-comparison-fitness-validation/08-CONTEXT.md (D-10 — commit the canonical one and/or document which is authoritative; D-08 skip-with-notice; D-12 swattr≡transarc)
    - CLAUDE.md (non-destructive: supersede via banner/note, never delete)
  </read_first>
  <action>
    Create `reports/ARTEMIS_PROVENANCE.md` as a short generated-doc ledger with three clearly
    labeled entries: (a) AUTHORITATIVE — `results_artemis_gpt54/`, the `ARTEMIS_LOCAL` path
    that `component_suite._artemis_model` reads (`<proj>/sad-code/sadSamTlr_<proj>.csv`), now
    committed (reference Task 1); state it is the canonical artemis sad-code links source for
    the suite and scorecard. (b) EXTERNAL (not vendored) — the sad-code doc-code side stays
    `ARTEMIS_DOC_CODE` = `sota/recovered-links/doc-code/artemis-<proj>-gpt-5.4.csv`, a guarded
    external root that degrades via the standardized `WARNING:` skip-with-notice if absent
    (D-08). (c) SECONDARY / SUPERSEDED — `reports_artemis_gpt54_20260605_030524/` (a derived
    reports/tables snapshot) and `paper-result/artemis-gpt5.csv` + `paper-result/s20U.csv` (a
    pre-computed link_p/r/f1 summary, NOT a links source); state explicitly that the suite
    NEVER reads these, that they are retained for history but are not authoritative, and that
    they are documented-as-secondary rather than deleted (non-destructive). Note the
    swattr≡transarc one-column convention (D-12) only insofar as it clarifies artemis is the
    LLM contrast. Keep it factual and consistent with what the code actually reads — do not
    invent paths. End with a one-line "Reproduce" pointer (`python3 src/bias/component_suite.py`
    reads the canonical source).
  </action>
  <acceptance_criteria>
    - `test -f reports/ARTEMIS_PROVENANCE.md` and the file is non-empty.
    - The ledger names the canonical source: `grep -q "results_artemis_gpt54" reports/ARTEMIS_PROVENANCE.md`.
    - The ledger marks the other two dirs as secondary/superseded:
      `grep -nE "reports_artemis_gpt54|paper-result" reports/ARTEMIS_PROVENANCE.md` matches AND
      `grep -niE "supersed|secondary|not.*read|authoritative" reports/ARTEMIS_PROVENANCE.md` matches.
    - The ledger records the external doc-code root stays unvendored:
      `grep -niE "ARTEMIS_DOC_CODE|recovered-links|external" reports/ARTEMIS_PROVENANCE.md` matches.
    - Non-destructive: the two secondary dirs still exist on disk (`test -d
      reports_artemis_gpt54_20260605_030524 && test -d paper-result`) — documented, not deleted.
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && test -s reports/ARTEMIS_PROVENANCE.md && grep -q "results_artemis_gpt54" reports/ARTEMIS_PROVENANCE.md && grep -nE "reports_artemis_gpt54|paper-result" reports/ARTEMIS_PROVENANCE.md && grep -niE "supersed|secondary|authoritative" reports/ARTEMIS_PROVENANCE.md && test -d reports_artemis_gpt54_20260605_030524 && test -d paper-result && echo PROVENANCE_LEDGER_OK</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untracked data → version control | The canonical artemis links source is untracked; a determinism claim built on it is unreproducible until pinned. |
| canonical vs secondary artemis dirs | Three artemis dirs coexist; ambiguity about which the suite reads can let a stale/secondary source silently feed the comparison. |
| filesystem → ledger | The provenance ledger must describe what the code actually reads; a wrong path claim would mislead reproduction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | unreproducible artemis source (untracked) | mitigate | `git add results_artemis_gpt54/`; acceptance asserts all 5 sad-code link files tracked and no `??` remains. |
| T-08-01-02 | Repudiation | ambiguous "which artemis dir is authoritative" | mitigate | ARTEMIS_PROVENANCE.md names the canonical source (ARTEMIS_LOCAL) and marks the other two secondary/superseded; ledger cross-checked against the code's constants. |
| T-08-01-03 | Tampering | deleting the secondary dirs (data loss) | mitigate | Non-destructive: secondary dirs documented, NOT deleted; acceptance asserts both still exist on disk. |
| T-08-01-SC | Tampering | dependency supply chain | accept | stdlib-only, no installs, no code added; not applicable. |
</threat_model>

<verification>
- `results_artemis_gpt54/` is tracked (git ls-files non-zero; all 5 sad-code link files
  tracked); `git status` no longer reports it untracked.
- `reports/ARTEMIS_PROVENANCE.md` exists, names `results_artemis_gpt54/` as canonical
  (ARTEMIS_LOCAL), records the external doc-code root stays unvendored, and marks
  `reports_artemis_gpt54_*` + `paper-result/` as secondary/superseded.
- The two secondary dirs remain on disk (non-destructive).
- No source code changed; no new dependencies.
</verification>

<success_criteria>
CMP-03 (provenance prerequisite) satisfied: the canonical artemis links source is pinned
under version control and a written ledger states which artemis directory is authoritative,
so the Phase-8 comparison (08-02) and scorecard (08-03) can assert deterministic regen.
Maps to ROADMAP Phase 8 SC1 ("graceful skip for absent external roots") foundation —
present-system reproducibility — and discharges 08-CONTEXT D-10 (pin `results_artemis_gpt54`,
resolve the untracked dirs).
</success_criteria>

<output>
Create `.planning/phases/08-multi-system-comparison-fitness-validation/08-01-SUMMARY.md` when done.
</output>
