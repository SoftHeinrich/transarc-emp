---
phase: 07-suite-universe-reconciliation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bias/evaluation_critique.py
  - mini-src/metrics.py
  - reports/metrics_sad-code.csv
  - writing/tables/metrics_sad-code.tex
  - writing/tables/consequences.tex
  - writing/tables/converged_framework.tex
  - reports/SADCODE_COMPARISON.md
  - reports/SADCODE_S11_S13F_VS_TRANSARC.csv
  - reports/rq1/lissa_sad-code.csv
  - reports/rq1/transarc_sad-code.csv
  - reports/rq1/artemis_sad-code.csv
  - reports/COMPONENT_UNIVERSE_RECONCILIATION.md
  - reports/EVALUATION_CRITIQUE.md
  - reports/RQ2_METRIC_REDUNDANCY.md
  - reports/CONSEQUENCES_STUDY.md
  - reports/EXTREME_BASELINES.md
  - reports/HOLISTIC_METRICS.md
autonomous: true
requirements: [CMP-02]
must_haves:
  truths:
    - "metrics_api headline component_f1 is the mapped-only number (the {b} fallback is gone)."
    - "Suite micro and metrics_api component_f1 agree for swattr/transarc at sad-code (reconciled by construction)."
    - "All in-repo artifacts carrying the old component_f1 are either regenerated or banner-marked superseded."
    - "The in-repo LaTeX tables that pull sad-code component_f1 from metrics_sad-code.csv (consequences.tex, converged_framework.tex) are regenerated off the old number via generate_tables.py."
    - "Every in-repo report/table that still shows the old component number (0.732 or 0.714) is accounted for as regenerated, banner-marked, or ledger-classified; only writing/eval.tex prose is deferred to Phase 9."
    - "mini-src/check.py still passes against the new definition."
    - "The numeric old→new component_f1 delta is recorded in a reports/ artifact, and the external alinker-paper repo is flagged (not touched) for Phase 9."
  artifacts:
    - path: "src/bias/evaluation_critique.py"
      provides: "_compute_component_f1 with NO {b} singleton fallback (mapped-only)"
      contains: "def _compute_component_f1"
    - path: "reports/metrics_sad-code.csv"
      provides: "Regenerated headline row with mapped-only component_f1"
    - path: "writing/tables/metrics_sad-code.tex"
      provides: "Regenerated LaTeX metrics table (D-03 pulled forward)"
    - path: "writing/tables/consequences.tex"
      provides: "Regenerated headline-vs-honest table; sad-code Component F1 = mapped-only (no 0.714)"
    - path: "writing/tables/converged_framework.tex"
      provides: "Regenerated converged Decision+Component table; sad-code Component F1 = mapped-only (no 0.714)"
    - path: "reports/SADCODE_S11_S13F_VS_TRANSARC.csv"
      provides: "Regenerated multi-system component_f1 comparison"
    - path: "reports/COMPONENT_UNIVERSE_RECONCILIATION.md"
      provides: "Old→new delta record + supersession ledger + Phase-9 external-repo flag"
      contains: "superseded by mapped-only universe (v1.2)"
  key_links:
    - from: "metrics_api.compute_sad_code_metrics"
      to: "_compute_component_f1 (mapped-only)"
      via: "row['component_f1'] now drops unmapped files"
      pattern: "_compute_component_f1"
    - from: "src/paper/generate_tables.py (t_consequences/t_converged_framework)"
      to: "reports/metrics_sad-code.csv component_f1"
      via: "tables re-read the regenerated CSV → consequences.tex / converged_framework.tex"
      pattern: "component_f1"
    - from: "mini-src/metrics.py component_f1"
      to: "mapped-only definition"
      via: "no per-file singleton fallback (keeps mini-src/check.py green)"
      pattern: "component_f1"
---

<objective>
Make the mapped-only component universe the single source of truth for the paper's
headline `component_f1` (CMP-02). Rewrite `_compute_component_f1` to drop the `{b}`
singleton fallback (D-01) so files with no SAM-CODE mapping are dropped exactly as the
suite's `_code_inputs` collapse does; regenerate every in-repo artifact carrying the
superseded sad-code number (D-03) — INCLUDING the committed `writing/tables/*.tex` via
`src/paper/generate_tables.py`; banner-mark anything not regenerated as superseded and
flag the EXTERNAL alinker-paper repo plus the in-repo `writing/eval.tex` prose for Phase 9
without touching them (D-04); and record the measured old→new delta in a `reports/`
artifact (D-02 framing: one definition + one bundled input = one set of numbers — no
source archaeology).

Purpose: After this, the suite and `metrics_api` agree on `component_f1` by construction,
and the repo carries no stale headline number in any regenerable artifact (CSV, MD, or
LaTeX table). Output: rewritten function; regenerated CSVs/MD/LaTeX; reconciliation/delta
artifact; superseded banners + a complete supersession ledger; updated mini-src so its
committed equivalence check stays green.

Discovery level: 0 (stdlib-only; reuses existing generators + `calc_metrics`; no new
dependencies).

NOTE — discovered ripple (surfaced to orchestrator): `mini-src/metrics.py` carries an
independent copy of the MICRO `component_f1` with the same `{b}` fallback, and
`mini-src/check.py` asserts it equals `metrics_api` to 1e-9. D-01 changes the canonical
definition, so mini-src MUST follow or that committed check regresses. Task 1 updates
both implementations together; this is consistency maintenance required by D-01, not new
scope.

NOTE — LaTeX-table ripple (D-03 pulled forward): `src/paper/generate_tables.py`
(`t_consequences`, `t_converged_framework`) reads sad-code `component_f1` straight from
`reports/metrics_sad-code.csv`, so `writing/tables/consequences.tex` and
`writing/tables/converged_framework.tex` carry the old `0.714` and WILL update if the
generator is re-run AFTER the CSV is regenerated. Task 2 runs `generate_tables.py` in that
order. `writing/tables/s12c_four_level.tex` is sourced from the INDEPENDENT
`reports/S12C_VS_TRANSARC.csv` pipeline (whose own `compute_component` does not flow from
`_compute_component_f1`) so it does NOT move — Task 3 ledger-classifies it (see below).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-suite-universe-reconciliation/07-CONTEXT.md
@.planning/milestones/v1.2-ROADMAP.md
@.planning/milestones/v1.2-REQUIREMENTS.md
@CLAUDE.md

Project rules enforced in EVERY task: stdlib-only; `calc_metrics` is the only F1
primitive; no benchmark-derived word lists; NON-DESTRUCTIVE — supersede with the exact
banner `superseded by mapped-only universe (v1.2)`, never delete; preserve the depth-2 +
`sys.path.insert` import pattern. The canonical input is the bundled in-repo `results/`
tree (D-02) — do NOT switch result sources.

<interfaces>
<!-- Extracted from the reconciliation surface — executor uses these directly. -->
src/bias/evaluation_critique.py:631
  _compute_component_f1(enrolled, result, file_to_comps) -> dict{..., "f1": ...}
    CURRENT: for (s,c) in enrolled/result -> add (s,comp) per mapped comp;
             `if not comps: gold_comp.add((s,c))`  <-- the {b} fallback to REMOVE
    AFTER D-01: drop files with no mapped comp from BOTH gold_comp and result_comp.
  Called by src/lib/metrics_api.py:395 (compute_sad_code_metrics -> row["component_f1"]).
src/lib/metrics_api.py:
  compute_sad_code_metrics(proj, res) -> row dict; res is set[(sentence, code_path)];
    row["component_f1"] = _compute_component_f1(enrolled, res, file_to_comps)["f1"].
  CLI: `python3 src/lib/metrics_api.py --task sad-code`
  -> writes reports/metrics_sad-code.csv (write_csv) AND writing/tables/metrics_sad-code.tex
     (write_latex; default tables-dir = committed writing/tables when no override).
src/transarc/sadcode_comparison.py main() (imports compute_sad_code_metrics)
  -> reports/SADCODE_S11_S13F_VS_TRANSARC.csv + reports/SADCODE_COMPARISON.md.
src/paper/rq1_panels.py main() -> writes reports/rq1/{system}_{task}.csv for EVERY system,
  so it regenerates reports/rq1/lissa_sad-code.csv AND reports/rq1/transarc_sad-code.csv
  (component_f1 AVG currently 0.7143) AND reports/rq1/artemis_sad-code.csv (each carries a
  component_f1 column).
src/paper/generate_tables.py main() -> rewrites all 13 booktabs tables. The two that carry
  sad-code component_f1 from reports/metrics_sad-code.csv are t_consequences ->
  writing/tables/consequences.tex and t_converged_framework -> writing/tables/converged_framework.tex
  (both currently show 0.714). t_s12c_four_level -> writing/tables/s12c_four_level.tex reads
  reports/S12C_VS_TRANSARC.csv (separate s12c_sadcode_comparison.py pipeline; will NOT move).
src/bias/component_suite.py: suite micro for swattr/transarc at sad-code at project p is
  cs.component_suite(gold, collapse(cs._swattr_code(p)))['micro'] where (gold, collapse) =
  cs._code_inputs(p); this equals the reconciled metrics_api component_f1 (by construction).
Old committed numbers: reports/metrics_sad-code.csv component_f1 AVG = 0.732 (old def);
  consequences.tex / converged_framework.tex sad-code Component F1 AVG = 0.714;
  ROADMAP cites ~0.795 vs ~0.714 for swattr/transarc as the expected mapped-only shift.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drop the {b} fallback in both component_f1 implementations; cross-check against the suite</name>
  <files>src/bias/evaluation_critique.py, mini-src/metrics.py</files>
  <read_first>
    - src/bias/evaluation_critique.py (lines ~631-652, `_compute_component_f1`)
    - src/lib/metrics_api.py (line ~395, the call site; confirm nothing else depends on the fallback)
    - mini-src/metrics.py (its `component_f1` collapse — the comp helper ~line 207 and the comp_f1 computation ~line 284) and mini-src/check.py (the PANEL["sad-code"] component_f1 assertion)
    - src/bias/component_suite.py (`_code_inputs` ~208-232 — the mapped-only collapse rule to mirror)
    - .planning/phases/07-suite-universe-reconciliation/07-CONTEXT.md (D-01, D-02)
  </read_first>
  <behavior>
    - A (sentence, file) pair whose file maps to >=1 SAM-CODE component contributes one (sentence, component) per mapped component (unchanged).
    - A (sentence, file) pair whose file maps to NO component contributes NOTHING (the {b} singleton `(s, c)` is no longer added) — same drop rule for gold and result.
    - metrics_api `component_f1` for swattr/transarc sad-code rises from the old ~0.71–0.73 to the mapped-only value (~0.79–0.80 band), matching `component_suite` micro.
    - mini-src `component_f1` produces the SAME mapped-only number, so mini-src/check.py still asserts mini==canon to 1e-9.
  </behavior>
  <action>
    In `evaluation_critique._compute_component_f1`, remove BOTH `if not comps:
    gold_comp.add((s, c))` and `result_comp.add((s, c))` fallback branches so only files
    with at least one mapped component contribute to `gold_comp` and `result_comp`; keep
    the final `calc_metrics(gold_comp, result_comp)` call and the returned dict shape
    unchanged. This makes the `metrics_api` row `component_f1` the mapped-only micro
    number, aligning it with `component_suite` micro by construction (D-01). Apply the
    IDENTICAL drop rule to the `component_f1` computation in `mini-src/metrics.py` (remove
    its per-file singleton fallback so its mapped-only collapse matches the canonical
    function) so the committed equivalence check `python3 mini-src/check.py` keeps passing
    on the new definition — this is the discovered D-01 ripple. Do NOT alter
    `rq2_trivial_baselines.per_component_macro_f1` (it already drops unmapped files and is
    the 07-03 oracle target) or any other independent macro implementation. Add no new F1
    math; the only primitive remains `calc_metrics`.
  </action>
  <acceptance_criteria>
    - `grep -nE "if not comps" src/bias/evaluation_critique.py` returns NOTHING inside
      `_compute_component_f1` (the `{b}` fallback branches are gone).
    - `python3 mini-src/check.py` exits 0 and prints the PASS line — mini and canon agree
      on `component_f1` (and all panel cells) to 1e-9 under the new definition.
    - metrics_api headline shifts: scoring swattr/transarc sad-code via
      `metrics_api.compute_sad_code_metrics` yields a per-project `component_f1` equal to
      `component_suite` micro for the same project to within 1e-6 (reconciled by
      construction) — ENFORCED by the automated assertion in `<verify>`, not just by eye;
      the cross-project average is in the ~0.79–0.80 band, NOT ~0.714/0.732.
    - `rq2_trivial_baselines.per_component_macro_f1` is unchanged (`git diff
      src/bias/rq2_trivial_baselines.py` empty).
    - stdlib-only and no new F1 helper added in either edited file.
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && ! grep -nE "if not comps" src/bias/evaluation_critique.py && python3 mini-src/check.py >/dev/null && python3 -c "
import sys
sys.path.insert(0, 'src/bias'); sys.path.insert(0, 'src/lib')
import component_suite as cs, metrics_api as ma
for p in cs.PROJECTS:
    gold, collapse = cs._code_inputs(p)
    res = cs._swattr_code(p)
    micro = cs.component_suite(gold, collapse(res))['micro']
    cf1 = ma.compute_sad_code_metrics(p, res)['component_f1']
    assert abs(micro - cf1) < 1e-6, (p, micro, cf1)
" && echo DEF_MINI_RECON_OK</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Regenerate every in-repo artifact that flows from the changed definition (CSV, MD, AND LaTeX tables)</name>
  <files>reports/metrics_sad-code.csv, writing/tables/metrics_sad-code.tex, writing/tables/consequences.tex, writing/tables/converged_framework.tex, reports/SADCODE_COMPARISON.md, reports/SADCODE_S11_S13F_VS_TRANSARC.csv, reports/rq1/lissa_sad-code.csv, reports/rq1/transarc_sad-code.csv, reports/rq1/artemis_sad-code.csv</files>
  <read_first>
    - src/lib/metrics_api.py (main() — confirms `--task sad-code` writes both the CSV and the committed .tex)
    - src/transarc/sadcode_comparison.py (main() + OUTPUT_CSV/OUTPUT_MD — it calls compute_sad_code_metrics)
    - src/paper/rq1_panels.py (main() — writes reports/rq1/{system}_{task}.csv for EVERY system, so lissa_sad-code.csv AND transarc_sad-code.csv AND artemis_sad-code.csv all regenerate; confirm whether each component_f1 flows from the changed def or an independent path)
    - src/paper/generate_tables.py (t_consequences -> consequences.tex and t_converged_framework -> converged_framework.tex BOTH read sad-code component_f1 from reports/metrics_sad-code.csv via _load_metrics; t_s12c_four_level reads reports/S12C_VS_TRANSARC.csv — a SEPARATE pipeline that will NOT move)
    - reports/metrics_sad-code.csv, reports/SADCODE_S11_S13F_VS_TRANSARC.csv, reports/rq1/lissa_sad-code.csv, reports/rq1/transarc_sad-code.csv, reports/rq1/artemis_sad-code.csv, writing/tables/consequences.tex, writing/tables/converged_framework.tex (current committed numbers, to diff against)
    - .planning/phases/07-suite-universe-reconciliation/07-CONTEXT.md (D-03)
  </read_first>
  <action>
    Re-run the generators that consume `_compute_component_f1` /
    `compute_sad_code_metrics` so their sad-code `component_f1` becomes the mapped-only
    number (D-03), IN THIS ORDER:
    (1) `python3 src/lib/metrics_api.py --task sad-code` (regenerates
    `reports/metrics_sad-code.csv` AND `writing/tables/metrics_sad-code.tex` in one run —
    confirm the default tables-dir targets the committed `writing/tables`, else pass
    `--tables-dir writing/tables`);
    (2) `python3 src/transarc/sadcode_comparison.py`
    (regenerates `reports/SADCODE_S11_S13F_VS_TRANSARC.csv` and
    `reports/SADCODE_COMPARISON.md`);
    (3) `python3 src/paper/rq1_panels.py` (regenerates the rq1 CSVs — note its `main()`
    writes EVERY system, so `reports/rq1/lissa_sad-code.csv`, `reports/rq1/transarc_sad-code.csv`,
    and `reports/rq1/artemis_sad-code.csv` all rewrite; confirm each sad-code component_f1
    moved off the old number);
    (4) AFTER step (1) has rewritten `reports/metrics_sad-code.csv`, run
    `python3 src/paper/generate_tables.py` so `t_consequences` and `t_converged_framework`
    re-read the regenerated CSV and emit `writing/tables/consequences.tex` and
    `writing/tables/converged_framework.tex` with the mapped-only sad-code Component F1
    (the old `0.714` disappears). generate_tables.py also rewrites
    `writing/tables/s12c_four_level.tex`, but from the UNCHANGED independent
    `reports/S12C_VS_TRANSARC.csv` pipeline (whose own `compute_component` does not flow
    from `_compute_component_f1`), so it stays at `0.714` and content is byte-identical —
    do NOT in-file-banner it (the next generate_tables.py run would clobber the banner);
    route s12c_four_level.tex + S12C_VS_TRANSARC.csv to Task 3's supersession LEDGER
    instead.
    For each regenerated artifact, confirm the sad-code `component_f1` value moved off the
    old number. If any generator computes `component_f1` by an independent path that does
    NOT flow from the changed definition (i.e. re-running does not move the number), do NOT
    force it — instead route that file to Task 3's supersession ledger + banner/ledger. Use
    only the bundled in-repo `results/` tree as input (D-02); do not pass an alternate
    `--results-dir`. After regeneration, a second run of each generator must leave its
    output byte-identical (determinism).
  </action>
  <acceptance_criteria>
    - `reports/metrics_sad-code.csv` `Average` row `component_f1` is NO LONGER `0.732`
      and reflects the mapped-only definition.
    - `writing/tables/metrics_sad-code.tex` `Average` row `Component \fone` matches the
      regenerated `reports/metrics_sad-code.csv` value (table and CSV agree).
    - `writing/tables/consequences.tex` and `writing/tables/converged_framework.tex` NO
      LONGER contain `0.714` (the sad-code Component F1 cells reflect the mapped-only
      definition, sourced from the regenerated `reports/metrics_sad-code.csv`).
    - `reports/SADCODE_S11_S13F_VS_TRANSARC.csv` regenerates and its
      `transarc_component_f1` column reflects the new definition;
      `reports/SADCODE_COMPARISON.md` regenerates consistently.
    - `reports/rq1/lissa_sad-code.csv`, `reports/rq1/transarc_sad-code.csv`, and
      `reports/rq1/artemis_sad-code.csv` are each regenerated (component_f1 moved off the
      old number) OR — if a file's component_f1 does not flow from the changed def —
      explicitly handed to Task 3's ledger with the superseded banner (recorded in the
      SUMMARY). `reports/rq1/transarc_sad-code.csv` specifically must no longer show the
      old AVG `0.7143`.
    - Re-running each generator a second time leaves outputs byte-identical
      (`git diff --stat` clean after the authoritative regen+commit).
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && python3 src/lib/metrics_api.py --task sad-code >/dev/null 2>&1 && python3 src/transarc/sadcode_comparison.py >/dev/null 2>&1 && python3 src/paper/rq1_panels.py >/dev/null 2>&1 && python3 src/paper/generate_tables.py >/dev/null 2>&1 && ! grep -qE "^Average,.*,0\.732," reports/metrics_sad-code.csv && ! grep -q "0.714" writing/tables/consequences.tex && ! grep -q "0.714" writing/tables/converged_framework.tex && echo REGEN_OK</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Discover, record the old→new delta, and account for EVERY stale-number file (regenerate / banner / ledger)</name>
  <files>reports/COMPONENT_UNIVERSE_RECONCILIATION.md, reports/EVALUATION_CRITIQUE.md, reports/RQ2_METRIC_REDUNDANCY.md, reports/CONSEQUENCES_STUDY.md, reports/EXTREME_BASELINES.md, reports/HOLISTIC_METRICS.md</files>
  <read_first>
    - reports/metrics_sad-code.csv and reports/COMPONENT_SUITE_sad-code.csv (the new mapped-only numbers, for the delta)
    - reports/EVALUATION_CRITIQUE.md, reports/RQ2_METRIC_REDUNDANCY.md, reports/CONSEQUENCES_STUDY.md, reports/EXTREME_BASELINES.md, reports/HOLISTIC_METRICS.md (markdown reports that cite the old component_f1 / TransArc 0.732 numbers and are NOT regenerated by Task 2)
    - writing/tables/s12c_four_level.tex + reports/S12C_VS_TRANSARC.csv (the independent S12C pipeline — its component number does NOT flow from `_compute_component_f1`, so it is ledger-classified as superseded, NOT regenerated and NOT in-file-bannered)
    - .planning/phases/07-suite-universe-reconciliation/07-CONTEXT.md (D-02, D-04) and CLAUDE.md (non-destructive archival rule)
  </read_first>
  <action>
    DISCOVERY FIRST: run `grep -rlE "0\.732|0\.714" reports/ writing/` (and, where
    feasible, also grep the per-project old sad-code values such as
    0.642/0.828/0.648/0.574/0.880) and enumerate EVERY hit into the supersession ledger.
    Classify each hit as one of:
      - REGENERATED — a Task-2 generator (or 07-01) already rewrote it to the new value
        (e.g. reports/metrics_sad-code.csv, writing/tables/metrics_sad-code.tex,
        writing/tables/consequences.tex, writing/tables/converged_framework.tex,
        reports/SADCODE_S11_S13F_VS_TRANSARC.csv, reports/SADCODE_COMPARISON.md, the rq1
        sad-code CSVs, and reports/COMPONENT_SUITE.md which 07-01 regenerates — do NOT edit
        the 07-01-owned file here to avoid cross-plan overlap, just record it).
      - SUPERSEDED (banner) — an in-repo MARKDOWN report carrying the genuine stale sad-code
        component / TransArc number, NOT regenerated by any generator: at minimum
        reports/EVALUATION_CRITIQUE.md, reports/RQ2_METRIC_REDUNDANCY.md,
        reports/CONSEQUENCES_STUDY.md (0.714), and reports/EXTREME_BASELINES.md (TransArc
        0.732); add reports/HOLISTIC_METRICS.md too IF its 0.714/0.732 is the sad-code
        component number (else classify it UNRELATED-COINCIDENTAL, see below). Prepend a
        one-line banner containing the EXACT phrase `superseded by mapped-only universe
        (v1.2)` pointing to this reconciliation file — never delete content (CLAUDE.md).
      - SUPERSEDED (ledger-only) — a regenerable artifact where an in-file banner is not
        viable: handle `.tex` via regen-or-ledger (a generator overwrites it, so banner
        would be clobbered) and `.csv` via ledger-only (a CSV has no safe comment channel;
        add a superseded note only where a header/comment line is possible, never corrupt
        the data — non-destructive per D-04/CLAUDE.md). Resolve `writing/tables/s12c_four_level.tex`
        and `reports/S12C_VS_TRANSARC.csv` HERE: their `component_f1` flows from the
        INDEPENDENT `s12c_sadcode_comparison.py` `compute_component` (NOT from
        `_compute_component_f1`/`metrics_api`), so they are NOT regenerated to the new value
        — record both in the ledger as SUPERSEDED (old-universe component def via a separate
        pipeline), and note s12c_four_level.tex is rewritten byte-identical by
        generate_tables.py so it must stay ledger-only (no in-file banner).
      - UNRELATED-COINCIDENTAL — the 0.714/0.732 in this file is the GENUINE current value of
        a DIFFERENT metric/task (e.g. a sad-sam value in reports/SADSAM_*.md/.csv or
        reports/rq1/lissa-gpt4omini_sad-sam.csv) that merely shares the digits; name the
        actual metric/task in the ledger so the hit is accounted for, do NOT banner it.
      - PHASE-9-DEFERRED — `writing/eval.tex` prose legitimately belongs to Phase 9: FLAG it
        in the ledger (name it as still citing the old number) but do NOT edit it.
    Create `reports/COMPONENT_UNIVERSE_RECONCILIATION.md` recording, per D-02, that the
    `component_f1` change is a DEFINITION change (mapped-only universe; bundled in-repo
    `results/` input) — give the measured old→new delta for the headline (e.g.
    metrics_sad-code.csv `component_f1` AVG `0.732` → the regenerated mapped-only value,
    plus the swattr/transarc per-project shift cited as ~0.714→~0.795 in the ROADMAP,
    using the ACTUAL regenerated numbers). In the same file, maintain the supersession
    LEDGER: a table listing every artifact from the DISCOVERY grep with its classification
    above. Finally, FLAG (do not touch) the EXTERNAL alinker-paper repo prose for Phase 9:
    add a clearly-labeled "External — deferred to Phase 9" line in the ledger naming that
    the alinker-paper headline prose still cites the old number, alongside the in-repo
    `writing/eval.tex` Phase-9 deferral line.
  </action>
  <acceptance_criteria>
    - `grep -q "superseded by mapped-only universe (v1.2)" reports/COMPONENT_UNIVERSE_RECONCILIATION.md`
      succeeds and the file states the measured old→new `component_f1` delta with both
      numbers.
    - The banner phrase `superseded by mapped-only universe (v1.2)` is present in
      reports/EVALUATION_CRITIQUE.md, reports/RQ2_METRIC_REDUNDANCY.md,
      reports/CONSEQUENCES_STUDY.md, and reports/EXTREME_BASELINES.md (banners prepended,
      original content intact below).
    - After Task 3, every file returned by `grep -rlE "0\.732|0\.714" reports/ writing/` is
      ACCOUNTED FOR in the ledger as exactly one of {REGENERATED to the new value,
      SUPERSEDED via banner, SUPERSEDED via ledger-only entry, UNRELATED-COINCIDENTAL with
      the real metric named, PHASE-9-DEFERRED}. No un-accounted stale-number file remains,
      and `writing/eval.tex` is the only in-repo file deliberately left unedited (Phase-9
      deferral, named in the ledger).
    - `writing/tables/s12c_four_level.tex` and `reports/S12C_VS_TRANSARC.csv` appear in the
      ledger as SUPERSEDED (independent S12C pipeline), NOT regenerated and NOT in-file-bannered.
    - The reconciliation ledger lists every Task-2 artifact as REGENERATED and every
      banner-marked report as SUPERSEDED, and contains a Phase-9 deferral line naming both
      the external alinker-paper repo and the in-repo `writing/eval.tex` prose (neither
      modified in this phase).
    - No file outside this repo is modified (only in-repo paths change in this commit).
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && grep -q "superseded by mapped-only universe (v1.2)" reports/COMPONENT_UNIVERSE_RECONCILIATION.md && for f in reports/EVALUATION_CRITIQUE.md reports/RQ2_METRIC_REDUNDANCY.md reports/CONSEQUENCES_STUDY.md reports/EXTREME_BASELINES.md; do grep -q "superseded by mapped-only universe (v1.2)" "$f" || { echo "MISSING BANNER: $f"; exit 1; }; done && grep -q "s12c_four_level" reports/COMPONENT_UNIVERSE_RECONCILIATION.md && grep -q "eval.tex" reports/COMPONENT_UNIVERSE_RECONCILIATION.md && echo SUPERSEDE_OK</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → generators | Bundled gold + result CSVs read as input; integrity of the regenerated number is the concern, not confidentiality. |
| changed def → many callers | `_compute_component_f1` is shared; the edit ripples to every sad-code `component_f1` consumer (incl. mini-src and the metrics_sad-code.csv-fed LaTeX tables). |
| in-repo vs external repo | The external alinker-paper repo AND `writing/eval.tex` prose are OUT of bounds this phase — flag only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | _compute_component_f1 edit | mitigate | Keep `calc_metrics` sole primitive; AUTOMATED 1e-6 assertion gates suite-micro == metrics_api component_f1 per project; mini-src/check.py 1e-9 gate. |
| T-07-02-02 | Repudiation | stale numbers left behind in CSV/MD/TeX | mitigate | DISCOVERY grep `0.732|0.714` enumerates every hit; ledger classifies REGENERATED vs SUPERSEDED(banner) vs SUPERSEDED(ledger-only) vs UNRELATED vs PHASE-9; generate_tables.py rerun regenerates consequences.tex/converged_framework.tex; banner phrase grep-asserted. |
| T-07-02-03 | Tampering | silent deletion of old artifacts | mitigate | Non-destructive: banner-prepend / ledger-entry only, original content retained (CLAUDE.md); CSVs never have data corrupted. |
| T-07-02-04 | Elevation | accidental edit of external repo or eval.tex | accept | Plan scopes file changes to in-repo paths excluding eval.tex; external repo + eval.tex flagged for Phase 9 only. |
| T-07-02-SC | Tampering | dependency supply chain | accept | stdlib-only, no installs; no package legitimacy gate applicable. |
</threat_model>

<verification>
- `! grep -nE "if not comps" src/bias/evaluation_critique.py` (fallback removed).
- `python3 mini-src/check.py` exits 0 (mini == canon at 1e-9 on the new def).
- AUTOMATED: metrics_api component_f1 (swattr/transarc, sad-code) == suite micro to 1e-6
  for every project (Task 1 verify asserts this, not just human-checked).
- `python3 src/lib/metrics_api.py --task sad-code` + `python3 src/transarc/sadcode_comparison.py`
  + `python3 src/paper/rq1_panels.py` + (AFTER the CSV regen) `python3 src/paper/generate_tables.py`
  regenerate their sad-code artifacts; numbers move off the old values; consequences.tex
  and converged_framework.tex no longer contain `0.714`; second runs are byte-identical.
- DISCOVERY grep `0.732|0.714` over reports/ + writing/ leaves no un-accounted stale-number
  file: each is regenerated, banner-marked, ledger-only, coincidental (named), or the
  eval.tex Phase-9 deferral.
- Banner phrase present in the reconciliation file and each superseded markdown report;
  s12c_four_level.tex + S12C_VS_TRANSARC.csv ledger-classified; external repo + eval.tex
  flagged not touched.
</verification>

<success_criteria>
CMP-02 satisfied: `micro` and `macro` share one mapped-only universe AND the divergence
from the paper headline `component_f1` is RECONCILED in `metrics_api` (mapped-only is the
single source of truth), with the numeric delta recorded in
`reports/COMPONENT_UNIVERSE_RECONCILIATION.md`. Maps to ROADMAP Phase 7 SC3 (the ~0.795
vs 0.714 divergence reconciled in metrics_api with the delta recorded in a reports/
artifact). All in-repo artifacts carrying the old number — INCLUDING the
metrics_sad-code.csv-fed LaTeX tables `writing/tables/consequences.tex` and
`writing/tables/converged_framework.tex` (regenerated via generate_tables.py) — are
regenerated or banner-marked/ledger-classified; the external alinker-paper repo and the
in-repo `writing/eval.tex` prose are flagged for Phase 9.
</success_criteria>

<output>
Create `.planning/phases/07-suite-universe-reconciliation/07-02-SUMMARY.md` when done.
</output>
</output>
