---
plan: 02-01
phase: 2
wave: 1
title: "Run all 13 pillar scripts; confirm clean execution + report regeneration"
autonomous: true
depends_on: []
files_modified:
  - "reports/* (regenerated by scripts)"
requirements: [REPRO-01, REPRO-02, REPRO-03]
must_haves:
  - "every Pillar 1 script (src/transarc/*.py) exits 0"
  - "every Pillar 2 script (src/bias/*.py) exits 0"
  - "src/lib loader importable from both pillars (no ModuleNotFoundError / path failure)"
  - "each script's report artifact present in reports/ after the run"
tasks:
  - id: 1
    title: "Execute all Pillar 1 + Pillar 2 scripts, capture exit codes"
    type: execute
    autonomous: true
    read_first:
      - ".planning/phases/02-verify/02-CONTEXT.md"
    action: |
      From repo root, run each of the 13 scripts with `python3 <path>`, redirecting
      stdout+stderr to a per-script log, recording exit code. Any non-zero exit that is
      import/path related (ModuleNotFoundError, FileNotFoundError on a moved path) is an
      in-scope fix; a pre-existing data/logic failure is recorded as a known issue.
    acceptance_criteria:
      - "all 4 src/transarc scripts exit 0"
      - "all 9 src/bias scripts exit 0"
      - "no ModuleNotFoundError for transarc_error_analysis or new_metrics_analysis"
    commit: "test(02): verify all pillar scripts run clean after reorg"
  - id: 2
    title: "Confirm report artifacts present"
    type: execute
    autonomous: true
    read_first:
      - ".planning/phases/02-verify/02-CONTEXT.md"
    action: |
      After the batch run, confirm reports/ contains the expected artifacts and the run
      produced/refreshed them. List reports/ and note any script that writes no file
      (stdout-only) so it is not falsely flagged missing.
    acceptance_criteria:
      - "reports/ directory present with the pillar report files"
      - "REPRO-01/02/03 satisfied (lib importable, both pillars run)"
    verification:
      - "All 3 ROADMAP Phase 2 success criteria hold"
    commit: "test(02): verify all pillar scripts run clean after reorg"
