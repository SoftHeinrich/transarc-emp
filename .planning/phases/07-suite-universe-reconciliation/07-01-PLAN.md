---
phase: 07-suite-universe-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bias/component_suite.py
  - reports/COMPONENT_SUITE_sad-code.csv
  - reports/COMPONENT_SUITE_sad-model.csv
  - reports/COMPONENT_SUITE.md
autonomous: true
requirements: [CMP-01]
must_haves:
  truths:
    - "python3 src/bias/component_suite.py emits a per-project + AVG CSV for both sad-model and sad-code, reusing only calc_metrics."
    - "micro and macro are computed on the SAME mapped-only gold∪result component universe (unchanged from foundation)."
    - "min_comp and pct_missed are restricted to gold-only components."
    - "The committed sad-code CSV contains rows for every present project × present system (not bigbluebutton-only)."
    - "The artemis long-tail-abandonment finding still holds under the gold-only tail definition."
  artifacts:
    - path: "src/bias/component_suite.py"
      provides: "Gold-only tail loop (min_comp/pct_missed); shared-universe micro/macro; argparse CLI"
      contains: "def component_suite"
    - path: "reports/COMPONENT_SUITE_sad-code.csv"
      provides: "Full per-project + AVG suite at sad-code for all present systems"
    - path: "reports/COMPONENT_SUITE_sad-model.csv"
      provides: "Full per-project + AVG suite at sad-model for all present systems"
    - path: "reports/COMPONENT_SUITE.md"
      provides: "Headline-pair=shared-universe / tail=gold-coverage framing + re-confirmed artemis tail numbers"
      contains: "gold coverage"
  key_links:
    - from: "component_suite.component_suite()"
      to: "gold-only tail"
      via: "per-component F1 list restricted to gold_by_c for min_comp/pct_missed"
      pattern: "min_comp|pct_missed"
    - from: "component_suite.component_suite()"
      to: "calc_metrics"
      via: "only F1 primitive"
      pattern: "calc_metrics"
---

<objective>
Finalize `src/bias/component_suite.py` (CMP-01) so the headline pair (`micro`/`macro`)
stays on the shared gold∪result mapped-only universe (D-10) while the tail pair
(`min_comp`/`pct_missed`) becomes gold-only (D-11), apply the discretionary mechanical
hardening, regenerate the committed COMPONENT_SUITE CSVs as FULL runs (all present
projects × all present systems), and re-confirm the artemis long-tail-abandonment
finding under the new tail definition (D-12).

Purpose: The suite is the foundation the equivalence oracle + determinism check
(07-03) pins, and the source of the tail-coverage story for Phases 8–9. Its universe
split must be correct and its committed CSVs must be a complete, deterministic run.
Output: hardened `component_suite.py`; regenerated `COMPONENT_SUITE_sad-code.csv` /
`COMPONENT_SUITE_sad-model.csv`; updated `COMPONENT_SUITE.md` framing + re-confirmed
artemis tail numbers.

Discovery level: 0 (stdlib-only; reuses `calc_metrics`; established depth-2 +
`sys.path.insert` pattern already present in the file — no new dependencies).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-suite-universe-reconciliation/07-CONTEXT.md
@.planning/milestones/v1.2-ROADMAP.md
@.planning/milestones/v1.2-REQUIREMENTS.md
@CLAUDE.md

Project rules enforced in EVERY task of this plan: stdlib-only (no third-party
imports, no requirements.txt); `calc_metrics` is the ONLY F1 primitive — no new F1
math; no benchmark-derived word lists; non-destructive (supersede via note + `git mv`,
never silent deletion); preserve the depth-2 `src/<pillar>/x.py` + `sys.path.insert`
to `src/lib` import pattern.

<interfaces>
<!-- Extracted from src/bias/component_suite.py — executor uses these directly. -->
component_suite(gold_sc, result_sc) -> dict{micro, macro, gap, min_comp, pct_missed, gold_gini}
  micro  = calc_metrics(gold_sc, result_sc)[2]                        # pooled (sentence, comp) F1
  per_comp over set(gold_by_c) | set(res_by_c) -> macro / gap         # SHARED universe (keep, D-10)
  min_comp / pct_missed currently derived from the SAME union list    # CHANGE to gold-only (D-11)
SUITE_COLS = ["micro","macro","gap","min_comp","pct_missed","gold_gini"]
SYSTEMS    = [swattr_transarc, s20linker, artemis]  # keep all 3 (D-08)
LEVELS     = {"sad-model": (_model_inputs, ...), "sad-code": (_code_inputs, ...)}
run_level(level, projects) / averages(rows) / write_csv(level, rows, avg) / print_table(...)
CLI: --level {sad-model,sad-code,both}=both  --project <name>=all
NOTE: passing --project writes a SINGLE-project CSV that OVERWRITES the full committed
file — the committed sad-code CSV is currently bigbluebutton-only because of this.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split the universe — gold-only tail, shared-universe headline — and mechanical hardening</name>
  <files>src/bias/component_suite.py</files>
  <read_first>
    - src/bias/component_suite.py (whole file — the only file changed here)
    - .planning/phases/07-suite-universe-reconciliation/07-CONTEXT.md (D-10, D-11, D-12, and the Claude's-Discretion list)
  </read_first>
  <action>
    In `component_suite.component_suite()`: KEEP `micro` (= `calc_metrics(gold_sc,
    result_sc)[2]`) and KEEP the per-component F1 list iterated over
    `set(gold_by_c) | set(res_by_c)` as the source of `macro` and `gap` — this is the
    shared gold∪result mapped-only universe required by D-10 (preserves micro=link-F1
    at sad-sam and keeps suite-macro equal to `per_component_macro_f1` for the 07-03
    oracle). Add a SEPARATE per-component F1 list restricted to components present in
    `gold_by_c` only, and derive `min_comp` and `pct_missed` from THAT gold-only list
    (D-11): a real gold component with no result still counts as missed/min; a
    result-only (FP) component no longer pollutes the tail. Preserve the empty-list
    guards (return 0.0 when the relevant list is empty) and the `gold_gini` computation
    unchanged. Apply the discretionary mechanical hardening: make the argparse `--level`
    / `--project` help text explicit; standardize the absent-external-root warn+skip
    message wording in `run_level` (it already prints to stderr — keep `WARNING:` prefix
    and name the system/level/project); define deterministic `min_comp` tie handling
    (document that `min(per_comp_gold)` is order-independent and ties are immaterial);
    keep CSV column order = `SUITE_COLS` and 4-dp precision in `write_csv`. Do NOT add
    any new F1 formula — every score still flows through `calc_metrics`. Do NOT change
    the `SYSTEMS` registry (all 3 systems stay, per D-08).
  </action>
  <acceptance_criteria>
    - `grep -nE "gold_by_c" src/bias/component_suite.py` shows the tail metrics
      (`min_comp`/`pct_missed`) derived from a gold-only per-component list, while
      `macro` is still derived from a `set(gold_by_c) | set(res_by_c)` list.
    - `python3 -c "import ast,sys; ast.parse(open('src/bias/component_suite.py').read())"`
      succeeds (file parses).
    - `python3 src/bias/component_suite.py --help` exits 0 and lists `--level` and
      `--project`.
    - stdlib-only: `grep -nE "^\s*import |^\s*from " src/bias/component_suite.py`
      shows only stdlib modules + the `transarc_error_analysis` project import (no
      third-party packages).
    - `calc_metrics` remains the only F1 primitive: `grep -c "def .*f1\|def calc"
      src/bias/component_suite.py` returns 0 (no new F1/precision/recall helper added
      inside the file beyond `_gini`).
    - A single-project sanity invocation `python3 src/bias/component_suite.py --level
      sad-code --project bigbluebutton` runs without error and prints a `[component-suite]`
      summary line (restore the committed CSV afterward — Task 2 does the authoritative
      full regen).
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('/mnt/hostshare/ardoco-home/transarc-emp/src/bias/component_suite.py').read())" && python3 /mnt/hostshare/ardoco-home/transarc-emp/src/bias/component_suite.py --help >/dev/null && echo CLI_OK</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Full regen of committed suite CSVs + COMPONENT_SUITE.md framing + re-confirm artemis tail</name>
  <files>reports/COMPONENT_SUITE_sad-code.csv, reports/COMPONENT_SUITE_sad-model.csv, reports/COMPONENT_SUITE.md</files>
  <read_first>
    - src/bias/component_suite.py (run_level / averages / write_csv / main — to know the full-run command)
    - reports/COMPONENT_SUITE.md (existing narrative: the "Reconciled component universe", the per-level finding tables, and the "Caveats / open items" CMP-02 line)
    - reports/COMPONENT_SUITE_sad-code.csv and reports/COMPONENT_SUITE_sad-model.csv (current committed state — sad-code is bigbluebutton-only)
    - .planning/phases/07-suite-universe-reconciliation/07-CONTEXT.md (D-08, D-12)
  </read_first>
  <action>
    Regenerate BOTH committed CSVs with a single FULL run `python3
    src/bias/component_suite.py` (no `--level`, no `--project` → both levels, all
    projects, all present systems). All three external roots are present in this
    environment, so the output must contain rows for every project × {swattr/transarc,
    s20linker, artemis} where results exist — this fixes the pre-existing bigbluebutton-only
    partial in `COMPONENT_SUITE_sad-code.csv` and makes it consistent with the sad-model
    CSV (D-08: keep all 3 systems, all projects). Then update `COMPONENT_SUITE.md`: (a)
    carry the D-12 documentation framing in prose — the headline pair (`micro`/`macro`)
    is on the shared gold∪result universe and the tail pair (`min_comp`/`pct_missed`) is
    gold coverage of REAL components; (b) re-confirm the artemis long-tail-abandonment
    finding under the NEW gold-only tail definition and refresh the per-level finding
    tables (the `min_comp`/`pct_missed` numbers shift; `pct_missed` now reads as
    "fraction of REAL components abandoned") — verify artemis still has the worst tail at
    both levels and update the numbers/sentence to the regenerated values; (c) update the
    "Caveats / open items" note that names CMP-02 to reflect that the `metrics_api`
    reconciliation is now being done in 07-02 (point to it, do not delete the history).
    Keep COMPONENT_SUITE.md numbers consistent with the regenerated CSVs.
  </action>
  <acceptance_criteria>
    - `reports/COMPONENT_SUITE_sad-code.csv` contains data rows for more than one
      project: `cut -d, -f1 reports/COMPONENT_SUITE_sad-code.csv | grep -v '^project$' |
      grep -v '^AVG$' | sort -u | wc -l` returns >= 2 (no longer bigbluebutton-only).
    - Both CSVs have the header `project,system,micro,macro,gap,min_comp,pct_missed,gold_gini`
      and contain `AVG` rows for each present system.
    - `reports/COMPONENT_SUITE.md` contains the D-12 framing: `grep -qi "gold coverage"
      reports/COMPONENT_SUITE.md` AND a sentence stating the tail (`min_comp`/`pct_missed`)
      is gold-only.
    - The artemis tail finding is re-confirmed: COMPONENT_SUITE.md still states artemis
      has the worst `min_comp` at both levels, with numbers matching the regenerated CSV
      `artemis` AVG rows (no stale pre-change numbers left in the finding tables).
    - Re-running `python3 src/bias/component_suite.py` a second time leaves both CSVs
      byte-identical (`git diff --stat` clean after the authoritative regen+commit) —
      determinism precondition for 07-03.
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && python3 src/bias/component_suite.py >/dev/null 2>&1 && test "$(cut -d, -f1 reports/COMPONENT_SUITE_sad-code.csv | grep -vE '^(project|AVG)$' | sort -u | wc -l)" -ge 2 && grep -qi "gold coverage" reports/COMPONENT_SUITE.md && echo REGEN_OK</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → suite | Benchmark gold + bundled/external result CSVs are read as untrusted-shaped input (column-tolerant readers already guard this). |
| suite → committed reports | Regenerated CSVs/MD overwrite committed artifacts — integrity/scope of the overwrite is the risk, not confidentiality. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | gold-only tail change in component_suite() | mitigate | Keep `calc_metrics` as sole F1 primitive; assert tail derives from `gold_by_c` only; 07-03 determinism check re-pins the CSVs. |
| T-07-01-02 | Tampering | `--project` overwriting the full committed CSV | mitigate | Authoritative regen is a no-filter full run; acceptance asserts >= 2 projects present. |
| T-07-01-03 | Repudiation | COMPONENT_SUITE.md numbers drifting from CSVs | mitigate | Acceptance requires MD finding numbers to match regenerated CSV AVG rows. |
| T-07-01-SC | Tampering | dependency supply chain | accept | stdlib-only, no installs; no package legitimacy gate applicable. |
</threat_model>

<verification>
- `python3 src/bias/component_suite.py --help` exits 0 and lists `--level`/`--project`.
- Full run `python3 src/bias/component_suite.py` regenerates both CSVs with all
  present projects × systems; second run is byte-identical.
- `min_comp`/`pct_missed` are gold-only; `micro`/`macro` remain on gold∪result.
- COMPONENT_SUITE.md carries the gold-coverage framing and re-confirmed artemis tail.
- stdlib-only; `calc_metrics` is the sole F1 primitive (no new F1 math).
</verification>

<success_criteria>
CMP-01 satisfied: a stdlib `component_suite.py` computes `{micro, macro, gap, min_comp,
pct_missed, gold_gini}` for any system at both levels reusing `calc_metrics` only, with
micro/macro on one shared mapped-only universe and tail metrics gold-only, and emits the
full committed per-project + AVG CSVs. Maps to ROADMAP Phase 7 SC1 (CLI + CSV for both
levels) and SC2 (micro/macro on the same mapped-only universe). The equivalence-oracle
assertion of SC2 and the determinism guarantee of SC4 are delivered by 07-03.
</success_criteria>

<output>
Create `.planning/phases/07-suite-universe-reconciliation/07-01-SUMMARY.md` when done.
</output>
