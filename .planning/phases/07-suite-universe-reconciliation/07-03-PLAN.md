---
phase: 07-suite-universe-reconciliation
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - src/bias/check_component_suite.py
autonomous: true
requirements: [CMP-06]
must_haves:
  truths:
    - "A single command (python3 src/bias/check_component_suite.py) runs BOTH the equivalence oracle and the determinism check and returns a pass/fail exit code."
    - "suite-macro equals rq2_trivial_baselines.per_component_macro_f1 to 1e-9 for every present system at sad-code across all 5 projects."
    - "Re-running the suite regenerates the committed CSVs deterministically for present-system rows at both levels."
    - "Absent external systems are skipped with a notice; running the check does NOT mutate the committed reports."
  artifacts:
    - path: "src/bias/check_component_suite.py"
      provides: "Standalone equivalence-oracle + determinism check (mini-src/check.py style)"
      contains: "1e-9"
  key_links:
    - from: "check_component_suite.py"
      to: "component_suite.component_suite() macro"
      via: "equivalence oracle target A"
      pattern: "component_suite"
    - from: "check_component_suite.py"
      to: "rq2_trivial_baselines.per_component_macro_f1"
      via: "equivalence oracle target B (independent macro reference)"
      pattern: "per_component_macro_f1"
    - from: "check_component_suite.py determinism check"
      to: "temp dir (never reports/)"
      via: "regenerate-to-temp + diff present-system rows"
      pattern: "tempfile|mkdtemp|TemporaryDirectory"
---

<objective>
Deliver the reproducibility guarantee for the component suite (CMP-06): ONE standalone
`src/bias/check_component_suite.py`, modeled on `mini-src/check.py`, that performs BOTH
checks in a single `python3 ...` invocation (D-05):
  (1) EQUIVALENCE ORACLE (D-06): assert `component_suite.component_suite(...)['macro']`
      equals `rq2_trivial_baselines.per_component_macro_f1(...)` to 1e-9, for every
      PRESENT system (swattr/transarc always; s20linker/artemis when their external roots
      exist), at SAD-CODE, across all 5 projects — skip-with-notice when a system is
      absent. sad-code only (per_component_macro_f1 is the only INDEPENDENT macro
      reference; sad-model would be circular).
  (2) DETERMINISM CHECK (D-07/D-09): regenerate the suite CSVs to a TEMP location and diff
      against the committed `reports/COMPONENT_SUITE_{level}.csv` for BOTH levels and all
      present systems, scoped to present-system rows (skip absent systems with a notice).

Purpose: This is the phase's reproducibility gate — it binds the suite's headline macro
to an independent oracle and proves the committed CSVs are regenerable. Output: the new
check script, passing green against the reconciled (07-01) + regenerated (07-02) state.

Discovery level: 0 (stdlib-only; reuses existing suite + oracle functions; copies the
`mini-src/check.py` structure — no new dependencies, no new F1 math).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-suite-universe-reconciliation/07-CONTEXT.md
@.planning/milestones/v1.2-ROADMAP.md
@.planning/milestones/v1.2-REQUIREMENTS.md
@CLAUDE.md

This plan depends on 07-01 (finalized suite + full committed COMPONENT_SUITE CSVs with
gold-only tail) and 07-02 (reconciled `metrics_api`). Run AFTER both are committed.

Project rules enforced: stdlib-only; reuse `calc_metrics` only (the oracle calls existing
suite/oracle functions — adds NO new F1 math); no benchmark-derived word lists;
non-destructive (the determinism check writes ONLY to a temp dir, never to `reports/`);
preserve the depth-2 `src/bias/x.py` + `sys.path.insert` to `src/lib` import pattern.

<interfaces>
<!-- Style precedent + the exact functions the oracle/determinism check call. -->
mini-src/check.py (COPY THIS SHAPE):
  PANEL / COMPUTE dicts; per-(task/project) loop; TOL = 1e-9;
  SKIP / OK / FAIL print lines; `failures` counter; sys.exit(1) on any failure.

src/bias/component_suite.py:
  _code_inputs(proj) -> (gold_sc, collapse)      # mapped-only sad-code collapse
    NOTE: returns ONLY (gold_collapsed, collapse) — it does NOT expose `enrolled`
    (file-level (sentence, file) gold) or `file_to_comps`, which the oracle's
    per_component_macro_f1 call needs. Re-derive them by replicating _code_inputs'
    internals (see Task 1 action).
  _model_inputs(proj) -> (gold, collapse)        # sad-model collapse (determinism only)
  component_suite(gold_sc, result_sc) -> {"macro": ..., ...}   # oracle target A
  SYSTEMS = [(key,label,model_loader,code_loader), ...]  # present-system iteration
  LEVELS  = {"sad-model": (...), "sad-code": (...)}
  run_level(level, projects) / averages(rows) / write_csv(level, rows, avg)  # determinism regen
  REPORTS = .../reports ; committed files reports/COMPONENT_SUITE_{level}.csv
  PROJECTS, calc_metrics, normalize_path imported from transarc_error_analysis.
  Loaders used by _code_inputs (all importable from transarc_error_analysis, the same
  module component_suite imports from): load_code_model_files(proj),
  load_model_element_names(proj), enroll_gold_standard(load_gs_sam_code_raw(proj), code_model),
  load_gs_sad_code_enrolled(proj, code_model).

src/bias/rq2_trivial_baselines.py:
  per_component_macro_f1(gold_sad_code, result_sad_code, file_to_comps) -> float  # oracle target B
  (iterates gold∪result components, drops files with no mapped comp — same universe as suite macro)
  gold_sad_code / result_sad_code are FILE-LEVEL (sentence, file) sets; file_to_comps is
  the file -> {component_name} map.

ENVIRONMENT NOTE: all three external roots (agent-linker, sota/recovered-links,
results_artemis_gpt54) are PRESENT here, so all 3 systems are exercised; swattr/transarc
is in-repo and ALWAYS present (the always-on assertion).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author check_component_suite.py (equivalence oracle + determinism check)</name>
  <files>src/bias/check_component_suite.py</files>
  <read_first>
    - mini-src/check.py (the structure to copy: PANEL/COMPUTE dicts, per-project loop, TOL=1e-9, SKIP/OK/FAIL lines, failures counter, exit code)
    - src/bias/component_suite.py (_code_inputs, _model_inputs, component_suite, SYSTEMS, LEVELS, run_level, averages, write_csv, REPORTS, PROJECTS). IMPORTANT: `_code_inputs(proj)` returns ONLY `(gold_collapsed, collapse)` — it does NOT return `enrolled` (file-level (sentence, file) gold) or `file_to_comps`. The oracle's `per_component_macro_f1(gold_sad_code, result_sad_code, file_to_comps)` needs BOTH of those file-level objects, so the check must re-derive them itself (see action).
    - src/lib/transarc_error_analysis.py (the loaders _code_inputs uses internally: load_code_model_files, load_model_element_names, enroll_gold_standard, load_gs_sam_code_raw, load_gs_sad_code_enrolled — these are what the check replicates to rebuild enrolled + file_to_comps)
    - src/bias/rq2_trivial_baselines.py (per_component_macro_f1 signature + that it builds file_to_comps from sam_enrolled)
    - .planning/phases/07-suite-universe-reconciliation/07-CONTEXT.md (D-05, D-06, D-07, D-09)
  </read_first>
  <action>
    Create `src/bias/check_component_suite.py` as a standalone stdlib script (depth-2 +
    `sys.path.insert` to `src/lib`, importing `component_suite`, `rq2_trivial_baselines`,
    and the needed loaders from `transarc_error_analysis`). Define `TOL = 1e-9` and a
    `failures` counter, printing `SKIP`/`OK`/`FAIL` lines exactly like `mini-src/check.py`
    and `sys.exit(1)` on any failure. PART 1 — EQUIVALENCE ORACLE (sad-code only): for
    each present system in `component_suite.SYSTEMS` and each project in
    `PROJECTS`, build the sad-code inputs via `component_suite._code_inputs(proj)` (gives
    `gold_sc` + `collapse`), load the system's code links via its code-loader, SKIP-with-notice
    when the loader returns empty (absent external root), else compute `result_sc =
    collapse(links)` and assert `component_suite(gold_sc, result_sc)['macro']` equals
    `rq2_trivial_baselines.per_component_macro_f1(<file-level gold>, <file-level result>,
    <file_to_comps>)` to within `TOL`. BE AWARE: `_code_inputs` does NOT hand back
    `enrolled` or `file_to_comps`, so the check must REBUILD the file-level gold +
    file_to_comps itself, replicating `_code_inputs`' internals EXACTLY using the
    `transarc_error_analysis` loaders — i.e. `code_model = load_code_model_files(proj)`;
    `names = load_model_element_names(proj)`; `sam_enrolled =
    enroll_gold_standard(load_gs_sam_code_raw(proj), code_model)`; `file_to_comps[fp] =
    {names.get(ae, ae) ...}` over sam_enrolled; `enrolled =
    load_gs_sad_code_enrolled(proj, code_model)` (file-level gold), and the file-level
    result = the system's raw code links — so BOTH oracle sides share the same mapped-only
    universe. (The suite macro side collapses via `collapse`; the oracle side passes the
    file-level objects to `per_component_macro_f1` which applies its own mapped-only drop —
    the 1e-9 assertion catches any drift between the two reconstructions.) Do NOT assume
    `_code_inputs` exposes enrolled/file_to_comps. PART 2 — DETERMINISM CHECK (both levels,
    present systems, D-07/D-09): regenerate the suite into a TEMP directory using
    `tempfile.mkdtemp()` (or `TemporaryDirectory`) — call `run_level`/`averages`/`write_csv`
    after temporarily pointing `component_suite.REPORTS` at the temp dir, OR copy the rows
    and write CSVs yourself into temp — and diff the temp CSV against the committed
    `reports/COMPONENT_SUITE_{level}.csv` comparing ONLY rows whose system is present now
    (skip absent-system rows with a notice). The check MUST restore
    `component_suite.REPORTS` and MUST NOT write anything into the real `reports/`
    directory. Add no new F1 math — all scores come from the imported suite/oracle
    functions.
  </action>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('src/bias/check_component_suite.py').read())"`
      succeeds.
    - `grep -q "1e-9" src/bias/check_component_suite.py` and the script defines a
      `failures` counter and calls `sys.exit(1)` on mismatch (grep `sys.exit`).
    - The check re-derives the file-level oracle inputs itself (does NOT assume
      `_code_inputs` returns enrolled/file_to_comps): grep shows it calls the
      `load_gs_sad_code_enrolled` / `enroll_gold_standard` (or equivalent) loaders to build
      enrolled + file_to_comps.
    - The determinism check writes only to a temp dir: `grep -nE
      "tempfile|mkdtemp|TemporaryDirectory" src/bias/check_component_suite.py` matches,
      and there is NO unconditional `write_csv(..., REPORTS ...)` targeting the real
      reports dir.
    - stdlib-only: `grep -nE "^\s*import |^\s*from " src/bias/check_component_suite.py`
      shows only stdlib + project modules (component_suite, rq2_trivial_baselines,
      transarc_error_analysis).
    - The script imports BOTH `component_suite.component_suite` and
      `rq2_trivial_baselines.per_component_macro_f1` (grep both names).
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && python3 -c "import ast; ast.parse(open('src/bias/check_component_suite.py').read())" && grep -q "1e-9" src/bias/check_component_suite.py && grep -qE "tempfile|mkdtemp|TemporaryDirectory" src/bias/check_component_suite.py && grep -q "per_component_macro_f1" src/bias/check_component_suite.py && echo SCRIPT_OK</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Run the check green against the reconciled state; prove it is non-mutating</name>
  <files>src/bias/check_component_suite.py</files>
  <read_first>
    - src/bias/check_component_suite.py (the script from Task 1)
    - reports/COMPONENT_SUITE_sad-code.csv, reports/COMPONENT_SUITE_sad-model.csv (the committed CSVs the determinism check pins — regenerated by 07-01)
    - .planning/phases/07-suite-universe-reconciliation/07-CONTEXT.md (D-09 — present-system scope)
  </read_first>
  <action>
    Run `python3 src/bias/check_component_suite.py` against the reconciled (07-01) +
    regenerated (07-02) committed state and make it pass: the equivalence oracle must hold
    for swattr/transarc (always present) at sad-code across all 5 projects to 1e-9, and
    for s20linker/artemis where their roots are present (all present here); the
    determinism diff must be clean for present-system rows at BOTH levels. If the oracle
    fails for a present system, diagnose whether the file-level gold / `file_to_comps`
    rebuilt in the check matches what `_code_inputs` uses (universe mismatch is the usual
    cause — confirm the check replicated the load_gs_sad_code_enrolled / enroll_gold_standard
    construction exactly) and fix the check's input construction — do NOT relax `TOL` and
    do NOT modify `component_suite` or `per_component_macro_f1` to force agreement. If the
    determinism diff fails, confirm 07-01 committed a FULL deterministic run (no
    `--project` partial) and that only present-system rows are compared. After the run,
    confirm the script left `reports/` untouched (the determinism regen went to temp only).
  </action>
  <acceptance_criteria>
    - `python3 src/bias/check_component_suite.py` exits 0 and prints a final PASS line; no
      `FAIL` lines in output.
    - Equivalence holds for swattr/transarc at sad-code for all 5 projects (OK lines for
      each), within 1e-9.
    - Determinism diff is clean for present-system rows at both `sad-model` and `sad-code`.
    - Running the check does NOT modify the committed CSVs: `git status --short
      reports/COMPONENT_SUITE_sad-code.csv reports/COMPONENT_SUITE_sad-model.csv` is empty
      after the run.
    - `TOL` remains `1e-9` (not relaxed) and neither `component_suite` nor
      `rq2_trivial_baselines` was edited (`git diff` of those two files empty in this plan).
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && python3 src/bias/check_component_suite.py && test -z "$(git status --short reports/COMPONENT_SUITE_sad-code.csv reports/COMPONENT_SUITE_sad-model.csv)" && echo CHECK_GREEN_AND_CLEAN</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → check | Reads committed CSVs + benchmark/result data; integrity of the equivalence/determinism verdict is the concern. |
| check → reports/ | The determinism regen MUST stay in a temp dir; writing into reports/ would corrupt the very artifact it validates. |
| present vs absent systems | Absent external roots must skip-with-notice, not fail (D-09 present-system scope). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | determinism regen overwriting reports/ | mitigate | Regenerate to `tempfile` temp dir only; restore `component_suite.REPORTS`; acceptance asserts `git status` clean after run. |
| T-07-03-02 | Spoofing | oracle passing via a relaxed tolerance | mitigate | TOL pinned at 1e-9; acceptance forbids relaxing it or editing the suite/oracle to force agreement. |
| T-07-03-03 | Repudiation | silent skip hiding a real regression | mitigate | swattr/transarc is always present and always asserted; SKIP lines are printed explicitly with the absent system/level. |
| T-07-03-SC | Tampering | dependency supply chain | accept | stdlib-only, no installs; no package legitimacy gate applicable. |
</threat_model>

<verification>
- `python3 src/bias/check_component_suite.py` exits 0, prints PASS, no FAIL lines.
- Equivalence (suite-macro == per_component_macro_f1) holds to 1e-9 for every present
  system at sad-code across all 5 projects; swattr/transarc always asserted.
- Determinism diff clean for present-system rows at both levels; absent systems skipped.
- `reports/` unchanged after the run (temp-only regen); TOL stays 1e-9; suite + oracle
  source files unedited by this plan.
</verification>

<success_criteria>
CMP-06 satisfied: one standalone check asserts suite-`macro` equals
`per_component_macro_f1` on the reconciled universe (equivalence oracle) AND that
re-running the suite regenerates the committed CSVs deterministically (present-system
scope). Maps to ROADMAP Phase 7 SC2 (equivalence oracle asserts suite-macro ==
per_component_macro_f1) and SC4 (re-running regenerates the committed CSVs
deterministically).
</success_criteria>

<output>
Create `.planning/phases/07-suite-universe-reconciliation/07-03-SUMMARY.md` when done.
</output>
