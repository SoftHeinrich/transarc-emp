---
plan: 03-01
phase: 3
wave: 1
title: "Restructure eval.tex into Ch1=TransArc + Ch2=Bias; generate tables from data"
autonomous: true
depends_on: []
files_modified:
  - "src/paper/generate_tables.py (new)"
  - "writing/tables/*.tex (generated)"
  - "writing/eval.tex"
requirements: [PAPER-01, PAPER-02, PAPER-03, PAPER-04]
must_haves:
  truths:
    - "eval.tex Ch1 = TransArc empirical study (SAD-SAM bottleneck, amplification, cascade, contribution, TP-gain, s12c)"
    - "eval.tex Ch2 = Benchmark bias = the two prior chapters merged into one"
    - "a stdlib-only script generates the LaTeX tables from retained reports/data into writing/tables/"
    - "every \\input target exists; \\begin/\\end balanced; \\ref/\\cref have matching \\label"
    - "every table/claim traces to a retained script or report (SC3)"
tasks:
  - id: 1
    title: "Table-generation script + generated tables"
    type: execute
    action: |
      Write src/paper/generate_tables.py (stdlib only; no benchmark-derived word
      lists). It reads retained reports/ (and/or re-derives via src/lib loaders)
      and writes LaTeX table files into writing/tables/*.tex (booktabs style).
      Cover the key Ch1 tables (SAD-SAM contribution, amplification, cascade,
      s12c four-level comparison) and at least the Ch2 dashboard table. Run it;
      confirm writing/tables/*.tex created.
    acceptance_criteria:
      - "python3 src/paper/generate_tables.py exits 0"
      - "writing/tables/ contains >=4 .tex table files"
      - "no benchmark-derived word lists in the script (stopwords only)"
  - id: 2
    title: "Author Ch1 = TransArc empirical study"
    type: execute
    action: |
      Insert a NEW first chapter into eval.tex BEFORE the current first chapter:
      \chapter{The TransArc Empirical Study} covering SAD-SAM bottleneck, error
      amplification/cascade, SAD-SAM actual contribution + TP-gain, SAM-CODE
      cascade, and S12C/S12E-vs-TransArc. Pull numbers/findings from
      TRANSARC_EMPIRICAL_STUDY, SAD_SAM_ACTUAL_CONTRIBUTION, SAD_SAM_TP_GAIN_STUDY,
      SAM_CODE_CASCADE, S12C_VS_TRANSARC.csv. \input the generated Ch1 tables.
      Match existing paper style (\sadsam/\samcode/\sadcode macros, booktabs).
    acceptance_criteria:
      - "eval.tex contains a TransArc-study \\chapter before the Distributional Inequality chapter"
      - "Ch1 references the s12c comparison and SAD-SAM bottleneck"
      - "Ch1 \\input targets exist in writing/tables/"
  - id: 3
    title: "Merge prior 2 chapters into Ch2=Bias; structural validation"
    type: execute
    action: |
      Convert the two existing chapters (Distributional Inequality + Comprehensive
      Metrics) into ONE \chapter{Benchmark Bias and Evaluation Metrics}, demoting
      their former \chapter titles to \section level and preserving all content.
      Then run structural validation: balanced \begin/\end, balanced braces, every
      \ref/\cref has a \label, every \input target exists. Fix any imbalance.
    acceptance_criteria:
      - "exactly 2 \\chapter commands remain in eval.tex (Ch1 TransArc, Ch2 Bias)"
      - "structural validation passes (env/brace balance, refs resolve, inputs exist)"
    verification:
      - "All 4 Phase 3 success criteria (reframed SC4 = structural validation) hold"
---

<objective>
Restructure writing/eval.tex into the two-pillar structure (Ch1=TransArc study,
Ch2=Benchmark bias) and add a data-driven LaTeX table generator. SC4 is reframed
(user): structural validation, not pdflatex compile.
</objective>

<environment_caveat>
Hostshare mount corrupts file reads intermittently. Read eval.tex/reports in small
`sed -n 'A,Bp'` slices with the sandbox DISABLED; re-read once if output looks
doubled/truncated. The Write tool is reliable. Verify every Write by reading it
back. Do NOT author from a read containing "(truncated)" or repeated lines. Do NOT
run git — the orchestrator commits.
</environment_caveat>
