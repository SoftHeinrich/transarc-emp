---
phase: 08-multi-system-comparison-fitness-validation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - reports/COMPONENT_SUITE.md
  - reports/COMPONENT_SUITE_sad-model.csv
  - reports/COMPONENT_SUITE_sad-code.csv
autonomous: true
requirements: [CMP-03]
must_haves:
  truths:
    - "The committed reports/COMPONENT_SUITE_{sad-model,sad-code}.csv regenerate bit-for-bit from `python3 src/bias/component_suite.py` over all three present systems (swattr/transarc, s20linker, artemis) at both levels — now deterministic because the artemis source is pinned (08-01)."
    - "reports/COMPONENT_SUITE.md hardens the 3-system, 2-level comparison: it carries the cross-level narrative, the swattr≡transarc one-column note (D-12), and a standardized skip-with-notice statement for absent external roots (D-08)."
    - "The report's skip-with-notice policy is exercised and documented: making an external root unavailable makes component_suite.run_level emit the standardized WARNING: line for that system/level/project and still produce the present-system rows, without hard-failing."
    - "All headline numbers cited in COMPONENT_SUITE.md match the regenerated CSVs (no stale numbers)."
  artifacts:
    - path: "reports/COMPONENT_SUITE_sad-code.csv"
      provides: "Deterministic per-project + AVG suite columns, 3 systems, doc-to-code"
      contains: "artemis"
    - path: "reports/COMPONENT_SUITE_sad-model.csv"
      provides: "Deterministic per-project + AVG suite columns, 3 systems, doc-to-model"
      contains: "artemis"
    - path: "reports/COMPONENT_SUITE.md"
      provides: "Hardened cross-level narrative + skip-with-notice + swattr≡transarc note"
      contains: "WARNING"
  key_links:
    - from: "reports/COMPONENT_SUITE.md"
      to: "reports/COMPONENT_SUITE_{level}.csv"
      via: "every cited number traces to a regenerated CSV cell"
      pattern: "COMPONENT_SUITE_"
    - from: "component_suite._warn_skip"
      to: "absent external root (graceful skip)"
      via: "standardized WARNING: notice, present-system rows still emitted"
      pattern: "WARNING:"
---

<objective>
Harden CMP-03 — the multi-system, two-level comparison — now that the artemis source is
pinned (08-01). This plan does NOT change suite math (frozen in Phase 7); it (1) regenerates
the committed `reports/COMPONENT_SUITE_{sad-model,sad-code}.csv` over all three present
systems at both levels and confirms the run is now DETERMINISTIC (the artemis sad-code rows
are reproducible because `results_artemis_gpt54/` is committed), (2) hardens
`reports/COMPONENT_SUITE.md` so its narrative, headline numbers, swattr≡transarc note (D-12),
and the standardized skip-with-notice policy (D-08) are all current and exercised, and
(3) PROVES the graceful-skip path: with an external root made unavailable,
`component_suite.run_level` emits the standardized `WARNING:` line and still produces the
present-system rows without hard-failing.

Purpose: this is SC1 — the suite runs over all three systems at both levels into a `reports/`
artifact with the cross-level narrative and graceful skip for absent external roots — made
defensible and reproducible. Output: regenerated CSVs (or confirmed-identical), a hardened
COMPONENT_SUITE.md, and a recorded skip-with-notice demonstration.

Discovery level: 0 (no new code — drives the existing `component_suite.py` and edits one
generated markdown report; no new dependencies, no F1 math).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-multi-system-comparison-fitness-validation/08-CONTEXT.md
@.planning/milestones/v1.2-ROADMAP.md
@.planning/milestones/v1.2-REQUIREMENTS.md
@CLAUDE.md

This plan depends on 08-01 (artemis source pinned) — run AFTER 08-01 is committed, so the
artemis sad-code rows in the regenerated CSVs are reproducible. It shares the `reports/`
tree with 08-03 but touches DIFFERENT files (this plan: COMPONENT_SUITE.*; 08-03:
METRIC_FITNESS.*), so the two Wave-2 plans have no file overlap.

Project rules enforced: stdlib-only; reuse `calc_metrics` only (the suite already does — this
plan adds NO new math); no benchmark-derived word lists; non-destructive (the report is
UPDATED to current numbers, history preserved via its existing reconciliation/caveats notes —
not rewritten to hide prior corrections); reports kept consistent with the script.

<interfaces>
<!-- The suite entry points this plan drives + the skip path it exercises. -->
src/bias/component_suite.py (DO NOT EDIT — drive it as-is):
  python3 src/bias/component_suite.py                 # both levels, all 3 systems -> writes both CSVs
  SYSTEMS = [swattr_transarc, s20linker, artemis]     # present-system iteration
  run_level(level, projects) / averages(rows) / write_csv(level, rows, avg)
  _warn_skip(level, label, proj): prints "WARNING: no {level} result links for system=...
                                  (absent or empty external root); skipping" to stderr
  REPORTS / COMPONENT_SUITE_{level}.csv               # the committed outputs
  External roots that, if absent, trigger the skip:
    AGENT_LINKER      (s20linker), ARTEMIS_DOC_CODE (artemis sad-code), ARTEMIS_LOCAL (artemis sad-model)
  --project <p> OVERWRITES the full CSV (partial); regen the AUTHORITATIVE CSVs with NO --project.

  IMPORTANT — how run_level resolves loaders (affects the skip demo): run_level iterates the
  SYSTEMS list and unpacks each tuple's loader (mloader/cloader) DIRECTLY from the tuple. Those
  function references were captured when SYSTEMS was built at import. Reassigning a module-level
  loader attribute (e.g. cs._s20_model = lambda p: set()) does NOT change what run_level calls —
  the tuple still holds the original function. To exercise the skip, make a ROOT CONSTANT point
  at a non-existent path (e.g. cs.ARTEMIS_LOCAL = Path('/nonexistent')) so the real loader reads
  empty, OR rebuild cs.SYSTEMS with the target system's loader replaced.

Committed CSV schema (header): project,system,micro,macro,gap,min_comp,pct_missed,gold_gini
  AVG rows present per system; 4-dp values. swattr/transarc AVG: sad-code micro 0.7949 /
  macro 0.8134; sad-model micro 0.7990 / macro 0.7932 (Phase-7 reconciled — verify, don't trust).

PROJECTS: mediastore, teastore, teammates, bigbluebutton, jabref. All 3 roots present today.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the committed suite CSVs deterministically over all 3 systems, both levels</name>
  <files>reports/COMPONENT_SUITE_sad-model.csv, reports/COMPONENT_SUITE_sad-code.csv</files>
  <read_first>
    - src/bias/component_suite.py (main / run_level / write_csv — confirm a no-argument run writes BOTH COMPONENT_SUITE_<level>.csv over all 3 SYSTEMS; note the --project partial-overwrite warning in main's help, so regen MUST be full)
    - reports/COMPONENT_SUITE_sad-code.csv, reports/COMPONENT_SUITE_sad-model.csv (the currently committed CSVs — the determinism target)
    - .planning/phases/08-multi-system-comparison-fitness-validation/08-CONTEXT.md (D-09 both levels; D-08 present-system reproducibility; D-10 the artemis pin that makes this deterministic; D-11 universe split frozen — do not re-open)
    - reports/ARTEMIS_PROVENANCE.md (from 08-01 — the canonical source the artemis rows now read from)
  </read_first>
  <action>
    Run `python3 src/bias/component_suite.py` with NO `--project` (full run) to regenerate
    BOTH `reports/COMPONENT_SUITE_sad-model.csv` and `reports/COMPONENT_SUITE_sad-code.csv`
    over all three present systems (swattr/transarc, s20linker, artemis) at both levels.
    Then run it a SECOND time and confirm the two CSVs are byte-identical between runs
    (determinism) — the artemis sad-code rows are now reproducible because 08-01 pinned the
    `ARTEMIS_LOCAL` source. Confirm `git diff` of the two CSVs against the previously committed
    versions is EMPTY (Phase 7 already committed the reconciled numbers; this run should
    reproduce them exactly now that provenance is pinned). If `git diff` is non-empty, do NOT
    edit the CSVs by hand and do NOT touch suite math — diagnose the cause (most likely a
    pre-08-01 artemis source difference) and record the delta; the expectation under a pinned
    source is a clean (empty) diff. Do NOT use `--project` for the authoritative regen (it
    partial-overwrites the committed file). The universe split (D-11) is frozen — do not alter
    `component_suite.py`.
  </action>
  <acceptance_criteria>
    - `python3 src/bias/component_suite.py` exits 0 and writes both
      `reports/COMPONENT_SUITE_sad-model.csv` and `reports/COMPONENT_SUITE_sad-code.csv`.
    - Determinism: running the suite twice yields byte-identical CSVs (a temp-copy + `diff` of
      the two runs is empty for both levels).
    - Each CSV contains all 3 systems for all 5 projects + AVG rows:
      `grep -c "artemis" reports/COMPONENT_SUITE_sad-code.csv` >= 6 (5 projects + 1 AVG) and
      likewise for `s20linker` and `swattr/transarc`.
    - `git diff --stat reports/COMPONENT_SUITE_sad-model.csv reports/COMPONENT_SUITE_sad-code.csv`
      is EMPTY against the Phase-7 committed CSVs (numbers reproduce exactly under the pinned
      source); if non-empty, the SUMMARY records the precise per-cell delta and its cause.
    - The CSV header is unchanged (`project,system,micro,macro,gap,min_comp,pct_missed,gold_gini`).
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && cp reports/COMPONENT_SUITE_sad-code.csv /tmp/sc_run1.csv && cp reports/COMPONENT_SUITE_sad-model.csv /tmp/sm_run1.csv && python3 src/bias/component_suite.py >/dev/null 2>/tmp/suite_warn.log && diff -q reports/COMPONENT_SUITE_sad-code.csv /tmp/sc_run1.csv && diff -q reports/COMPONENT_SUITE_sad-model.csv /tmp/sm_run1.csv && test "$(grep -c artemis reports/COMPONENT_SUITE_sad-code.csv)" -ge 6 && head -1 reports/COMPONENT_SUITE_sad-code.csv | grep -q "project,system,micro,macro,gap,min_comp,pct_missed,gold_gini" && echo SUITE_CSVS_DETERMINISTIC</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Harden COMPONENT_SUITE.md (cross-level narrative + swattr≡transarc + skip-with-notice) and demonstrate graceful skip</name>
  <files>reports/COMPONENT_SUITE.md</files>
  <read_first>
    - reports/COMPONENT_SUITE.md (the current narrative — the per-level result tables ~lines 72-95, Findings ~96-129, Caveats/open items ~131-143; this plan refreshes numbers + tightens the skip-with-notice + swattr≡transarc statements without erasing the reconciliation history)
    - reports/COMPONENT_SUITE_sad-code.csv, reports/COMPONENT_SUITE_sad-model.csv (the regenerated source-of-truth numbers every cited figure must match)
    - src/bias/component_suite.py (_warn_skip exact message format + the three external-root constants AGENT_LINKER / ARTEMIS_DOC_CODE / ARTEMIS_LOCAL; AND the run_level loader-resolution note in this plan's <interfaces> — the skip demo must patch a ROOT CONSTANT, not a module-level loader attribute)
    - .planning/phases/08-multi-system-comparison-fitness-validation/08-CONTEXT.md (D-08 skip-with-notice; D-09 both levels; D-12 swattr≡transarc one column; D-11 framing macro=headline/tail=discriminator carried)
  </read_first>
  <action>
    Update `reports/COMPONENT_SUITE.md` so it is a hardened, current CMP-03 artifact: (a)
    ensure every cited number in the per-level result tables and Findings matches the
    regenerated CSVs from Task 1 (no stale figures — re-read the CSVs and reconcile each cited
    AVG/per-project value); (b) make the swattr≡transarc one-column convention (D-12) explicit
    in the systems description (SWATTR ⋈ ArCoTL at doc-to-code; the live contrast is heuristic-
    uniform vs LLM-peaky-but-leaky); (c) state the standardized skip-with-notice policy (D-08)
    using the ACTUAL `_warn_skip` message format and naming the three guarded roots
    (`AGENT_LINKER`, `ARTEMIS_DOC_CODE`, `ARTEMIS_LOCAL`), making clear that an absent root
    yields a `WARNING:` line and present-system rows without hard-fail; (d) cross-reference the
    new `reports/ARTEMIS_PROVENANCE.md` (08-01) as the source-of-truth for the artemis input.
    Preserve the existing universe-reconciliation history/caveats (non-destructive — keep the
    "what it corrected" section). THEN demonstrate the graceful skip empirically IN A THROWAWAY
    PROCESS: make ONE external root unavailable by pointing its root CONSTANT at a non-existent
    path — e.g. set `component_suite.ARTEMIS_LOCAL = Path('/nonexistent_artemis_root')` (or
    `AGENT_LINKER` for s20linker) — then call `component_suite.run_level('sad-model',
    ['mediastore'])` and capture that the standardized `WARNING:` line is emitted for that
    system AND the OTHER present systems' rows are still produced. CRITICAL: do NOT try to
    demonstrate the skip by reassigning a module-level loader attribute (e.g.
    `cs._artemis_model = lambda p: set()`) — `run_level` resolves each loader from the `SYSTEMS`
    tuple captured at import, so a module-attribute reassignment is IGNORED and the system still
    runs (this is the faithful "absent external root" demo anyway: patch the CONSTANT, or rebuild
    `cs.SYSTEMS` with the target system's loader replaced). This is a throwaway in-process demo
    only — do NOT commit a broken root, do NOT regenerate the committed CSVs from the broken
    state, and leave `component_suite.py` + the committed CSVs unchanged. Record the captured
    WARNING line + present-rows count in the SUMMARY. Do NOT edit `component_suite.py`.
  </action>
  <acceptance_criteria>
    - COMPONENT_SUITE.md states the skip-with-notice policy quoting the real notice:
      `grep -q "WARNING:" reports/COMPONENT_SUITE.md` and it names the guarded roots
      (`grep -nE "AGENT_LINKER|ARTEMIS_DOC_CODE|ARTEMIS_LOCAL|external" reports/COMPONENT_SUITE.md`).
    - The swattr≡transarc one-column note is present:
      `grep -niE "swattr.?(≡|=|is).?transarc|SWATTR.*ArCoTL" reports/COMPONENT_SUITE.md` matches.
    - It cross-references the provenance ledger:
      `grep -q "ARTEMIS_PROVENANCE" reports/COMPONENT_SUITE.md`.
    - No stale headline number: the swattr/transarc AVG macro and micro cited in the doc-to-code
      table equal the regenerated `reports/COMPONENT_SUITE_sad-code.csv` AVG row (verify by
      reading both — the cited values match the CSV cells to the precision shown).
    - Graceful-skip DEMONSTRATED: a throwaway run with ARTEMIS_LOCAL pointed at a non-existent
      path emits the standardized `WARNING: no sad-model result links for system=artemis` line
      AND still returns the present-system rows (s20linker present, artemis skipped); the real
      `component_suite.py` and the committed CSVs are left UNCHANGED afterward
      (`git status --short src/bias/component_suite.py reports/COMPONENT_SUITE_*.csv` empty).
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && grep -q "WARNING:" reports/COMPONENT_SUITE.md && grep -nE "AGENT_LINKER|ARTEMIS_DOC_CODE|ARTEMIS_LOCAL|external" reports/COMPONENT_SUITE.md && grep -niE "swattr.?(≡|=|is).?transarc|SWATTR.*ArCoTL" reports/COMPONENT_SUITE.md && grep -q "ARTEMIS_PROVENANCE" reports/COMPONENT_SUITE.md && python3 -c "import sys; from pathlib import Path; sys.path.insert(0,'src/lib'); sys.path.insert(0,'src/bias'); import component_suite as cs; cs.ARTEMIS_LOCAL=Path('/nonexistent_artemis_root'); rows=cs.run_level('sad-model',['mediastore']); print('PRESENT_ROWS',len(rows)); assert any(k=='s20linker' for _,k,_,_ in rows); assert all(k!='artemis' for _,k,_,_ in rows)" 2>/tmp/skip_demo.log && grep -q "WARNING: no sad-model result links for system=artemis" /tmp/skip_demo.log && test -z "$(git status --short src/bias/component_suite.py reports/COMPONENT_SUITE_sad-code.csv reports/COMPONENT_SUITE_sad-model.csv)" && echo COMPONENT_SUITE_HARDENED</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suite run → committed CSVs | The regen must reproduce the committed numbers; an accidental partial (`--project`) run would corrupt the authoritative CSVs. |
| report prose → CSV cells | Cited numbers must trace to the regenerated CSVs; a stale figure would misrepresent the comparison. |
| present vs absent external roots | An absent root must skip-with-notice (graceful), not hard-fail or silently drop a present system. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | partial `--project` run overwriting the full committed CSV | mitigate | Regen with NO `--project`; acceptance asserts all 3 systems × 5 projects + AVG present and a clean byte-identical re-run. |
| T-08-02-02 | Spoofing | stale numbers in COMPONENT_SUITE.md not matching the CSVs | mitigate | Acceptance cross-checks cited swattr/transarc AVG macro/micro against the regenerated CSV cells; numbers re-read from CSV. |
| T-08-02-03 | Repudiation | claiming graceful-skip without exercising it | mitigate | A throwaway run with a root constant pointed at a non-existent path is required to emit the real `WARNING:` line and still return present rows; real files left unchanged. |
| T-08-02-SC | Tampering | dependency supply chain | accept | stdlib-only, no installs; not applicable. |
</threat_model>

<verification>
- `python3 src/bias/component_suite.py` regenerates both CSVs deterministically (byte-identical
  on a second run; clean `git diff` against the Phase-7 committed numbers under the pinned source).
- COMPONENT_SUITE.md carries current numbers, the swattr≡transarc note (D-12), the
  standardized skip-with-notice policy (D-08) naming the real roots, and a cross-reference to
  ARTEMIS_PROVENANCE.md.
- Graceful skip is demonstrated: a root constant pointed at a non-existent path emits the
  standardized `WARNING:` and still yields present-system rows, with no permanent change to the
  suite or committed CSVs.
</verification>

<success_criteria>
CMP-03 satisfied: the suite runs over all three present systems at both levels into the
committed `reports/COMPONENT_SUITE_{level}.csv` + a hardened `COMPONENT_SUITE.md` cross-level
narrative, with graceful skip-with-notice for absent external roots, deterministic now that
the artemis source is pinned. Maps to ROADMAP Phase 8 SC1 (suite runs over all three systems
at both levels into a reports/ artifact with the cross-level narrative; graceful skip for
absent external roots).
</success_criteria>

<output>
Create `.planning/phases/08-multi-system-comparison-fitness-validation/08-02-SUMMARY.md` when done.
</output>
