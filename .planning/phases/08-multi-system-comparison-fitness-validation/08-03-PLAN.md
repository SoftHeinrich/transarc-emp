---
phase: 08-multi-system-comparison-fitness-validation
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/bias/metric_fitness.py
  - reports/METRIC_FITNESS.md
  - reports/METRIC_FITNESS.csv
autonomous: true
requirements: [CMP-04]
must_haves:
  truths:
    - "A single command (python3 src/bias/metric_fitness.py) scores each candidate suite column (micro, macro, gap, min_comp, pct_missed; gold_gini as a descriptor) on FOUR axes — separation, validity, stability, degeneracy — at BOTH levels and writes reports/METRIC_FITNESS.csv + reports/METRIC_FITNESS.md + a one-line verdict, exiting 0."
    - "The separation axis ranks the three REAL paper systems (swattr/transarc, s20linker, artemis) against the rq2 trivial floor (Random + Top-3, reused as-is — no new synthetic baselines) and reports the margin per column."
    - "The validity axis bounds each column against the rq2 oracle/random anchors (compute_oracle_f1 / compute_random_f1), the stability axis reports cross-project variance, and the degeneracy axis flags saturation/insensitivity — all numeric."
    - "The verdict is DATA-DERIVED from the computed axis scores (headline vs diagnostic emerges from the numbers, not asserted); if the data contradicts the milestone's expected verdict (headline=macro+tail, diagnostic=micro/gap), the report says so explicitly."
    - "The SC4 artemis long-tail-abandonment finding is reproduced and quantified under the Phase-7 gold-only tail definition (min_comp/pct_missed), with the numbers stated."
    - "No new F1 math: every score flows through transarc_error_analysis.calc_metrics via the imported component_suite / rq2_trivial_baselines functions; stdlib-only imports."
  artifacts:
    - path: "src/bias/metric_fitness.py"
      provides: "Standalone numeric fitness scorecard (separation/validity/stability/degeneracy per column, both levels) + data-derived verdict"
      contains: "separation"
    - path: "reports/METRIC_FITNESS.csv"
      provides: "One row per (level × column) with the four axis scores"
      contains: "separation"
    - path: "reports/METRIC_FITNESS.md"
      provides: "Scorecard narrative + data-derived verdict + quantified artemis tail finding"
      contains: "verdict"
  key_links:
    - from: "metric_fitness.py separation axis"
      to: "rq2_trivial_baselines trivial floor (Random/Top-3) vs the 3 real systems"
      via: "trivial-vs-real margin per column (no new baselines)"
      pattern: "baseline_random_same_size|baseline_majority_k"
    - from: "metric_fitness.py validity axis"
      to: "rq2_trivial_baselines.compute_oracle_f1 / compute_random_f1 anchors"
      via: "oracle-ceiling headroom bound per column"
      pattern: "compute_oracle_f1|compute_random_f1"
    - from: "metric_fitness.py column scores"
      to: "component_suite.component_suite() / run_level outputs"
      via: "reads suite columns per system/level/project (calc_metrics only)"
      pattern: "component_suite"
    - from: "metric_fitness.py verdict + SC4"
      to: "gold-only tail (min_comp/pct_missed) artemis abandonment"
      via: "data-derived headline/diagnostic split + quantified tail finding"
      pattern: "min_comp|pct_missed|verdict"
---

<objective>
Deliver CMP-04 — the metric fitness scorecard. Build a NEW standalone
`src/bias/metric_fitness.py` (mirroring the `check_component_suite.py` script shape:
sys.path.insert to src/lib + src/bias, per-(level/column) loop, OK/WARN style lines, exit
code) that scores each candidate suite column on FOUR axes and emits a numeric scorecard +
a DATA-DERIVED verdict:

  Candidate columns (D-05): micro, macro, gap, min_comp, pct_missed  (gold_gini = descriptor only)
  Axes (D-05, formulas at executor discretion D-13):
    separation  — does the column rank the THREE REAL paper systems (swattr/transarc,
                  s20linker, artemis) clearly above the rq2 TRIVIAL FLOOR (Random + Top-3,
                  reused verbatim — D-01/D-03)? Report the margin (real vs trivial).
    validity    — does the column sit within / move coherently against the rq2 oracle ceiling
                  (compute_oracle_f1) and random floor (compute_random_f1)? Report headroom.
    stability   — cross-project variance of the column across the 5 projects (lower = more
                  stable). Report the variance statistic.
    degeneracy  — does the column saturate / go insensitive (e.g. always ≈1, always ≈0,
                  always identical across systems)? Report a saturation/spread test.

The verdict is DATA-DERIVED (D-06/D-07): headline vs diagnostic emerges from the axis scores,
NOT from the milestone's expected answer. The expected verdict (headline = macro + tail;
diagnostic = micro/gap) is the HYPOTHESIS UNDER TEST — if the numbers contradict it (e.g.
micro ≈ macro per the Phase-7 reconciliation), the report SAYS SO. The SC4 artemis long-tail
finding is reproduced + quantified under the Phase-7 gold-only tail definition.

Purpose: this is SC2 + SC3 + SC4 — a numeric scorecard on four axes, a verdict stated WITH
supporting numbers and hardened by the real multi-system set (not new synthetic baselines),
and the quantified artemis tail finding. Output: the scorecard script + METRIC_FITNESS.{md,csv}.

Discovery level: 0 (stdlib-only; reuses component_suite + rq2_trivial_baselines + calc_metrics;
copies the check_component_suite.py standalone shape — no new dependencies, no new F1 math).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-multi-system-comparison-fitness-validation/08-CONTEXT.md
@.planning/milestones/v1.2-ROADMAP.md
@.planning/milestones/v1.2-REQUIREMENTS.md
@CLAUDE.md

This plan depends on 08-01 (artemis source pinned → the scorecard's artemis column inputs are
reproducible). It is independent of 08-02 at the file level (this plan creates new files:
metric_fitness.py + METRIC_FITNESS.*; 08-02 touches COMPONENT_SUITE.*), so both Wave-2 plans
run in parallel. The scorecard reads suite COLUMNS via the imported suite functions; it does
not need 08-02's hardened markdown.

Project rules enforced: stdlib-only; reuse `calc_metrics` ONLY (the scorecard imports the
suite + oracle functions, which call calc_metrics — it adds NO new F1 formula); no
benchmark-derived word lists; non-destructive (new artifacts; nothing deleted); depth-2
`src/bias/x.py` + `sys.path.insert` to src/lib import pattern preserved.

<interfaces>
<!-- The exact functions the scorecard imports + reuses. NO reimplementation. -->
src/bias/check_component_suite.py (COPY THIS SHAPE):
  HERE=.../src/bias; sys.path.insert src/lib then src/bias; import component_suite,
  rq2_trivial_baselines; per-loop with OK/SKIP/FAIL-style lines; failures counter; sys.exit.
  _oracle_file_inputs(proj) -> (enrolled file-level gold, file_to_comps)  # reuse to rebuild
                                                                          # file-level inputs

src/bias/component_suite.py:
  component_suite(gold_sc, result_sc) -> {micro,macro,gap,min_comp,pct_missed,gold_gini}
  _model_inputs(proj) -> (gold, collapse) ; _code_inputs(proj) -> (gold, collapse)
  SYSTEMS = [(key,label,model_loader,code_loader)...]  # the 3 REAL systems
  LEVELS = {"sad-model":(...), "sad-code":(...)} ; run_level(level, projects)
  SUITE_COLS = ["micro","macro","gap","min_comp","pct_missed","gold_gini"]
  PROJECTS, calc_metrics, normalize_path (via transarc_error_analysis)

src/bias/rq2_trivial_baselines.py  (TRIVIAL FLOOR + ORACLE ANCHORS — reuse as-is, D-01):
  baseline_random_same_size, baseline_majority_k  (imported from stupid_baseline_analysis)
  per_component_macro_f1(gold_sad_code, result_sad_code, file_to_comps) -> float
  compute_random_f1, compute_oracle_f1            (NDG anchors, validity axis)
  random.seed(42) is set at import for reproducibility (Random baseline determinism)
  measure(...) and run_project(...) show HOW the trivial floor + anchors are built per project.

transarc_error_analysis: PROJECTS, calc_metrics (the SOLE F1 primitive), the loaders
  (load_code_model_files, load_model_element_names, enroll_gold_standard, load_gs_sam_code_raw,
   load_gs_sad_code_enrolled, load_gs_sam_code_maps, load_gs_sad_sam_maps) — reuse via the
  rq2 helpers / check_component_suite._oracle_file_inputs, do NOT re-derive metric math.

Trivial-floor column values: the rq2 floor is computed at SAD-CODE (its enrolled gold). To
score the suite COLUMNS for Random/Top-3, collapse each trivial baseline's (sentence, file)
links via component_suite._code_inputs(proj)'s collapse() into (sentence, component) pairs and
run component_suite() on them — exactly how a real system's sad-code links are scored. This
reuses the suite math for the floor too (no new formula). For sad-model, the rq2 floor is
file-level (sad-code) only; scope the separation axis at sad-model accordingly (state the
scoping in the report — D-08 present-scope honesty).

PROJECTS: mediastore, teastore, teammates, bigbluebutton, jabref. All 3 roots present today.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author metric_fitness.py — score each suite column on the four axes (both levels)</name>
  <files>src/bias/metric_fitness.py</files>
  <behavior>
    - Reuse only: component_suite.component_suite/_code_inputs/_model_inputs/run_level/SYSTEMS/LEVELS,
      rq2_trivial_baselines.baseline_random_same_size/baseline_majority_k/per_component_macro_f1/
      compute_random_f1/compute_oracle_f1, transarc_error_analysis.calc_metrics/PROJECTS. NO new F1 math.
    - Separation: per column, compute the margin between the mean column value over the 3 real
      systems and the mean over the trivial floor (Random + Top-3). The two trivial baselines'
      suite-column values are produced by collapsing their (sentence,file) links through the
      suite's collapse() and running component_suite() — the same path real systems take.
    - Validity: per column, bound the column against the rq2 oracle/random anchors
      (compute_oracle_f1 high, compute_random_f1 low) — report headroom (does real sit ≤ oracle
      and > random, and move with quality). For non-F1-scaled columns (gap, pct_missed) state
      the bound interpretation explicitly rather than forcing an F1 comparison.
    - Stability: per column, cross-project variance (or stdev) of the column over the 5 projects
      (per system or pooled — executor's documented choice); lower = more stable.
    - Degeneracy: per column, a saturation/spread test (e.g. column nearly constant across
      systems, or pinned at ≈0/≈1) flagged numerically.
    - Output: reports/METRIC_FITNESS.csv (one row per level × column with the four axis scores,
      4-dp like the suite) + reports/METRIC_FITNESS.md (per-axis tables + the derived verdict).
    - random.seed(42) (or reuse rq2's seeding) so the Random floor is reproducible.
  </behavior>
  <read_first>
    - src/bias/check_component_suite.py (the standalone-script structure to copy: sys.path.insert order, imports of component_suite + rq2_trivial_baselines + transarc_error_analysis loaders, _oracle_file_inputs to rebuild file-level inputs, per-loop OK/SKIP lines, failures/exit pattern)
    - src/bias/component_suite.py (component_suite, _code_inputs, _model_inputs, run_level, SYSTEMS, LEVELS, SUITE_COLS, write_csv style, _gini; the collapse() returned by _code_inputs is what converts a trivial baseline's (sentence,file) links into (sentence,component) pairs)
    - src/bias/rq2_trivial_baselines.py (baseline_random_same_size + baseline_majority_k signatures; per_component_macro_f1; compute_random_f1/compute_oracle_f1 anchor construction in run_project lines ~178-223 — copy HOW the floor + anchors are built per project, including random.seed(42) and target_size=len(gs_sad_code))
    - src/bias/stupid_baseline_analysis.py (baseline_random_same_size(gold_sents, all_code_files, target_size) and baseline_majority_k(gold_sents, gs_sam_code_enrolled, code_model, k=1) exact signatures — line ~208/~223)
    - reports/COMPONENT_SUITE_sad-code.csv, reports/COMPONENT_SUITE_sad-model.csv (the real-system column values the separation/stability axes consume)
    - .planning/phases/08-multi-system-comparison-fitness-validation/08-CONTEXT.md (D-01 no new baselines; D-02 real set = 3 paper systems; D-03 separation = trivial-vs-real margin; D-05 four axes + candidate columns; D-13 numeric rubric is discretion; D-09 both levels; D-11 universe split frozen)
    - CLAUDE.md (stdlib-only; reuse calc_metrics only; depth-2 + sys.path.insert)
  </read_first>
  <action>
    Create `src/bias/metric_fitness.py` as a standalone stdlib script following
    `check_component_suite.py`'s structure (`HERE = .../src/bias`; `sys.path.insert` to src/lib
    then src/bias; `import component_suite`, `import rq2_trivial_baselines`; import the needed
    loaders from `transarc_error_analysis`). Define the candidate columns
    (`micro, macro, gap, min_comp, pct_missed`; `gold_gini` carried as a descriptor) and the
    two levels from `component_suite.LEVELS`. For the REAL systems, read the suite columns per
    system/level/project by calling `component_suite.run_level(level, PROJECTS)` (or
    `component_suite.component_suite` directly via the per-level inputs) — do NOT recompute F1.
    For the TRIVIAL FLOOR, build Random and Top-3 per project EXACTLY as
    `rq2_trivial_baselines.run_project` does (`random.seed(42)`,
    `target_size = len(load_gs_sad_code_enrolled(proj, code_model))`,
    `baseline_random_same_size(gold_sents, code_model, target_size)` and
    `baseline_majority_k(gold_sents, gs_sam_code_enrolled, code_model, k=3)`), then SCORE the
    floor on the suite columns by collapsing each baseline's (sentence,file) links through the
    sad-code `_code_inputs(proj)` collapse() and running `component_suite.component_suite(gold,
    collapsed)` — so the floor and the real systems are scored by the identical suite math
    (NO new formula). Compute the four axes (separation/validity/stability/degeneracy) per
    column with a numeric rubric of your discretion (D-13) — but separation MUST be the
    real-vs-trivial margin (D-03), validity MUST bound against `compute_oracle_f1` /
    `compute_random_f1` (reused), stability MUST be a cross-project variance statistic, and
    degeneracy MUST be a saturation/insensitivity test. Write `reports/METRIC_FITNESS.csv`
    (one row per level × column, the four axis scores, 4-dp) and `reports/METRIC_FITNESS.md`
    (per-axis tables). Defer the actual verdict-derivation logic to Task 2's run, but the
    script MUST contain a function that derives the verdict FROM the computed axis scores (not
    a hardcoded string). Use the standardized `WARNING:` skip-with-notice for any absent root
    (reuse component_suite._warn_skip or the same format). Add no new F1 math; do not edit
    component_suite.py or rq2_trivial_baselines.py.
  </action>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('src/bias/metric_fitness.py').read())"` succeeds.
    - stdlib-only + project modules: `grep -nE "^\s*import |^\s*from " src/bias/metric_fitness.py`
      shows only stdlib (csv, sys, math/statistics, random, pathlib, collections, argparse, tempfile)
      + component_suite / rq2_trivial_baselines / transarc_error_analysis (no third-party).
    - Reuses the floor + anchors, not new baselines: grep shows
      `baseline_random_same_size`, `baseline_majority_k`, `compute_oracle_f1`,
      `compute_random_f1`, and `component_suite` are all referenced; NO new `def ..._f1(` that
      reimplements precision/recall (the only F1 path is calc_metrics via the imports).
    - The four axes are named in the source: `grep -nE "separation|validity|stability|degeneracy"
      src/bias/metric_fitness.py` matches all four.
    - The candidate columns are scored (not just suite-dumped): grep shows the column set
      `micro|macro|gap|min_comp|pct_missed` referenced as the scored columns.
    - A verdict-derivation function exists that consumes computed scores (grep for a
      `verdict`/`derive` function name) — not a hardcoded headline/diagnostic string literal as
      the sole verdict.
    - No new F1 math: `grep -c "calc_metrics" src/bias/metric_fitness.py` may be 0 (it is called
      transitively via the imported suite/oracle functions); there is NO local precision/recall
      reimplementation (`grep -nE "tp *=|/ *\(tp" src/bias/metric_fitness.py` returns nothing).
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && python3 -c "import ast; ast.parse(open('src/bias/metric_fitness.py').read())" && grep -qE "separation" src/bias/metric_fitness.py && grep -qE "validity" src/bias/metric_fitness.py && grep -qE "stability" src/bias/metric_fitness.py && grep -qE "degeneracy" src/bias/metric_fitness.py && grep -q "baseline_random_same_size" src/bias/metric_fitness.py && grep -q "baseline_majority_k" src/bias/metric_fitness.py && grep -q "compute_oracle_f1" src/bias/metric_fitness.py && grep -q "compute_random_f1" src/bias/metric_fitness.py && grep -q "component_suite" src/bias/metric_fitness.py && test -z "$(grep -nE 'tp *=|/ *\(tp' src/bias/metric_fitness.py)" && echo METRIC_FITNESS_AUTHORED</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Run the scorecard; emit the data-derived verdict + quantified artemis tail finding</name>
  <files>reports/METRIC_FITNESS.md, reports/METRIC_FITNESS.csv</files>
  <read_first>
    - src/bias/metric_fitness.py (the script from Task 1 — run it, confirm the verdict is derived from the computed axis scores)
    - reports/COMPONENT_SUITE_sad-code.csv, reports/COMPONENT_SUITE_sad-model.csv (the gold-only tail columns min_comp/pct_missed the SC4 finding quantifies: artemis AVG min_comp 0.3455 sad-code / 0.2788 sad-model; pct_missed 0.0533 / 0.0832; min_comp=0 on bbb & jabref both levels + teammates sad-model)
    - .planning/phases/08-multi-system-comparison-fitness-validation/08-CONTEXT.md (D-06 verdict data-derived; D-07 honesty — do NOT oversell micro-vs-macro, state contradictions explicitly; D-11 gold-only tail definition for SC4)
    - .planning/milestones/v1.2-ROADMAP.md (Phase 8 SC3 verdict-with-numbers; SC4 artemis tail reproduced; Notes/Risks honesty constraint)
  </read_first>
  <action>
    Run `python3 src/bias/metric_fitness.py` and make it pass (exit 0), writing
    `reports/METRIC_FITNESS.csv` (one row per level × column, the four axis scores) and
    `reports/METRIC_FITNESS.md`. The markdown MUST contain: (a) the per-axis scorecard tables at
    both levels; (b) a ONE-LINE verdict that is DERIVED from the computed axis scores (the
    verdict-derivation function from Task 1 selects headline vs diagnostic columns FROM the
    numbers — e.g. columns that separate real-from-trivial AND stay stable AND don't degenerate
    rank headline; columns that fail separation or saturate rank diagnostic); (c) the SC4
    artemis long-tail-abandonment finding reproduced + QUANTIFIED under the gold-only tail
    definition (state artemis's AVG min_comp / pct_missed at both levels and the projects where
    artemis scores min_comp=0 on real gold components), drawn from the suite's gold-only tail
    columns. CRITICAL honesty constraint (D-06/D-07): the verdict is whatever the numbers rank
    out — if the scores show micro ≈ macro (aggregation second-order, per the Phase-7
    reconciliation) or otherwise contradict the milestone's expected "headline=macro+tail,
    diagnostic=micro/gap", the report must SAY SO explicitly rather than restate the expected
    answer. Do NOT hand-edit METRIC_FITNESS.csv to force a clean verdict and do NOT relax the
    axis rubric to manufacture separation. After the run, confirm determinism: a second run
    produces a byte-identical CSV (Random floor is seeded). Cross-check that the artemis tail
    numbers in the report match the committed COMPONENT_SUITE_<level>.csv gold-only columns.
  </action>
  <acceptance_criteria>
    - `python3 src/bias/metric_fitness.py` exits 0 and writes both `reports/METRIC_FITNESS.csv`
      and `reports/METRIC_FITNESS.md`.
    - The CSV has a row per scored column at each level (≥ 5 columns × 2 levels = ≥ 10 data
      rows) with the four axis scores; header names the four axes
      (`grep -niE "separation|validity|stability|degeneracy" reports/METRIC_FITNESS.csv`).
    - The markdown contains a one-line verdict and the four-axis tables:
      `grep -niE "verdict" reports/METRIC_FITNESS.md` matches and the four axis names appear.
    - SC4 reproduced + quantified: `grep -niE "artemis" reports/METRIC_FITNESS.md` matches AND
      the report states artemis's gold-only tail numbers (min_comp / pct_missed) — the cited
      artemis AVG min_comp/pct_missed values match the committed COMPONENT_SUITE_<level>.csv
      gold-only columns (verify by reading both).
    - Honesty: the report's verdict is consistent with the CSV axis scores (the headline/
      diagnostic split it states matches which columns actually scored best/worst on the axes —
      verify the verdict columns are the ones with the strongest separation+stability in the CSV,
      not a restatement of the expected answer regardless of numbers).
    - Determinism: a second `python3 src/bias/metric_fitness.py` run yields a byte-identical
      `reports/METRIC_FITNESS.csv` (Random floor seeded).
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && python3 src/bias/metric_fitness.py >/dev/null 2>/tmp/mf_run1.log && test -s reports/METRIC_FITNESS.csv && test -s reports/METRIC_FITNESS.md && test "$(grep -c , reports/METRIC_FITNESS.csv)" -ge 11 && grep -niE "separation|validity|stability|degeneracy" reports/METRIC_FITNESS.csv && grep -niE "verdict" reports/METRIC_FITNESS.md && grep -niE "artemis" reports/METRIC_FITNESS.md && grep -niE "min_comp|pct_missed" reports/METRIC_FITNESS.md && cp reports/METRIC_FITNESS.csv /tmp/mf_run1.csv && python3 src/bias/metric_fitness.py >/dev/null 2>&1 && diff -q reports/METRIC_FITNESS.csv /tmp/mf_run1.csv && echo SCORECARD_GREEN_AND_DETERMINISTIC</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scorecard run → reports/ | The scorecard writes only its own new artifacts (METRIC_FITNESS.*); it must not corrupt the COMPONENT_SUITE.* it reads. |
| computed scores → verdict | The verdict must be derived from the numbers; a verdict passing via a relaxed rubric or hidden assumption would misrepresent fitness. |
| real systems vs trivial floor | The trivial floor must be the rq2 Random/Top-3 reused as-is (no new synthetic baselines); fabricating a floor would game the separation axis. |
| present vs absent external roots | Absent roots skip-with-notice; reproducibility scoped to present systems. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | scorecard overwriting / corrupting COMPONENT_SUITE.* it reads | mitigate | The scorecard writes ONLY METRIC_FITNESS.{md,csv}; acceptance asserts those are the only new files and reuses the suite read-only. |
| T-08-03-02 | Spoofing | verdict passing via a relaxed rubric / restating the expected answer | mitigate | Verdict is derived by a function from the computed CSV scores; acceptance cross-checks the stated headline/diagnostic split against the CSV's strongest-axis columns and forbids hand-editing the CSV. |
| T-08-03-03 | Tampering | inventing a new baseline to inflate separation (violates D-01) | mitigate | Separation reuses rq2 baseline_random_same_size/baseline_majority_k verbatim; acceptance greps for them and forbids new precision/recall reimplementation. |
| T-08-03-04 | Repudiation | overselling micro-vs-macro contrary to the reconciled finding | mitigate | D-07 honesty constraint encoded: if scores show micro≈macro/contradiction, the report must say so; verdict must match the numbers. |
| T-08-03-SC | Tampering | dependency supply chain | accept | stdlib-only, no installs; not applicable. |
</threat_model>

<verification>
- `python3 src/bias/metric_fitness.py` exits 0, writes METRIC_FITNESS.{md,csv}, deterministic
  on a second run (Random floor seeded).
- The CSV scores each candidate column (micro/macro/gap/min_comp/pct_missed) on the four axes
  (separation/validity/stability/degeneracy) at both levels.
- Separation reuses the rq2 trivial floor (Random/Top-3) vs the 3 real paper systems — no new
  synthetic baselines; validity bounds against compute_oracle_f1/compute_random_f1.
- The markdown verdict is data-derived (matches the CSV axis scores) and honest about
  micro-vs-macro (states any contradiction with the expected verdict).
- SC4 artemis long-tail abandonment reproduced + quantified under the gold-only tail
  definition; cited numbers match COMPONENT_SUITE_<level>.csv.
- stdlib-only; no new F1 math (only calc_metrics via the imported suite/oracle functions).
</verification>

<success_criteria>
CMP-04 satisfied: a standalone numeric scorecard scores candidate suite columns on four
fitness axes at both levels, hardened by the real multi-system set (rq2 trivial floor as-is,
3 paper systems as the real comparators — no new synthetic baselines), and states a
DATA-DERIVED verdict (headline vs diagnostic) WITH supporting numbers; the artemis long-tail-
abandonment finding is reproduced + quantified under the gold-only tail definition. Maps to
ROADMAP Phase 8 SC2 (scorecard on separation/validity/stability/degeneracy), SC3 (hardened
beyond random/top-3 via the real systems + verdict with numbers), and SC4 (artemis tail
reproduced + quantified).
</success_criteria>

<output>
Create `.planning/phases/08-multi-system-comparison-fitness-validation/08-03-SUMMARY.md` when done.
</output>
