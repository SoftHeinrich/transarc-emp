---
phase: 01-reorganize
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/propagation/sad_sam_actual_contribution.py
  - src/propagation/sad_sam_tp_gain_analysis.py
  - src/propagation/sam_code_cascade_analysis.py
  - src/evaluation/s12c_sadcode_comparison.py
  - src/evaluation/evaluation_critique.py
  - src/evaluation/creative_metrics_analysis.py
  - src/evaluation/holistic_metrics_analysis.py
  - src/evaluation/extreme_baseline_analysis.py
  - src/evaluation/stupid_baseline_analysis.py
  - src/annotation/doc_structure_analysis_v2.py
  - src/annotation/reverse_engineer_convention.py
  - src/transarc/
  - src/bias/
  - archive/
  - reports/ANNOTATION_CONVENTION.md
autonomous: true
requirements:
  - SCOPE-01
  - SCOPE-02
  - SCOPE-03
  - SCOPE-04
  - SCOPE-05
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04

must_haves:
  truths:
    - "src/transarc/ contains the 3 propagation scripts plus s12c_sadcode_comparison.py"
    - "src/bias/ contains the original 4 bias scripts plus the 5 moved evaluation scripts"
    - "src/lib/ is unchanged: still holds transarc_error_analysis.py and new_metrics_analysis.py"
    - "archive/ contains both annotation scripts and ANNOTATION_CONVENTION.md"
    - "src/evaluation/, src/propagation/, src/annotation/ no longer exist (empty dirs removed)"
    - "Every moved script keeps depth src/<pillar>/script.py so parent.parent/lib resolves to src/lib"
    - "No retained script imports an archived path; no retained report links archived work"
    - "reports/ stays flat (no per-pillar subdirs); S12C_VS_TRANSARC.csv stays in reports/"
    - "No __pycache__ dirs or .pyc files remain under src/"
  artifacts:
    - path: "src/transarc/sad_sam_actual_contribution.py"
      provides: "Pillar 1 SAD-SAM actual-contribution analysis (moved from propagation)"
    - path: "src/transarc/sad_sam_tp_gain_analysis.py"
      provides: "Pillar 1 SAD-SAM TP-gain analysis (moved from propagation)"
    - path: "src/transarc/sam_code_cascade_analysis.py"
      provides: "Pillar 1 SAM-CODE cascade analysis (moved from propagation)"
    - path: "src/transarc/s12c_sadcode_comparison.py"
      provides: "Pillar 1 S12C/S12E-vs-TransArc comparison (moved from evaluation)"
    - path: "src/bias/evaluation_critique.py"
      provides: "Pillar 2 evaluation critique (moved from evaluation)"
    - path: "src/bias/creative_metrics_analysis.py"
      provides: "Pillar 2 creative metrics (moved from evaluation)"
    - path: "src/bias/holistic_metrics_analysis.py"
      provides: "Pillar 2 holistic metrics (moved from evaluation)"
    - path: "src/bias/extreme_baseline_analysis.py"
      provides: "Pillar 2 extreme baseline (moved from evaluation)"
    - path: "src/bias/stupid_baseline_analysis.py"
      provides: "Pillar 2 trivial/stupid baseline (moved from evaluation)"
    - path: "archive/doc_structure_analysis_v2.py"
      provides: "Archived out-of-scope annotation script"
    - path: "archive/reverse_engineer_convention.py"
      provides: "Archived out-of-scope annotation script (imports doc_structure_analysis_v2)"
    - path: "archive/ANNOTATION_CONVENTION.md"
      provides: "Archived gray report (output of reverse_engineer_convention)"
  key_links:
    - from: "src/transarc/*.py and src/bias/*.py (moved)"
      to: "src/lib/transarc_error_analysis.py"
      via: "sys.path.insert(0, str(Path(__file__).resolve().parent.parent / 'lib'))"
      pattern: "parent\\.parent / .lib."
    - from: "src/transarc/s12c_sadcode_comparison.py + src/bias/{creative,extreme}*.py"
      to: "src/lib/new_metrics_analysis.py"
      via: "imported as a library (new_metrics_analysis stays in lib)"
      pattern: "new_metrics_analysis"
---

<objective>
Restructure the `src/` tree into two explicit research pillars and move
out-of-scope work into `archive/`. This is a MOVE-ONLY phase: pure `git mv`
reorganization with ZERO edits to any `.py` content and NO renames of shared
modules.

Purpose: Make the two pillars of the project (Pillar 1 = TransArc empirical
study, Pillar 2 = Benchmark bias analysis) structurally explicit so Phase 2
(verify) and Phase 3 (paper) can reason about each pillar independently. Archival
is non-destructive.

Output: `src/transarc/` (Pillar 1: 3 propagation scripts + s12c), an expanded
`src/bias/` (Pillar 2: original 4 bias scripts + 5 moved evaluation scripts), an
UNCHANGED `src/lib/` shared library, and an `archive/` holding the two annotation
scripts and the gray annotation report. The now-empty `src/evaluation/`,
`src/propagation/`, and `src/annotation/` directories are removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reorganize/01-CONTEXT.md

<environment_caveat>
CRITICAL — this repo lives on a hostshare mount with a FLAKY git stat-cache and
a sandbox that blocks the mount.

RULES for EVERY bash command in this plan:
1. Run ALL bash commands with the sandbox DISABLED
   (dangerouslyDisableSandbox: true). The sandbox blocks the hostshare mount and
   produces empty/garbled output. This is confirmed for this environment — every
   verify command below assumes the sandbox is off.
2. Before inspecting git state, ALWAYS run `git update-index -q --refresh` first
   (ignore its exit code) to settle the stat-cache. Plain `git status` can
   transiently report tracked files as "deleted".
3. For ground truth about what exists, prefer `git ls-files`, `git ls-tree HEAD`,
   and `git hash-object` over porcelain `git status` and over shell `[ -f ]`
   tests. If a porcelain check disagrees with `git ls-files`, trust `git ls-files`.
4. If a command returns empty output unexpectedly, re-run it once before
   concluding anything is missing.
</environment_caveat>

<current_state>
Verified ground truth at planning time (via `git ls-files` + grep, sandbox off):

TRACKED move-sources under src/ (use `git mv`):
- src/propagation/sad_sam_actual_contribution.py
- src/propagation/sad_sam_tp_gain_analysis.py
- src/propagation/sam_code_cascade_analysis.py
- src/evaluation/evaluation_critique.py
- src/evaluation/creative_metrics_analysis.py
- src/evaluation/holistic_metrics_analysis.py
- src/evaluation/extreme_baseline_analysis.py
- src/evaluation/stupid_baseline_analysis.py
- src/annotation/doc_structure_analysis_v2.py
- src/annotation/reverse_engineer_convention.py

UNTRACKED move-source (git mv will FAIL — use plain `mv` then `git add`):
- src/evaluation/s12c_sadcode_comparison.py

STAY PUT — do NOT move:
- src/bias/benchmark_bias_study.py
- src/bias/enrollment_bias_analysis.py
- src/bias/enrollment_distortion_analysis.py
- src/bias/sam_code_distribution_analysis.py
- src/lib/transarc_error_analysis.py
- src/lib/new_metrics_analysis.py   (STAYS in lib — imported as a library, NOT a
  standalone Pillar-2 script; resolves the STATE.md SCOPE-05 open question)

There are NO __init__.py files anywhere under src/ (packages are plain folders).
Destination dir src/transarc/ does NOT yet exist (mkdir it). archive/ exists.

There ARE __pycache__ dirs under EVERY src subfolder
(src/evaluation, src/lib, src/propagation, src/bias, src/annotation), 16 .pyc
files total — these are gitignored junk. Delete src/evaluation, src/propagation,
src/annotation __pycache__ as part of removing those dirs.

Report to archive (TRACKED): reports/ANNOTATION_CONVENTION.md
Leave alone in reports/: S12C_VS_TRANSARC.csv (output, stays flat) and the stale
editor lock `reports/.~lock.S12C_VS_TRANSARC.csv#` (untracked, do not commit).
</current_state>

<depth_invariant>
THE critical constraint. EVERY moved script must remain at depth
`src/<pillar>/script.py` so that
`sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "lib"))`
still resolves to `src/lib`.

Confirmed at planning time: all 10 tracked analysis scripts AND s12c use that
exact line. All moves in this plan are depth-2 → depth-2 (evaluation/propagation
into transarc/bias), so the invariant holds with ZERO code edits. The two
annotation scripts move to archive/ which is depth-1, but they are NOT imported
by anything retained and are out of the import graph entirely, so their own path
resolution is irrelevant.

NEVER edit any `.py` content to "fix" a path. Per the locked layout, no fix is
needed. If you ever find a moved retained script whose `parent.parent/lib` would
NOT resolve to src/lib, STOP and flag the orchestrator — do not edit.
</depth_invariant>

<verified_facts>
- s12c imports from BOTH transarc_error_analysis AND (transitively, via
  baselines/metrics) new_metrics_analysis — both stay in src/lib, so imports are
  intact post-move.
- s12c writes to an ABSOLUTE path
  `/mnt/hostshare/ardoco-home/transarc-emp/reports/S12C_VS_TRANSARC.csv`
  → reports/ MUST stay flat; the script is unaffected by its own move.
- reverse_engineer_convention.py imports doc_structure_analysis_v2 and writes
  reports/ANNOTATION_CONVENTION.md → all three archived together (success
  criteria 3 & 4).
- grep at planning time: ZERO references to `annotation/`,
  `doc_structure_analysis`, `reverse_engineer_convention`, or
  `ANNOTATION_CONVENTION` in any RETAINED src/ script or retained reports/*.md.
  (The only hits are inside the annotation scripts themselves and benign prose
  like the word "annotation" in holistic_metrics_analysis table text — NOT an
  import or a path.) Success criterion 5 already holds; Task 3 RE-VERIFIES it.
- SWATTR FP-filter work is already in archive/ and is referenced by nothing
  retained (ARCH-03 already satisfied; Task 3 confirms).
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble Pillar 1 (src/transarc/)</name>
  <files>src/transarc/sad_sam_actual_contribution.py, src/transarc/sad_sam_tp_gain_analysis.py, src/transarc/sam_code_cascade_analysis.py, src/transarc/s12c_sadcode_comparison.py</files>
  <action>
Run all commands with the sandbox DISABLED. Satisfies SCOPE-01, SCOPE-02.

1. Settle the stat-cache: `git update-index -q --refresh` (ignore exit code).
2. Create the destination: `mkdir -p src/transarc`.
3. `git mv` the 3 TRACKED propagation scripts (history-preserving):
   - `git mv src/propagation/sad_sam_actual_contribution.py src/transarc/sad_sam_actual_contribution.py`
   - `git mv src/propagation/sad_sam_tp_gain_analysis.py     src/transarc/sad_sam_tp_gain_analysis.py`
   - `git mv src/propagation/sam_code_cascade_analysis.py    src/transarc/sam_code_cascade_analysis.py`
4. The s12c script is UNTRACKED, so `git mv` fails on it. Move + stage:
   - `mv src/evaluation/s12c_sadcode_comparison.py src/transarc/s12c_sadcode_comparison.py`
   - `git add src/transarc/s12c_sadcode_comparison.py`
   Do NOT touch reports/S12C_VS_TRANSARC.csv or the stale `reports/.~lock...#`.
5. DO NOT edit any file content. The depth-2 invariant holds (parent.parent/lib
   still → src/lib).
6. Commit atomically:
   `git commit -m "refactor(01): assemble transarc pillar (src/transarc/)" -m "git mv 3 propagation scripts + s12c comparison into src/transarc/. Move-only, no content edits. SCOPE-01, SCOPE-02." -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"`
  </action>
  <verify>
    <automated>git update-index -q --refresh 2>/dev/null; test "$(git ls-files src/transarc | sort | tr '\n' '|')" = "src/transarc/s12c_sadcode_comparison.py|src/transarc/sad_sam_actual_contribution.py|src/transarc/sad_sam_tp_gain_analysis.py|src/transarc/sam_code_cascade_analysis.py|" && test -z "$(git ls-files src/propagation)" && grep -q "parent.parent / \"lib\"" src/transarc/s12c_sadcode_comparison.py && echo PILLAR1_OK</automated>
  </verify>
  <done>git ls-files src/transarc lists exactly the 4 expected scripts; git ls-files src/propagation is empty; each moved script still contains the parent.parent/lib line; commit created.</done>
</task>

<task type="auto">
  <name>Task 2: Expand Pillar 2 (src/bias/) and archive out-of-scope work</name>
  <files>src/bias/evaluation_critique.py, src/bias/creative_metrics_analysis.py, src/bias/holistic_metrics_analysis.py, src/bias/extreme_baseline_analysis.py, src/bias/stupid_baseline_analysis.py, archive/doc_structure_analysis_v2.py, archive/reverse_engineer_convention.py, archive/ANNOTATION_CONVENTION.md</files>
  <action>
Run all commands with the sandbox DISABLED.
Satisfies SCOPE-03, SCOPE-04, SCOPE-05, ARCH-01, ARCH-02.

1. `git update-index -q --refresh` (ignore exit code).
2. `git mv` the 5 evaluation scripts into the EXISTING src/bias/ (depth-2 → depth-2):
   - `git mv src/evaluation/evaluation_critique.py        src/bias/evaluation_critique.py`
   - `git mv src/evaluation/creative_metrics_analysis.py  src/bias/creative_metrics_analysis.py`
   - `git mv src/evaluation/holistic_metrics_analysis.py  src/bias/holistic_metrics_analysis.py`
   - `git mv src/evaluation/extreme_baseline_analysis.py  src/bias/extreme_baseline_analysis.py`
   - `git mv src/evaluation/stupid_baseline_analysis.py   src/bias/stupid_baseline_analysis.py`
   Do NOT move src/lib/new_metrics_analysis.py — it STAYS in lib (locked decision).
3. `git mv` the annotation pair + its report into archive/ (move them together —
   reverse_engineer_convention imports doc_structure_analysis_v2 and produces the
   report):
   - `git mv src/annotation/doc_structure_analysis_v2.py    archive/doc_structure_analysis_v2.py`
   - `git mv src/annotation/reverse_engineer_convention.py  archive/reverse_engineer_convention.py`
   - `git mv reports/ANNOTATION_CONVENTION.md               archive/ANNOTATION_CONVENTION.md`
4. Remove the now-empty source dirs and their gitignored __pycache__:
   - `rm -rf src/evaluation src/propagation src/annotation`
   (These hold only __pycache__/.pyc now; all tracked files were moved in Tasks
   1–2. `rm -rf` is safe here — confirm with the verify step that git tracks
   nothing under them.)
5. DO NOT edit any file content.
6. Commit atomically:
   `git commit -m "refactor(01): expand bias pillar + archive annotation work" -m "git mv 5 evaluation scripts into src/bias/; archive annotation scripts + ANNOTATION_CONVENTION.md; remove empty evaluation/propagation/annotation dirs. new_metrics_analysis.py stays in lib. SCOPE-03/04/05, ARCH-01/02." -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"`
  </action>
  <verify>
    <automated>git update-index -q --refresh 2>/dev/null; test "$(git ls-files src/bias | sort | tr '\n' '|')" = "src/bias/benchmark_bias_study.py|src/bias/creative_metrics_analysis.py|src/bias/enrollment_bias_analysis.py|src/bias/enrollment_distortion_analysis.py|src/bias/evaluation_critique.py|src/bias/extreme_baseline_analysis.py|src/bias/holistic_metrics_analysis.py|src/bias/sam_code_distribution_analysis.py|src/bias/stupid_baseline_analysis.py|" && test -z "$(git ls-files src/evaluation src/annotation)" && test -n "$(git ls-files src/lib/new_metrics_analysis.py)" && git ls-files archive | grep -q "archive/ANNOTATION_CONVENTION.md" && git ls-files archive | grep -q "archive/doc_structure_analysis_v2.py" && git ls-files archive | grep -q "archive/reverse_engineer_convention.py" && test ! -d src/evaluation && test ! -d src/propagation && test ! -d src/annotation && echo PILLAR2_OK</automated>
  </verify>
  <done>src/bias holds the 9 expected scripts; new_metrics_analysis.py still tracked under src/lib; the 3 annotation artifacts tracked under archive/; src/evaluation, src/propagation, src/annotation directories gone; commit created.</done>
</task>

<task type="auto">
  <name>Task 3: Static reference + structure verification (no edits)</name>
  <files>(verification only — no files modified)</files>
  <action>
Run all commands with the sandbox DISABLED. This task makes NO changes; it
statically proves success criteria 1–5 + ARCH-03/04 hold after Tasks 1–2. If any
check fails, STOP and report which criterion failed — do NOT edit code to fix it
(that would violate the move-only constraint; a failure means a move was wrong).

1. `git update-index -q --refresh` (ignore exit code).

2. No retained script imports an archived/out-of-scope path (ARCH-04, SC5):
   grep across all retained scripts (src/transarc, src/bias, src/lib) for import
   or path references to archived names. Expect ZERO matches:
   `grep -rnE "(import|from)\s+(doc_structure_analysis|reverse_engineer_convention)|annotation/|swattr|llm_" src/transarc src/bias src/lib`
   (Benign prose containing the word "annotation" in a markdown-style table
   string is NOT an import — only flag actual `import`/`from` statements or
   path-like `annotation/` strings. The regex above already restricts to those.)

3. No retained report links to archived work (ARCH-04, SC5):
   `grep -rln "ANNOTATION_CONVENTION\|doc_structure_analysis\|reverse_engineer_convention\|src/annotation" reports/`
   Expect ZERO matching files.

4. SWATTR work confirmed archived + unreferenced (ARCH-03):
   `git ls-files archive | grep -q swattr` returns success (still archived) AND
   step 2 already proved nothing retained imports swattr.

5. Depth-2 invariant holds for every retained moved script (REPRO-01 precondition):
   For each file in src/transarc/*.py and the 5 moved src/bias/*.py, confirm the
   parent.parent/lib line is present and that src/lib/transarc_error_analysis.py
   exists at the resolved target:
   `for f in src/transarc/*.py src/bias/evaluation_critique.py src/bias/creative_metrics_analysis.py src/bias/holistic_metrics_analysis.py src/bias/extreme_baseline_analysis.py src/bias/stupid_baseline_analysis.py; do grep -q 'parent.parent / "lib"' "$f" || echo "MISSING-PATH: $f"; done; test -f src/lib/transarc_error_analysis.py && echo LIB_TARGET_OK`

6. reports/ stayed flat (SC2): no per-pillar subdirs created.
   `find reports -mindepth 1 -type d` should print nothing.
   `git ls-files reports/S12C_VS_TRANSARC.csv` should be non-empty (CSV stayed).

7. Final tree sanity (SC1, SC3): print the retained src layout for the SUMMARY.
   `git ls-files src | sed 's#/[^/]*$##' | sort -u`

This task creates no commit (verification only).
  </action>
  <verify>
    <automated>git update-index -q --refresh 2>/dev/null; test -z "$(grep -rnE '(import|from)[[:space:]]+(doc_structure_analysis|reverse_engineer_convention)|annotation/|swattr|llm_' src/transarc src/bias src/lib)" && test -z "$(grep -rln 'ANNOTATION_CONVENTION\|doc_structure_analysis\|reverse_engineer_convention\|src/annotation' reports/)" && test -z "$(find reports -mindepth 1 -type d)" && test -n "$(git ls-files reports/S12C_VS_TRANSARC.csv)" && test -f src/lib/transarc_error_analysis.py && echo STATIC_CHECKS_OK</automated>
  </verify>
  <done>Zero archived-path imports in retained scripts; zero retained reports link archived work; SWATTR still archived; every moved retained script keeps the parent.parent/lib line with src/lib/transarc_error_analysis.py present; reports/ flat with the CSV intact. All 5 success criteria + ARCH-03/04 confirmed.</done>
</task>

</tasks>

<verification>
## Mapping to Phase 1 Success Criteria + Requirements

| Success Criterion (ROADMAP) | Proven by |
|---|---|
| 1. Clear Pillar 1 (transarc) + Pillar 2 (bias) groupings | Task 1 verify (src/transarc = 4 scripts), Task 2 verify (src/bias = 9 scripts), Task 3 step 7 |
| 2. s12c + S12C_VS_TRANSARC.csv co-located with Pillar 1 | Task 1 (s12c → src/transarc), Task 3 step 6 (CSV stays in flat reports/) |
| 3. src/annotation empty/gone, both annotation scripts in archive/ | Task 2 verify (annotation dir gone; archive/ holds both scripts) |
| 4. ANNOTATION_CONVENTION.md + gray reports in archive/ | Task 2 (report → archive/) |
| 5. No retained import of archived path; no retained report links archived work | Task 3 steps 2–3 |

| Requirement | Proven by |
|---|---|
| SCOPE-01 (TransArc study scripts grouped) | Task 1 |
| SCOPE-02 (s12c + CSV into Pillar 1) | Task 1 + Task 3 step 6 |
| SCOPE-03 (bias + evaluation_critique grouped) | Task 2 |
| SCOPE-04 (baselines folded into Pillar 2) | Task 2 (extreme + stupid baseline → src/bias) |
| SCOPE-05 (proposed-metrics folded; new_metrics stays in lib) | Task 2 (holistic + creative → bias; new_metrics_analysis stays in lib) |
| ARCH-01 (annotation scripts → archive/) | Task 2 |
| ARCH-02 (gray reports → archive/) | Task 2 |
| ARCH-03 (SWATTR stays archived, unreferenced) | Task 3 step 4 |
| ARCH-04 (no broken imports / dangling links) | Task 3 steps 2–3 |

Note: REPRO-01/02/03 (scripts actually RUN) are Phase 2. This phase only moves +
statically checks imports per the locked guardrail (no "run every script" tasks).
</verification>

<success_criteria>
- `git ls-files src/transarc` = exactly 4 scripts (3 propagation + s12c).
- `git ls-files src/bias` = exactly 9 scripts (4 original + 5 evaluation).
- `src/lib/new_metrics_analysis.py` and `src/lib/transarc_error_analysis.py` still tracked, unchanged.
- `archive/` holds doc_structure_analysis_v2.py, reverse_engineer_convention.py, ANNOTATION_CONVENTION.md.
- `src/evaluation`, `src/propagation`, `src/annotation` directories no longer exist; no __pycache__ remains under them.
- Zero archived-path imports in retained scripts; zero retained reports link archived work.
- reports/ is flat; S12C_VS_TRANSARC.csv retained.
- Two atomic `git mv` commits created (Task 1, Task 2), each ending with the Co-Authored-By trailer.
</success_criteria>

<output>
After completion, create
`.planning/phases/01-reorganize/01-01-SUMMARY.md` recording: the final two-pillar
src/ tree (from Task 3 step 7), the two commit hashes, confirmation that the
depth-2 import invariant held with zero code edits, and a note that REPRO-* (the
actual run checks) are deferred to Phase 2.
</output>
