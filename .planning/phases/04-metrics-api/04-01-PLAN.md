---
phase: 04-metrics-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/metrics_api.py
  - reports/metrics_sad-sam.csv
  - reports/metrics_sad-code.csv
  - writing/tables/metrics_sad-sam.tex
  - writing/tables/metrics_sad-code.tex
autonomous: true
requirements: [MTR-01, MTR-02, MTR-03, MTR-04, MTR-05]

must_haves:
  truths:
    - "Running `python3 src/lib/metrics_api.py --task sad-sam` exits 0 and computes metrics for all 5 benchmark projects (MTR-01)."
    - "Running `python3 src/lib/metrics_api.py --task sad-code` exits 0, applies gold-standard enrollment, and computes metrics for all 5 benchmark projects (MTR-02)."
    - "The metric set includes link/sentence/decision/component (sad-sam) and file/decision/component/weighted (sad-code) P/R/F1 plus alternative metrics (MCC/MAP/ACF1/NDG/HUS), computed only by importing existing functions — no reimplementation, no benchmark-derived word lists (MTR-03)."
    - "Each task writes a wide CSV to reports/metrics_<task>.csv with one row per project plus an Average row, openable in Excel, stdlib csv only (MTR-04)."
    - "Each task writes a ready-to-paste booktabs LaTeX table to writing/tables/metrics_<task>.tex (MTR-05)."
    - "A missing per-project results file produces a stderr warning and is skipped — no crash."
    - "sad-code Decision F1 and File F1 for jabref match the already-published values (File 0.943, Decision 0.394) since the same functions are reused."
  artifacts:
    - path: "src/lib/metrics_api.py"
      provides: "stdlib-only CLI computing the unified metric set for sad-sam and sad-code, emitting CSV + LaTeX"
      min_lines: 150
      contains: "argparse"
    - path: "reports/metrics_sad-sam.csv"
      provides: "wide sad-sam metrics, 5 project rows + Average row"
    - path: "reports/metrics_sad-code.csv"
      provides: "wide sad-code metrics, 5 project rows + Average row"
    - path: "writing/tables/metrics_sad-sam.tex"
      provides: "booktabs LaTeX table of sad-sam metrics"
      contains: "tabular"
    - path: "writing/tables/metrics_sad-code.tex"
      provides: "booktabs LaTeX table of sad-code metrics"
      contains: "tabular"
  key_links:
    - from: "src/lib/metrics_api.py"
      to: "transarc_error_analysis.calc_metrics / loaders / PROJECTS"
      via: "sys.path.insert(parent) + import"
      pattern: "from transarc_error_analysis import"
    - from: "src/lib/metrics_api.py"
      to: "evaluation_critique._compute_decision_f1 / _compute_component_f1 / _compute_weighted_f1"
      via: "sys.path.insert(parent.parent/'bias') + import (sad-code path ONLY)"
      pattern: "from evaluation_critique import"
    - from: "src/lib/metrics_api.py"
      to: "new_metrics_analysis.compute_mcc / compute_map / compute_acf1 / compute_ndg / compute_hus"
      via: "import"
      pattern: "from new_metrics_analysis import"
    - from: "src/lib/metrics_api.py"
      to: "generate_tables.render_table / write_table"
      via: "sys.path.insert(parent.parent/'paper') + import"
      pattern: "from generate_tables import"
---

<objective>
Create `src/lib/metrics_api.py`: a stdlib-only CLI that ingests TransArc-format results
for a sad-sam or sad-code run, computes the full unified metric set for ALL benchmark
projects by reusing existing functions (zero reimplementation), and emits both a wide CSV
(`reports/metrics_<task>.csv`) and a ready-to-paste booktabs LaTeX table
(`writing/tables/metrics_<task>.tex`).

Purpose: Give the project one reproducible metrics-reporting entry point (MTR-01..MTR-05)
that the Phase 5 consequences study consumes as evidence — and that keeps reported numbers
identical to the already-published reports because it calls the same primitives.

Output:
- `src/lib/metrics_api.py` (new)
- `reports/metrics_sad-sam.csv`, `reports/metrics_sad-code.csv` (generated)
- `writing/tables/metrics_sad-sam.tex`, `writing/tables/metrics_sad-code.tex` (generated)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-metrics-api/04-CONTEXT.md
@.planning/phases/04-metrics-api/04-PATTERNS.md

CONSTRAINTS:
- Stdlib only: csv, json, math, os, sys, argparse, collections, pathlib, dataclasses, re. NO third-party deps, NO requirements.txt edit.
- NO benchmark-derived word lists anywhere (workspace CLAUDE.md leakage rule). Stopwords only — and this file needs none.
- ZERO reimplementation of metric math. Every metric must come from an imported function. If an import is awkward, prefer import-as-is over reimplementing (CONTEXT Claude's Discretion).
- The sad-code granularity helpers (`_compute_decision_f1`, `_compute_component_f1`, `_compute_weighted_f1`) and ACF1/NDG/HUS-on-files are SAD-CODE ONLY. Do NOT wire any of them into the sad-sam path — they require enrollment maps (`raw_to_enrolled`, `enrolled_to_raw`, `file_to_comps`) that do not exist for sad-sam.
- All output paths are absolute or via `write_table` (which writes to `writing/tables/`). Define `REPORTS = Path("/mnt/hostshare/ardoco-home/transarc-emp/reports")` at module top per the hardcoded-absolute-path convention.

INTERFACES (exact signatures/return shapes verified from source — use directly, no exploration needed):

From src/lib/transarc_error_analysis.py:
  PROJECTS = ["mediastore", "teastore", "teammates", "bigbluebutton", "jabref"]
  BENCHMARK, RESULTS  # module-level Path constants
  calc_metrics(gold, result) -> (precision, recall, f1, tp, fp, fn)   # exact-set
  load_code_model_files(project) -> code_model_files
  enroll_gold_standard(gold, code_model_files)
  load_gs_sad_sam(project) -> set[(modelElementID, sentence)]          # NOTE order: (component, sentence)
  load_gs_sad_sam_maps(project) -> (sent_to_models: dict, model_to_sents: dict)
  load_gs_sam_code_maps(project, code_model_files) -> (model_to_files: dict, ...)  # element 0 = model_id -> set(files)
  load_gs_sad_code_enrolled(project, code_model_files) -> set[(sentence, code_path)]
  load_result_sad_sam_standalone(project) -> set[...]    # returns set() if file absent
  load_result_sad_code(project) -> set[(sentence, code_path)]  # returns set() if file absent
  load_text(project) -> dict   # keys = sentence ids
  load_model_element_names(project) -> dict  # modelElementID -> component name

From src/bias/evaluation_critique.py (SAD-CODE path ONLY):
  load_sad_code_raw_with_provenance(project) -> raw_entries
  enroll_with_provenance(raw_entries, code_model_files) -> (enrolled, raw_to_enrolled, enrolled_to_raw)
  load_sam_code_enrolled(project, code_model_files) -> set[(ae_id, file_path)]
  _compute_decision_f1(enrolled, result, raw_to_enrolled) -> {"tp","fp","fn","p","r","f1"}
  _compute_component_f1(enrolled, result, file_to_comps) -> {"tp","fp","fn","p","r","f1","gold_size","result_size"}
  _compute_weighted_f1(enrolled, result, enrolled_to_raw, raw_to_enrolled) -> {"p","r","f1","w_tp","w_fp","w_fn"}

From src/lib/new_metrics_analysis.py:
  compute_mcc(gold_links, result_links, all_sentences, all_components) -> {"mcc", "tp","fp","fn","tn", ...}
  compute_map(gold_links, ranked_predictions) -> dict   # ranked = [(sent, comp, conf), ...]; key for value: verify in file (e.g. "map")
  transarc_sad_sam_as_ranked(sad_sam_links) -> [(sent, comp, 0.5), ...]  # input is (comp, sent); it flips to (sent, comp)
  compute_acf1(gold_sad_code, result_sad_code, gold_sam_code_map) -> {"acf1", ...}
  compute_random_f1(gold_sad_code, n_sentences, n_components, gold_sam_code_map) -> (f1, ...)  # confirm exact arity/return at call site lines 342-383
  compute_oracle_f1(gold_sad_code, gold_sam_code_map, gold_sad_sam_maps) -> (f1, oracle_set)
  compute_ndg(system_f1, random_f1, oracle_f1) -> float
  compute_hus(gold_links, result_links) -> {"hus","coverage","purity", ...}   # groups by FIRST tuple element

From src/paper/generate_tables.py:
  ROOT = Path(__file__).resolve().parents[2]; OUT = ROOT/"writing"/"tables"
  latex_escape(cell) -> str
  colspec(header) -> str
  render_table(rows, caption, label, note=None, header_override=None) -> str   # rows = [header] + data rows
  write_table(name, content) -> Path   # writes writing/tables/<name>.tex

CRITICAL tuple-orientation gotchas:
  - sad-sam links are (modelElementID, sentence) => component is element [0], sentence is element [1].
    `compute_hus` groups by element [0] (component). To get SENTENCE-level grouping for sad-sam,
    flip to (sentence, component) before grouping, or build the sentence-level set transform inline
    then call calc_metrics. `transarc_sad_sam_as_ranked` already handles the flip for MAP.
  - sad-code links are (sentence, code_path) => sentence is element [0]; `compute_hus` groups correctly.

REFERENCE VALUES (spot-check targets — API MUST reproduce these since it reuses the same functions):
  - sad-code jabref: File F1 = 0.943, Decision F1 = 0.394 (reports/EVALUATION_CRITIQUE.md line 256).
  - Cross-check additional sad-code File/Decision/Component/Weighted values vs reports/EVALUATION_CRITIQUE.md Part 5.
  - Cross-check alt-metric values vs reports/NEW_METRICS_REPORT.md.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CLI skeleton, imports, schema, CSV+LaTeX emitters with placeholder rows</name>
  <read_first>
    - src/lib/metrics_api.py (the file being created — does not exist yet; create it)
    - src/lib/new_metrics_analysis.py lines 12-35 (import-block + sys.path pattern), 630-662 (per-project load loop), 851-860 (averaging idiom)
    - src/lib/transarc_error_analysis.py lines 22-26 (PROJECTS/BENCHMARK/RESULTS), 296-309 (calc_metrics), 604-610 (load_model_element_names)
    - src/paper/generate_tables.py lines 28-30 (ROOT/OUT), 115-173 (latex_escape/colspec/render_table/write_table), 353-397 (t_s12c_four_level caller idiom)
  </read_first>
  <files>src/lib/metrics_api.py</files>
  <action>
Create `src/lib/metrics_api.py`. Build the shared scaffolding that both task paths use; leave metric computation as placeholders to be filled in Tasks 2 and 3.

1. Stdlib imports: `csv, json, math, os, sys, argparse` plus `from collections import defaultdict` and `from pathlib import Path`.

2. Three `sys.path.insert` blocks (metrics_api.py lives IN src/lib, so the self-import target is `parent`; bias/paper are siblings of the lib dir):
   - `sys.path.insert(0, str(Path(__file__).resolve().parent))` then
     `from transarc_error_analysis import (PROJECTS, BENCHMARK, RESULTS, calc_metrics, load_code_model_files, enroll_gold_standard, load_gs_sad_sam, load_gs_sad_sam_maps, load_gs_sam_code_maps, load_gs_sad_code_enrolled, load_result_sad_sam_standalone, load_result_sad_code, load_text, load_model_element_names)`
   - `sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "bias"))` then
     `from evaluation_critique import (_compute_decision_f1, _compute_component_f1, _compute_weighted_f1, load_sad_code_raw_with_provenance, enroll_with_provenance, load_sam_code_enrolled)`
   - `sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "paper"))` then
     `from generate_tables import render_table, latex_escape, colspec, write_table`
   - `from new_metrics_analysis import (compute_mcc, compute_map, compute_acf1, compute_random_f1, compute_oracle_f1, compute_ndg, compute_hus, transarc_sad_sam_as_ranked)`

3. Module-top constant: `REPORTS = Path("/mnt/hostshare/ardoco-home/transarc-emp/reports")` (hardcoded-absolute-path convention; transarc_error_analysis.py lines 22-24).

4. Unified column schema as a module constant. Anchor on this CSV header key order:
   `SCHEMA = ["project", "link_f1", "sentence_f1", "decision_f1", "component_f1", "file_f1", "weighted_f1", "mcc", "map", "acf1", "ndg", "hus"]`
   `NUMERIC_COLS = SCHEMA[1:]` (everything except "project").
   `NA = "—"` (em dash, for CSV inapplicable cells); `NA_TEX = "--"` (for LaTeX cells).

5. Helper `_fmt(v)`: return `f"{v:.3f}"` if `isinstance(v, (int, float))` else `v` (passes NA through unchanged).

6. `select_projects(args)`: return `[args.project] if args.project else list(PROJECTS)`. If `args.project` is given and not in `PROJECTS`, print an error to stderr and `sys.exit(2)`.

7. Placeholder computation functions (filled later): `compute_sad_sam_row(proj)` and `compute_sad_code_row(proj)`, each returning a dict keyed by every SCHEMA column. For now return `{"project": proj}` plus every NUMERIC_COL set to `NA`, so the file imports and runs end-to-end.

8. `write_csv(task, rows, avg_row)`: open `REPORTS / f"metrics_{task}.csv"` with `newline=""`; `csv.writer`; write `SCHEMA` header, then each project row in SCHEMA order (use `row[col]`), then `avg_row`.

9. `build_avg_row(rows)`: first cell `"Average"`; for each col in NUMERIC_COLS, collect `row[col]` values that are numeric (not NA), append `sum(vals)/len(vals)` if any else `NA`. Return a dict keyed by SCHEMA (project="Average").

10. `write_latex(task, rows, avg_row)`: build display header
    `["Project", "Link \\fone", "Sentence \\fone", "Decision \\fone", "Component \\fone", "File \\fone", "Weighted \\fone", "MCC", "MAP", "ACF1", "NDG", "HUS"]`.
    Build data rows: for each row dict (project rows + avg_row) emit `[row["project"]] + [(_fmt(row[c]) if row[c] != NA else NA_TEX) for c in NUMERIC_COLS]`.
    Call `write_table(f"metrics_{task}", render_table([header] + data_rows, caption="Unified metric set for the %s task." % task, label="tab:metrics-%s" % task, note="Source: reports/metrics_%s.csv." % task, header_override=header))`.
    Use `header_override` because headers contain `\fone` macros (generate_tables.py line 139 emits header_override verbatim, bypassing latex_escape).

11. `main()`: argparse with required `--task` (`choices=["sad-sam","sad-code"]`) and optional `--project`. For each selected project, call `compute_sad_sam_row` or `compute_sad_code_row` by task; collect rows; `avg = build_avg_row(rows)`; `write_csv(task, rows, avg)`; `write_latex(task, rows, avg)`. Print a one-line stdout summary (task, project count, output paths). Guard with `if __name__ == "__main__": main()`.

This task wires the full pipeline with placeholder rows. Tasks 2 and 3 replace the placeholders.
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && python3 src/lib/metrics_api.py --task sad-sam && python3 src/lib/metrics_api.py --task sad-code && python3 -c "import csv; r=list(csv.reader(open('reports/metrics_sad-sam.csv'))); assert len(r)==7,len(r); assert r[0][0]=='project'; assert r[-1][0]=='Average'" && grep -q 'tabular' writing/tables/metrics_sad-code.tex && grep -q 'argparse' src/lib/metrics_api.py && echo SKELETON_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python3 src/lib/metrics_api.py --task sad-sam` exits 0; `python3 src/lib/metrics_api.py --task sad-code` exits 0.
    - `reports/metrics_sad-sam.csv` and `reports/metrics_sad-code.csv` each have exactly 7 CSV rows (header + 5 projects + Average); row 0 col 0 == "project"; last row col 0 == "Average".
    - `writing/tables/metrics_sad-sam.tex` and `writing/tables/metrics_sad-code.tex` each contain the string `tabular`.
    - `grep -c 'def reimplement\|def _f1\|def precision' src/lib/metrics_api.py` returns 0 (no metric math reimplemented).
    - `grep -q 'from transarc_error_analysis import' src/lib/metrics_api.py` and `from generate_tables import` and `from new_metrics_analysis import` and `from evaluation_critique import` all succeed.
  </acceptance_criteria>
  <done>The CLI runs end-to-end for both tasks, writes 7-row CSVs and booktabs .tex tables with placeholder (NA) numeric cells, and imports all four reused modules.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement the sad-sam metric row (link/sentence/component + MCC/MAP/HUS)</name>
  <read_first>
    - src/lib/metrics_api.py (extend the placeholder compute_sad_sam_row)
    - src/lib/transarc_error_analysis.py lines 139-156 (load_gs_sad_sam + maps), 199-208 (load_result_sad_sam_standalone, returns set() if absent), 296-309 (calc_metrics), 604-610 (load_model_element_names)
    - src/lib/new_metrics_analysis.py lines 90-128 (compute_mcc), 209-260 (compute_map + transarc_sad_sam_as_ranked), 420-475 (compute_hus)
    - reports/NEW_METRICS_REPORT.md (spot-check sad-sam MCC/MAP/HUS values)
  </read_first>
  <files>src/lib/metrics_api.py</files>
  <action>
Replace the placeholder `compute_sad_sam_row(proj)` with the real computation. SAD-SAM has NO files and NO enrollment — work directly on `(modelElementID, sentence)` pairs. Do NOT import or call any evaluation_critique `_compute_*` helper here.

1. Load: `gold = load_gs_sad_sam(proj)`; `res = load_result_sad_sam_standalone(proj)`.
   Missing-file skip: `if not res: print(f"WARNING: no sad-sam results for {proj}, skipping", file=sys.stderr); return None`. (main() must drop None rows before CSV/avg — update main() to filter out None and warn if all skipped.)

2. Also load: `sent_to_models, model_to_sents = load_gs_sad_sam_maps(proj)`; `text = load_text(proj)`; `names = load_model_element_names(proj)`; `all_sents = set(text.keys())`; `all_comps = set(names.keys())`.

3. link_f1 (= decision_f1 for sad-sam; every pair is one human decision, zero enrollment inflation):
   `_, _, link_f1, *_ = calc_metrics(gold, res)`. Set both `row["link_f1"] = link_f1` and `row["decision_f1"] = link_f1`.

4. sentence_f1: aggregate to the sentence level — a sentence is correct if it has any correct component. Build sentence-keyed sets by transform then calc_metrics:
   `gold_sent = {s for (_c, s) in gold}` is NOT sufficient (loses correctness). Instead compute per-sentence TP using set logic: a sentence is a TP if `gold-pairs-for-s` and `res-pairs-for-s` share at least one component.
   Concretely: build `gold_by_s = defaultdict(set)` and `res_by_s = defaultdict(set)` over `(c, s)` pairs (key = s, value adds c). Then build `pred_correct_sentences = {s for s in res_by_s if gold_by_s.get(s) and (gold_by_s[s] & res_by_s[s])}`. Build sentence-level link sets and call calc_metrics: `gold_S = {(s, "*") for s in gold_by_s}`; `res_S = {(s, "*") for s in res_by_s if s in pred_correct_sentences}`; then `row["sentence_f1"] = calc_metrics(gold_S, res_S)[2]`. (This reuses calc_metrics; the only inline logic is the set transform, which CONTEXT explicitly sanctions: "all granularities are built by transforming the link set then calling calc_metrics".)

5. component_f1: the sad-sam set is already keyed by modelElementID. Map ids to component names and compare component-keyed sets so synonymous ids collapse:
   `gold_C = {(names.get(c, c), s) for (c, s) in gold}`; `res_C = {(names.get(c, c), s) for (c, s) in res}`; `row["component_f1"] = calc_metrics(gold_C, res_C)[2]`.

6. MCC: `row["mcc"] = compute_mcc(gold, res, all_sents, all_comps)["mcc"]`.

7. MAP: `ranked = transarc_sad_sam_as_ranked(res)`; `row["map"] = compute_map(gold, ranked)[<map key>]` — confirm the exact return-dict key by reading compute_map (lines 209-254); use that key.

8. HUS: sad-sam HUS groups by sentence, but compute_hus groups by element [0]. The raw pairs are (component, sentence), so flip first: `gold_flip = {(s, c) for (c, s) in gold}`; `res_flip = {(s, c) for (c, s) in res}`; `row["hus"] = compute_hus(gold_flip, res_flip)["hus"]`.

9. N/A cells for sad-sam: `row["file_f1"] = NA`; `row["weighted_f1"] = NA`; `row["acf1"] = NA`; `row["ndg"] = NA` (these require enrollment / gold_sam_code_map which sad-sam lacks).

Return the dict keyed by every SCHEMA column.
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && python3 src/lib/metrics_api.py --task sad-sam && python3 -c "
import csv
rows={r['project']:r for r in csv.DictReader(open('reports/metrics_sad-sam.csv'))}
assert 'mediastore' in rows
for p in ['mediastore','teastore','teammates','bigbluebutton','jabref']:
    if p in rows:
        r=rows[p]
        assert r['link_f1']!='—' and 0<=float(r['link_f1'])<=1, (p,r['link_f1'])
        assert r['decision_f1']==r['link_f1']
        assert r['component_f1']!='—'; assert r['mcc']!='—'; assert r['hus']!='—'
        assert r['file_f1']=='—' and r['weighted_f1']=='—' and r['acf1']=='—' and r['ndg']=='—'
print('SADSAM_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - For every present sad-sam project row: `link_f1`, `sentence_f1`, `decision_f1`, `component_f1`, `mcc`, `map`, `hus` are numeric in [0,1] (mcc may be in [-1,1]); and `decision_f1 == link_f1`.
    - For every sad-sam project row: `file_f1`, `weighted_f1`, `acf1`, `ndg` are all the NA sentinel `—`.
    - No evaluation_critique `_compute_*` helper is called inside `compute_sad_sam_row`: `grep -A40 'def compute_sad_sam_row' src/lib/metrics_api.py | grep -c '_compute_decision_f1\|_compute_component_f1\|_compute_weighted_f1'` returns 0.
    - sad-sam alt-metric values match reports/NEW_METRICS_REPORT.md within rounding (spot-check at least MCC and HUS for one project).
  </acceptance_criteria>
  <done>compute_sad_sam_row returns real link/sentence/decision/component F1 + MCC/MAP/HUS from reused functions, with file/weighted/ACF1/NDG marked N/A, and the regenerated CSV reflects this.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement the sad-code metric row (file/decision/component/weighted + MCC/ACF1/NDG/HUS) and verify both tables</name>
  <read_first>
    - src/lib/metrics_api.py (extend the placeholder compute_sad_code_row)
    - src/bias/evaluation_critique.py lines 55-87 (load_sad_code_raw_with_provenance, enroll_with_provenance), 95-99 (load_sam_code_enrolled), 600-684 (_compute_decision_f1/_compute_component_f1/_compute_weighted_f1), 687-760 (part5 caller: file_to_comps construction + call sequence)
    - src/lib/new_metrics_analysis.py lines 274-326 (compute_acf1), 342-398 (compute_random_f1 + compute_oracle_f1), 401-418 (compute_ndg), 420-475 (compute_hus), 90-128 (compute_mcc)
    - src/lib/transarc_error_analysis.py lines 191-198 (load_gs_sad_code_enrolled), 223-232 (load_result_sad_code, returns set() if absent), 170-181 (load_gs_sam_code_maps)
    - reports/EVALUATION_CRITIQUE.md lines 250-285 (File/Decision/Component/Weighted reference values, incl. jabref 0.943/0.394)
  </read_first>
  <files>src/lib/metrics_api.py</files>
  <action>
Replace the placeholder `compute_sad_code_row(proj)` with the real computation, reusing the evaluation_critique helpers verbatim (the part5 caller idiom, lines 687-760, is the exact reference).

1. Load code model + enrolled gold + result:
   `code_model = load_code_model_files(proj)`
   `enrolled = load_gs_sad_code_enrolled(proj, code_model)`   # set[(sentence, code_path)]
   `res = load_result_sad_code(proj)`
   Missing-file skip: `if not res: print(f"WARNING: no sad-code results for {proj}, skipping", file=sys.stderr); return None`.

2. Provenance maps (use the provenance loaders, NOT plain enroll_gold_standard):
   `raw_entries = load_sad_code_raw_with_provenance(proj)`
   `enrolled_prov, raw_to_enrolled, enrolled_to_raw = enroll_with_provenance(raw_entries, code_model)`
   Use `enrolled_prov` as the gold for the decision/component/weighted helpers (it is the provenance-tracked enrolled set matching raw_to_enrolled/enrolled_to_raw). For the plain file-level headline use `enrolled` from step 1 — confirm both are equivalent sets; if they differ in size, prefer `enrolled_prov` consistently for ALL four granularity calls so they agree (read the part5 caller: it uses the provenance `enrolled`). Use one consistent `enrolled` variable for all four calls.

3. file_to_comps map (caller idiom, lines 715-720):
   `names = load_model_element_names(proj)`
   `sam_enrolled = load_sam_code_enrolled(proj, code_model)`   # set[(ae_id, file_path)]
   `file_to_comps = defaultdict(set)`; for `(ae, fp)` in sam_enrolled: `file_to_comps[fp].add(names.get(ae, ae))`.

4. Four granularities (reuse verbatim):
   `_, _, file_f1, *_ = calc_metrics(enrolled, res)`; `row["file_f1"] = file_f1`.
   `row["decision_f1"]  = _compute_decision_f1(enrolled, res, raw_to_enrolled)["f1"]`
   `row["component_f1"] = _compute_component_f1(enrolled, res, file_to_comps)["f1"]`
   `row["weighted_f1"]  = _compute_weighted_f1(enrolled, res, enrolled_to_raw, raw_to_enrolled)["f1"]`

5. Alt metrics (sad-code):
   `gs_sam_code_map, _ = load_gs_sam_code_maps(proj, code_model)`   # model_id -> set(files); confirm element-0 orientation from lines 170-181
   `row["acf1"] = compute_acf1(enrolled, res, gs_sam_code_map)["acf1"]`
   NDG: needs system_f1, random_f1, oracle_f1.
     `system_f1 = file_f1`
     `n_sentences = len({s for (s, _c) in enrolled})`; `n_components = len(gs_sam_code_map)`. `compute_random_f1` returns a BARE FLOAT (verified at lines 342-383: `return f1`, NOT a tuple), so do NOT subscript. Call it with all four positional args (n_components is a real parameter): `random_f1 = compute_random_f1(enrolled, n_sentences, n_components, gs_sam_code_map)`.
     `sad_sam_maps = load_gs_sad_sam_maps(proj)`; `oracle_f1, _ = compute_oracle_f1(enrolled, gs_sam_code_map, sad_sam_maps)`.
     `row["ndg"] = compute_ndg(system_f1, random_f1, oracle_f1)`.
   `row["hus"] = compute_hus(enrolled, res)["hus"]`   # sad-code links are (sentence, code) → groups by sentence correctly, no flip.
   MCC: build the universe of all sentences × all code files.
     `all_sents = {s for (s, _c) in enrolled}`; `all_files = set().union(*gs_sam_code_map.values()) if gs_sam_code_map else set()`.
     `row["mcc"] = compute_mcc(enrolled, res, all_sents, all_files)["mcc"]`.

6. N/A cells for sad-code: `row["link_f1"] = NA`; `row["sentence_f1"] = NA` (link/sentence are sad-sam-only granularities).

7. Update `main()` (if not already done in Task 2) to drop `None` rows returned by the skip path before building the average and writing outputs; if ALL projects skipped, print a stderr warning and still write an (empty-bodied) CSV/LaTeX with just header + Average(all NA) so the run does not crash.

Return the dict keyed by every SCHEMA column.
  </action>
  <verify>
    <automated>cd /mnt/hostshare/ardoco-home/transarc-emp && python3 src/lib/metrics_api.py --task sad-code && python3 -c "
import csv
rows={r['project']:r for r in csv.DictReader(open('reports/metrics_sad-code.csv'))}
for p,r in rows.items():
    if p=='Average': continue
    for col in ['file_f1','decision_f1','component_f1','weighted_f1','acf1','hus','mcc']:
        assert r[col]!='—', (p,col)
    assert r['link_f1']=='—' and r['sentence_f1']=='—', p
if 'jabref' in rows:
    assert abs(float(rows['jabref']['file_f1'])-0.943)<0.01, rows['jabref']['file_f1']
    assert abs(float(rows['jabref']['decision_f1'])-0.394)<0.01, rows['jabref']['decision_f1']
print('SADCODE_OK')
" && python3 src/lib/metrics_api.py --task sad-code --project jabref && grep -q 'tabular' writing/tables/metrics_sad-code.tex && echo ALL_OK</automated>
  </verify>
  <acceptance_criteria>
    - For every present sad-code project row: `file_f1`, `decision_f1`, `component_f1`, `weighted_f1`, `acf1`, `hus`, `mcc` are numeric; `link_f1` and `sentence_f1` are the NA sentinel `—`.
    - jabref `file_f1` ≈ 0.943 and `decision_f1` ≈ 0.394 (within 0.01) — matching reports/EVALUATION_CRITIQUE.md line 256.
    - `python3 src/lib/metrics_api.py --task sad-code --project jabref` exits 0 and produces a CSV with header + jabref row + Average row only.
    - `writing/tables/metrics_sad-code.tex` contains `tabular` and a `\fone` header macro; `grep -c 'def ' src/lib/metrics_api.py` shows the granularity math comes from imports (no local `_compute_*` redefinition: `grep -c 'def _compute_decision_f1\|def _compute_component_f1\|def _compute_weighted_f1' src/lib/metrics_api.py` returns 0).
    - Deleting/renaming one project's results file then rerunning prints a `WARNING: no sad-code results` line to stderr and still exits 0 (skip + warn).
  </acceptance_criteria>
  <done>compute_sad_code_row returns real file/decision/component/weighted F1 + MCC/ACF1/NDG/HUS from reused functions with link/sentence marked N/A; jabref values reproduce the published 0.943/0.394; both CSV and LaTeX regenerate for the full run and for a single-project filter; missing results skip gracefully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → script | Reads project-owned benchmark CSVs (`BENCHMARK/...`), `.acm` code models, and `results/<project>/<task>/` outputs. All inputs are local, project-owned, not network-sourced. |
| CLI args → script | `--task` (constrained by argparse `choices`) and `--project` (validated against PROJECTS). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering/DoS | malformed/missing per-project results CSV | mitigate | Loaders already return `set()` on missing files; skip+warn per project (CONTEXT). No crash on absent input. |
| T-04-02 | Information Disclosure | output paths (`reports/`, `writing/tables/`) | accept | Paths are hardcoded project-relative constants; no user-controlled path component. Offline, no network. |
| T-04-03 | Elevation/Injection | `--project` value used in dict lookup | mitigate | Validated against `PROJECTS` allow-list; reject + exit 2 otherwise. `--task` constrained by argparse `choices`. |

Surface is minimal: offline stdlib script, local file I/O only, no network, no untrusted input. No high-severity threats.
</threat_model>

<verification>
- `python3 src/lib/metrics_api.py --task sad-sam` and `--task sad-code` both exit 0 and write CSV + LaTeX.
- Each CSV has header + 5 project rows + Average row (7 rows) for a full run; single `--project` run has header + 1 row + Average.
- sad-sam rows populate link/sentence/decision/component/MCC/MAP/HUS, N/A for file/weighted/ACF1/NDG.
- sad-code rows populate file/decision/component/weighted/MCC/ACF1/NDG/HUS, N/A for link/sentence.
- jabref sad-code File F1 ≈ 0.943, Decision F1 ≈ 0.394 (reuse parity with EVALUATION_CRITIQUE.md).
- Both `.tex` files contain `tabular` and `\fone` header macros (structural check — no pdflatex available).
- No metric math reimplemented; all values flow from imported functions. No benchmark-derived word lists.
- Missing a project's results file → stderr WARNING + skip, no crash.
</verification>

<success_criteria>
- MTR-01: sad-sam run computes all metrics for all benchmark projects. ✓ (Task 2)
- MTR-02: sad-code run applies enrollment and computes all metrics for all projects. ✓ (Task 3)
- MTR-03: File/Decision/Component (+Link/Sentence/Weighted) P/R/F1 plus alt metrics, by reuse only, zero leakage. ✓ (Tasks 2+3)
- MTR-04: wide CSV per task, all projects in one sheet + Average, stdlib csv. ✓ (Task 1)
- MTR-05: ready-to-paste booktabs LaTeX table per task via generate_tables. ✓ (Task 1, populated by 2+3)
</success_criteria>

<output>
After completion, create `.planning/phases/04-metrics-api/04-01-SUMMARY.md`
</output>
