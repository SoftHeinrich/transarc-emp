---
phase: 09-paper-integration
verified: 2026-06-22T07:10:00Z
status: passed
score: 3/3 success criteria verified (all invariants hold)
overrides_applied: 0
re_verification:
  previous_status: none
  note: initial verification (no prior VERIFICATION.md)
---

# Phase 9: Paper Integration (CMP-05) Verification Report

**Phase Goal:** A reader of `writing/eval.tex` Ch2 sees the level-agnostic component suite, the tail-coverage discriminator (artemis abandons the long tail), and the universe-reconciliation correction, with generated tables.
**Verified:** 2026-06-22T07:10:00Z
**Status:** passed
**Re-verification:** No — initial verification (GSD manual mode; gsd-sdk absent, all checks run by hand)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | New Ch2 section presents the suite (one object, both granularities), the universe-reconciliation correction, the artemis long-tail finding, and the fitness verdict, citing ONLY retained scripts/reports; every cited path exists; numeric claims match the source CSVs | ✓ VERIFIED | `writing/eval.tex` §`sec:eval:component-suite` L1047–1112; all six numeric claims cross-checked against committed CSVs (below) |
| SC2 | Two tables generated by `src/paper/generate_tables.py` into `writing/tables/`; deterministic (byte-identical on 2nd run); registered in `main()`; cell values match CSV AVG rows / pooled means | ✓ VERIFIED | Generator ran clean (exit 0); `WROTE component_suite.tex` + `WROTE metric_fitness.tex`; run1==run2 byte-identical; committed==regenerated; builders registered L731–732 |
| SC3 | `eval.tex` validates structurally (no dangling refs, all `\input` resolve); both new tables `\input` AND `\Cref`'d | ✓ VERIFIED | `check_eval_structure.py` exit 0 (`69 labels, 47 references, 15 \input targets; OK`); negative test confirmed it catches a dangling ref (exit 1); both tables `\input`+`\Cref`'d |

**Score:** 3/3 success criteria verified.

### SC1 numeric cross-check (prose vs committed data)

| Claim (eval.tex) | Prose value | Committed source | Source value | Match |
|---|---|---|---|---|
| artemis AVG micro (sad-model / sad-code) | 0.836 / 0.799 | COMPONENT_SUITE_{sad-model,sad-code}.csv AVG | 0.8355 / 0.7995 | ✓ |
| artemis AVG min_comp (sad-model / sad-code) | 0.279 / 0.345 | same | 0.2788 / 0.3455 (→%.3f 0.345) | ✓ |
| swattr/transarc avg micro/macro gap before→after | −0.099 → −0.019 | reports/COMPONENT_SUITE.md L68; after = COMPONENT_SUITE_sad-code.csv AVG gap −0.0186 | −0.099 / −0.0186 | ✓ |
| bigbluebutton swattr gap before→after | −0.31 → −0.05 | reports/COMPONENT_SUITE.md L69; after = sad-code bbb gap −0.0505 | −0.31 / −0.0505 | ✓ |
| reconciled headline sad-code component F1 | 0.714 → 0.795 | reports/COMPONENT_UNIVERSE_RECONCILIATION.md §2a | 0.7143 → 0.7949 | ✓ |
| re-anchored Sentence-F1 example (BigBlueButton) | sentence 0.876 vs link 0.793, +0.083 gap | reports/metrics_sad-sam.csv (bbb) | 0.876 / 0.793 | ✓ |
| artemis min_comp = 0 on real gold components | bbb+jabref both levels, +teammates doc-to-model | COMPONENT_SUITE_{sad-model,sad-code}.csv | bbb 0.0/0.0, jabref 0.0/0.0, teammates 0.0 (model) / 0.3333 (code) | ✓ |
| s20linker covers every real component (pct missed=0) | pct_missed = 0 | both suite CSVs, s20linker AVG | 0.0000 / 0.0000 | ✓ |
| stale buggy figure removed | no 0.916 / 0.206 anywhere in eval.tex | `grep` of eval.tex | zero matches | ✓ |

All nine SC1 numeric claims trace to committed, retained artifacts. The buggy Sentence-F1 narration (Teammates 0.916 / "0.206 gap") is fully removed.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `writing/eval.tex` §`sec:eval:component-suite` | New Ch2 section, 3 claims, corrected Sentence-F1 | ✓ VERIFIED | L1047–1112; 4 paragraphs (suite intro, second-order aggregation, tail discriminator, headline-status); Sentence-F1 re-anchored at L416–417 |
| `src/paper/generate_tables.py` | `t_component_suite` + `t_metric_fitness`, registered in `main()` | ✓ VERIFIED | defs L609/L661; registered L731–732; CSV-driven (`float(r[...])`), no hardcoded metric values |
| `writing/tables/component_suite.tex` | 3×2 AVG suite, `tab:component-suite` | ✓ VERIFIED | all 30 cells match CSV AVG rows; label present |
| `writing/tables/metric_fitness.tex` | fitness scorecard, `tab:metric-fitness` | ✓ VERIFIED | all 25 cells = pooled mean of the two per-level CSV rows; verdict read from CSV |
| `src/paper/check_eval_structure.py` | stdlib structural validator | ✓ VERIFIED | re/sys/pathlib only; real validation (negative test passes); exit 0 clean |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| eval.tex new section | component_suite.tex / metric_fitness.tex | `\input` + `\Cref` | ✓ WIRED | `\Cref{tab:component-suite}` L1057 + `\input` L1064; `\Cref{tab:metric-fitness}` L1095 + `\input` L1111 |
| `t_component_suite` / `t_metric_fitness` | COMPONENT_SUITE_*.csv / METRIC_FITNESS.csv | `csv.DictReader` AVG/per-level rows | ✓ WIRED | reads AVG rows (suite) and both per-level rows (fitness); no hardcoded numbers |
| `check_eval_structure.py` | eval.tex + tables/*.tex | regex `\label`/`\ref`/`\Cref`/`\input` scan | ✓ WIRED | follows `\input` graph; 15 inputs all resolve |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Generator emits both tables, clean run | `python3 src/paper/generate_tables.py` | exit 0; WROTE both new files | ✓ PASS |
| Determinism | run twice + `cmp` | both tables byte-identical run1==run2 | ✓ PASS |
| No committed-vs-regenerated drift | `cmp` committed vs regenerated | identical | ✓ PASS |
| Structural validator clean | `python3 src/paper/check_eval_structure.py` | exit 0, `69/47/15 OK` | ✓ PASS |
| Validator is not a no-op | inject dangling `\Cref` | reports DANGLING, exit 1; exit 0 after restore | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| CMP-05 | 09-01-PLAN | Publish v1.2 component suite into eval.tex Ch2 (section + 2 generated tables + structural gate) | ✓ SATISFIED | SC1–SC3 all verified above |

### Invariants

| Invariant | Status | Evidence |
|---|---|---|
| stdlib-only (new/edited scripts) | ✓ HELD | generate_tables.py: csv/re/pathlib; check_eval_structure.py: re/sys/pathlib. No third-party imports |
| no benchmark-derived word lists | ✓ HELD | only system ids (swattr/transarc, s20linker, artemis) + column/axis names used as opaque CSV data keys — allowed per invariant |
| every emitted table traces to a retained reports/ artifact | ✓ HELD | both tables' Source notes cite committed CSVs; all source CSVs exist |
| no metric math reimplemented; upstream untouched | ✓ HELD | `git diff --name-only 7111505^ 7111505` = only the 5 intended files; `component_suite.py` / `metric_fitness.py` / `src/lib/metrics_api.py` NOT in the diff; verdict read from CSV, not recomputed |

### Anti-Patterns Found

None. No `TBD`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER`/"coming soon" markers in any of the 5 phase files. No stubs; tables are data-driven; validator performs real work.

### Informational Notes (non-blocking)

- **Citation-home nuance (INFO):** the "−0.099→−0.019 / −0.31→−0.05" before/after gap numbers (L1072–1073) have their literal home in `reports/COMPONENT_SUITE.md` L68–69, while the sentence attributes the paragraph to `COMPONENT_UNIVERSE_RECONCILIATION.md` (which holds the co-cited 0.714→0.795). Both reports are retained and both are cited within the section, so every number traces to a committed, cited artifact. Not a numeric mismatch; not a gap.
- **PDF build (out of scope / deferred):** a real `pdflatex` render is explicitly deferred since v1.1 (no local LaTeX toolchain) and is not a Phase 9 success criterion. Decision D-18 substitutes the structural validator as the SC3 gate; that validator was independently confirmed functional. No human-visual gap is raised because the deliverable is the `.tex` source, every claim was verified against committed data, and PDF rendering is a standing milestone-level deferral rather than a Phase 9 deliverable.

### Human Verification Required

None. Every success criterion and invariant is verifiable from the codebase and was verified programmatically against the committed data. (The only conceivable human step — compiling the PDF — is explicitly out of scope/deferred and is not a phase deliverable.)

### Gaps Summary

No gaps. All three success criteria are achieved in the codebase: (SC1) the new §`sec:eval:component-suite` presents the suite as one object over both granularities, states the universe-reconciliation correction, the artemis long-tail finding, and the fitness verdict, citing only retained scripts/reports, with every numeric claim matching the committed CSVs and the stale 0.916/0.206 figures removed; (SC2) both tables are generated by `generate_tables.py` (registered in `main()`), are deterministic and drift-free, and every cell matches the CSV AVG rows / pooled means; (SC3) `check_eval_structure.py` exits 0 with no dangling refs and all `\input` resolving, and both new tables are `\input` AND `\Cref`'d. All invariants hold, including the untouched-upstream check via the phase commit diff.

---

_Verified: 2026-06-22T07:10:00Z_
_Verifier: Claude (gsd-verifier) — GSD manual mode_
